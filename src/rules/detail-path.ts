/**
 * Single-sourced detailPath resolver + traversal guard (D-08 + IN-05).
 *
 * STUB — the RED phase of 03-02 Task 2. Replaced with the real implementation in
 * the GREEN commit. Kept as a throwing stub so the detail-path.test.ts suite
 * compiles but fails until the resolver + guard land.
 */
export function resolveDetailPath(
  sourceFile: string,
  detailPath: string,
  packRoot: string,
): string {
  void sourceFile;
  void detailPath;
  void packRoot;
  throw new Error("resolveDetailPath not implemented");
}
