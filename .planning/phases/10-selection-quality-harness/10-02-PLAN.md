---
phase: 10-selection-quality-harness
plan: 02
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - src/governance/ship-gate-hook.ts
  - src/governance/ship-gate-hook.test.ts
  - .claude/skills/aidlc-governance-verify/SKILL.md
autonomous: true
requirements: [SEL-06]
must_haves:
  truths:
    - "shipGateHook calls readEvalOrFail BEFORE writing ship evidence; missing eval evidence → throws /ship gate: missing eval evidence <path>/ (D-07, GATE-05)"
    - "shipGateHook calls assertNoFailedEval; criticalRecall < 1.0 → throws /ship gate: eval evidence failed - criticalRecall=<n> (<case misses>)/ (D-05)"
    - "Forward-scoping guard: phases with phaseNumber < '10' skip eval evidence check — legacy phases 06-09 NOT retroactively failed (RESEARCH Open Q2 resolution)"
    - "aidlc-governance-verify SKILL.md step 5 runs 'node dist/select/eval-cli.js <phaseNumber>' AFTER capture-test-evidence, BEFORE audit (D-13)"
    - "Passing eval evidence (criticalRecall===1.0) does not block; ship evidence written"
    - "No ship evidence written on any blocking condition (fail-closed ordering)"
  artifacts:
    - "src/governance/ship-gate-hook.ts (modified — readEvalOrFail + assertNoFailedEval + forward-scoping guard)"
    - "src/governance/ship-gate-hook.test.ts (extended — 4 eval-blocking cases)"
    - ".claude/skills/aidlc-governance-verify/SKILL.md (step 5 eval inserted, step 6 renumbered)"
  key_links:
    - "shipGateHook → readEvalEvidence (from Plan 01 eval-evidence.ts)"
    - "shipGateHook → assertNoFailedEval (D-05 floor enforcement at ship boundary)"
    - "aidlc-governance-verify SKILL.md → dist/select/eval-cli.js (verify:post invocation)"
---

<objective>
Wire Plan 01's eval evidence into the ship gate as a fail-closed prior-evidence check (D-07; GATE-05 pattern), and add the verify:post skill step that runs the harness every governed phase (D-13). Ship becomes blocked on critical-recall regression (SEL-06 ship-blocking half).

Purpose: A persisted failed eval report must not pass ship. Without this plan, Plan 01's exit-2 signal dies at CLI level — a regression slipping past `git commit` would still ship.
Output: readEvalOrFail + assertNoFailedEval in ship-gate-hook.ts, extended ship-gate-hook.test.ts, SKILL.md step 5.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Purpose |
|----------|------|---------|
| `src/governance/ship-gate-hook.ts` | Modified gate hook | `readEvalOrFail` + `assertNoFailedEval` + forward-scoping `phaseNumber >= "10"` guard; inserted before `writeGateEvidence("ship")` |
| `src/governance/ship-gate-hook.test.ts` | Extended test | 4 new cases: missing-eval-fails-closed, failed-eval-fails-closed-with-details, passing-eval-proceeds, legacy-phase-skips-eval-check |
| `.claude/skills/aidlc-governance-verify/SKILL.md` | Modified skill | New step 5 invokes `node dist/select/eval-cli.js <phaseNumber>` after capture-test-evidence, before audit; existing "Propagate failures" becomes step 6 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-selection-quality-harness/10-CONTEXT.md
@.planning/phases/10-selection-quality-harness/10-RESEARCH.md
@.planning/phases/10-selection-quality-harness/10-PATTERNS.md
@.planning/phases/10-selection-quality-harness/10-VALIDATION.md
@.planning/phases/10-selection-quality-harness/10-01-SUMMARY.md
@src/governance/ship-gate-hook.ts
@src/governance/ship-gate-hook.test.ts
@src/governance/eval-evidence.ts
@src/governance/paths.ts
@.claude/skills/aidlc-governance-verify/SKILL.md
</context>

<tasks>

<task type="tdd" phase="red">
  <name>Task 1: RED — extend ship-gate-hook.test.ts with eval-blocking cases</name>
  <read_first>
    - .planning/phases/10-selection-quality-harness/10-PATTERNS.md (§ship-gate-hook.ts, §ship-gate-hook.test.ts — full excerpts with line refs)
    - .planning/phases/10-selection-quality-harness/10-RESEARCH.md (§Pattern 3 readEvalOrFail/assertNoFailedEval, §Pitfall 5 forward-scoping, §Open Question 2)
    - src/governance/ship-gate-hook.ts (full file — locate readApprovalOrFail L108-137, assertNoBlockingApprovals L143-149, readRequiredEvidence L26-38, shipGateHook export L151-162; find insertion point BEFORE writeGateEvidence)
    - src/governance/ship-gate-hook.test.ts (full file — clone approval-blocking cases L225-296, RUNNER pattern L315-323, seedPriorEvidence + makeApproval helpers)
    - src/governance/eval-evidence.ts (from Plan 01 — EvalReport shape + readEvalEvidence + writeEvalEvidence exports)
  </read_first>
  <files>src/governance/ship-gate-hook.test.ts</files>
  <action>
Extend the existing test file with 4 new cases. Tests will fail because `shipGateHook` does NOT yet call `readEvalOrFail`. Reuse existing `withTempRoot`, `seedPriorEvidence`, `makeApproval` helpers.

Add helper `evalReportFixture(status: "pass" | "fail"): EvalReport` — returns a minimal valid EvalReport literal (mirror makeValidEvalReport from Plan 01's validate-eval-report.test.ts). For "pass": all `recallBySeverity` = 1.0, criticalMisses = []. For "fail": `recallBySeverity.critical = 0.5`, criticalMisses = `[{case:"keywords-input-validation", expectedNotSelected:["input-validation"], severity:"critical"}]`, all other severities = 1.0. Both must satisfy eval-report.schema.json.

(a) `shipGateHook fails closed when eval evidence is missing for phase >= 10 (D-07)` — withTempRoot; seed plan + verify evidence (existing seedPriorEvidence); seed approval("approved"); DO NOT seed eval evidence; call `shipGateHook({projectRoot: root, phaseNumber: "10"})`; assert.throws with `/ship gate: missing eval evidence .*\.planning[\\/]governance[\\/]eval[\\/]10\.json/i`; assert `existsSync(gateEvidencePath(root, "10", "ship")) === false` (fail-closed — no ship evidence written).

(b) `shipGateHook fails closed when eval evidence has criticalRecall < 1.0 (D-05)` — withTempRoot; seed plan + verify + approval("approved") + `writeEvalEvidence(root, "10", evalReportFixture("fail"))`; call shipGateHook; assert.throws with `/ship gate: eval evidence failed - criticalRecall=0\.5.*keywords-input-validation.*input-validation/i`; assert no ship evidence written.

(c) `shipGateHook proceeds when eval evidence passes (criticalRecall===1.0) and approval approved (D-05 pass path)` — withTempRoot; seed plan + verify + approval("approved") + `writeEvalEvidence(root, "10", evalReportFixture("pass"))`; call shipGateHook; assert NO throw; assert `existsSync(gateEvidencePath(root, "10", "ship")) === true`.

(d) `shipGateHook skips eval check for legacy phases (phaseNumber < '10') (RESEARCH Open Q2 — forward-scoping)` — withTempRoot; seed plan + verify + approval("approved") for phase "08"; DO NOT seed eval evidence; call `shipGateHook({projectRoot: root, phaseNumber: "08"})`; assert NO throw + ship evidence written. Documents the forward-looking guard — legacy phases shipped without eval evidence and must not retroactively fail.

Tests MUST fail at this commit (readEvalOrFail not yet wired). Commit: `test(10-02): RED — ship-gate eval blocking cases`.
  </action>
  <verify>
    <automated>npm run build:test && node --test dist-test/governance/ship-gate-hook.test.js 2>&1 | tail -40</automated>
  </verify>
  <done>4 new test cases added; running them fails because shipGateHook doesn't read eval evidence yet. Existing approval/plan/verify cases still compile.</done>
</task>

<task type="tdd" phase="green">
  <name>Task 2: GREEN — readEvalOrFail + assertNoFailedEval + forward-scoping guard + SKILL.md step 5</name>
  <read_first>
    - .planning/phases/10-selection-quality-harness/10-PATTERNS.md (§ship-gate-hook.ts Pattern 3 excerpts, §SKILL.md step pattern)
    - .planning/phases/10-selection-quality-harness/10-RESEARCH.md (§Pattern 3, §Pitfall 5)
    - src/governance/ship-gate-hook.ts (readApprovalOrFail L108-137, assertNoBlockingApprovals L143-149, readRequiredEvidence L26-38, shipGateHook export L151-162 — locate insertion BEFORE writeGateEvidence)
    - src/governance/eval-evidence.ts (readEvalEvidence + EvalReport type — from Plan 01)
    - src/governance/paths.ts (evalEvidencePath — from Plan 01)
    - .claude/skills/aidlc-governance-verify/SKILL.md (step 4 capture-test-evidence + step 5 audit + final propagate-failures — locate insertion after capture-test-evidence, renumber subsequent)
  </read_first>
  <files>src/governance/ship-gate-hook.ts, .claude/skills/aidlc-governance-verify/SKILL.md</files>
  <action>
(a) `src/governance/ship-gate-hook.ts` (MODIFY):
  - Import `readEvalEvidence` + `type EvalReport` from `./eval-evidence.js`; import `evalEvidencePath` from `./paths.js`.
  - Add `readEvalOrFail(projectRoot, phaseNumber): EvalReport` — clone `readRequiredEvidence` shape (L26-38): `const report = readEvalEvidence(projectRoot, phaseNumber); if (report === null) throw new Error("ship gate: missing eval evidence " + evalEvidencePath(projectRoot, phaseNumber)); return report;`.
  - Add `assertNoFailedEval(report: EvalReport): void` — D-05 floor: `if (report.aggregate.recallBySeverity.critical < 1.0) { const misses = report.criticalMisses.map(m => m.case + ": " + m.expectedNotSelected.join(", ")).join("; "); throw new Error("ship gate: eval evidence failed - criticalRecall=" + report.aggregate.recallBySeverity.critical + " (" + misses + ")"); }`.
  - Forward-scoping guard (RESEARCH Open Q2 resolution): wrap the eval read+assert pair in `if (phaseNumber >= "10") { const evalReport = readEvalOrFail(args.projectRoot, args.phaseNumber); assertNoFailedEval(evalReport); }`. String compare works because PHASE_NUMBER_RE enforces 2-digit padding. Add comment documenting the threshold — phases < 10 shipped without eval evidence and are not retroactively failed.
  - Wire into `shipGateHook`: AFTER `assertNonBlocking(verifyEvidence, "verify")` AND after `assertNoBlockingApprovals(approval)` — both orderings (eval-before-approval or eval-after-approval) pass the RED tests as long as ALL required evidence is checked BEFORE `writeGateEvidence("ship")`. Pick whichever minimizes diff against existing seed order in tests. MUST be before writeGateEvidence (fail-closed ordering — no ship evidence on blocking condition).

(b) `.claude/skills/aidlc-governance-verify/SKILL.md` (MODIFY — insert step 5; renumber existing audit/propagate steps):
  - After current step 4 (capture-test-evidence), before the audit step, insert step 5:
    "5. **Run the standing eval harness (SEL-06).** AFTER test evidence and BEFORE the audit step, run the recall/precision harness so every governed phase's ship evidence includes a fresh eval run. Invoke `node dist/select/eval-cli.js <phaseNumber>` (pass the concrete {NN} phase number). The harness loads `test/fixtures/eval/cases/eval-cases.json`, builds the index from `test/fixtures/eval/eval-rules/`, runs every case through `select()`, persists `.planning/governance/eval/{NN}.json` + `{NN}-report.md`, and emits pretty markdown to stdout (or JSON with `--json`). Exit 0 = pass; exit 2 = critical-recall regression (blocking); exit 3 = parse/index/load error. Any non-zero exit fails `verify:post` — surface stderr and halt."
  - Renumber the subsequent "Propagate failures" step to 6 (and any later steps accordingly).

Commit: `feat(10-02): GREEN — ship-gate eval blocking + verify:post eval step`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 && node --test dist-test/governance/ship-gate-hook.test.js 2>&1 | grep -E "eval|tests|pass|fail" | tail -20</automated>
  </verify>
  <done>All 4 new eval cases pass + existing ship-gate-hook tests stay green + full npm test green. SKILL.md step 5 inserted, subsequent steps renumbered. Manual: open SKILL.md and confirm ordering reads capture-test-evidence → eval → audit → propagate-failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable-state → ship gate | eval evidence record read from disk; a tampered record could mask a regression |
| verify:post skill → dist entry | SKILL.md invokes `node dist/select/eval-cli.js <NN>`; exit codes feed verify:post pass/fail |
| phaseNumber → ship gate | legacy vs forward phases (06-09 vs 10+) — scoping guard decides if eval check runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Tampering | tampered eval evidence masking regression at ship | critical | mitigate | `readEvalEvidence` re-validates via 4-rung ladder (Plan 01); `assertNoFailedEval` re-derives pass/fail from the persisted `aggregate.recallBySeverity.critical` field — even a forged record can't raise the floor above what `aggregate` (severity from index, eval-harness.ts:123) produced at write time |
| T-10-09 | DoS | ship gate fails closed on missing eval evidence for legacy phases 06-09 (false-positive block) | high | mitigate | Forward-scoping guard `phaseNumber >= "10"` skips eval check for legacy phases (RESEARCH Open Q2); documented threshold; covered by test case (d) |
| T-10-10 | Information Disclosure | silent critical-rule drop passing ship (project's defining anti-pattern) | critical | mitigate | D-05 floor enforced twice: Plan 01 at write time (exit 2 + failed report persisted), Plan 02 at ship time (assertNoFailedEval throws) — defense-in-depth |
| T-10-11 | Repudiation | verify:post step skipped — no eval evidence produced for a phase | medium | mitigate | SKILL.md step 5 invokes dist entry directly (not `npm run eval`); ship gate fails closed on missing eval evidence for phase >= 10 (T-10-08 chain) |
</threat_model>

<verification>
- `npm test` green (full suite).
- `node --test dist-test/governance/ship-gate-hook.test.js` — all 4 new eval cases pass + existing approval/plan/verify cases stay green.
- Manual: read `.claude/skills/aidlc-governance-verify/SKILL.md` — confirm step 4 capture-test-evidence → step 5 eval → audit → propagate-failures ordering.
- End-to-end: `node dist/select/eval-cli.js 10` produces evidence; then a direct call to `shipGateHook({projectRoot: process.cwd(), phaseNumber: "10"})` does NOT throw (corpus passes criticalRecall today).
- Negative: hand-edit `.planning/governance/eval/10.json` setting `aggregate.recallBySeverity.critical` to 0.5 + adding a criticalMisses entry → `shipGateHook` throws with finding details; no ship evidence written.
</verification>

<success_criteria>
- D-07 ship gate consumes eval evidence via fail-closed readEvalOrFail.
- D-05 criticalRecall < 1.0 blocks ship via assertNoFailedEval.
- RESEARCH Open Q2 resolved: forward-scoping guard (phase >= 10); legacy phases not retroactively failed.
- D-13 verify:post SKILL.md step 5 runs harness every governed phase.
- D-16 second half (ship-gate consumption) delivered — TDD plan complete.
</success_criteria>

<output>
Create `.planning/phases/10-selection-quality-harness/10-02-SUMMARY.md` when done.
</output>
