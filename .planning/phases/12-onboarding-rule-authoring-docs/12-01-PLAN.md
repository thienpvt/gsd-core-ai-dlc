---
phase: 12-onboarding-rule-authoring-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/onboarding.md
  - docs/governance-workflow.md
autonomous: true
requirements: [DOC-01, DOC-02]
user_setup: []
must_haves:
  truths:
    - docs/onboarding.md contains a runnable first-run smoke check (build-index + `governance select --phase inception --input <sample-signal>`) that produces non-empty selected[] output containing require-mfa against the built CLI (per CONTEXT.md locked smoke command line 26)
    - docs/onboarding.md documents the CB-3 consent flow as TWO distinct steps: (1) the loader capability consent grant via `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw` (writes consent.recordProjectConsent, content-hashed; flips capability status inactive→active), and (2) the post-consent activation toggle `.planning/config.json` `governance.enabled` (default true). Both must be satisfied for the 6 hooks (discuss/plan/execute/verify/audit/ship) to fire.
    - docs/governance-workflow.md contains all 5 CLI command signatures matching src/cli/commands/*.ts exactly, each with a flags table, copy-pasteable example, sample output, and exit codes
    - docs/governance-workflow.md contains a single end-to-end worked example chaining build-index to select to inject to rule-detail to eval
    - docs/governance-workflow.md documents the audit gate (GOVERNANCE.md) and ship gate chain with evidence file paths
    - both docs end with a Next cross-reference to the next doc in the install-operate-author chain
  artifacts:
    - docs/onboarding.md
    - docs/governance-workflow.md
  key_links:
    - docs/onboarding.md Next-link to docs/governance-workflow.md
    - docs/governance-workflow.md Next-link to docs/rule-authoring.md
---

<objective>
Write the onboarding guide (DOC-01) and governance workflow usage doc (DOC-02). Both are user-facing Markdown deliverables that let an end user install + consent + smoke-check the overlay and operate all 5 CLI commands end-to-end by following docs alone.

Purpose: DOC-01 proves an end user can install and activate governance without reading source code. DOC-02 proves they can operate the full CLI workflow with documented examples as the sole reference.
Output: docs/onboarding.md, docs/governance-workflow.md
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-rule-authoring-docs/12-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@package.json
@src/cli/index.ts
@src/cli/commands/build-index.ts
@src/cli/commands/select.ts
@src/cli/commands/inject.ts
@src/cli/commands/rule-detail.ts
@src/cli/commands/eval.ts
@src/select/eval-cli.ts
@src/schema/task-signal.schema.json
@src/types.ts
@.gsd/capabilities/aidlc-governance/capability.json
@aidlc-rules/enterprise/require-mfa.md
@.claude/skills/aidlc-governance-discuss/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/onboarding.md (DOC-01)</name>
  <files>docs/onboarding.md</files>
  <read_first>
    - src/cli/index.ts — CLI usage text (lines 31-36) for exact subcommand list
    - src/cli/commands/build-index.ts — build-index signature, defaults, output format (lines 1-32)
    - src/cli/commands/select.ts — select signature, --phase requirement, VALID_PHASES (line 34: inception|construction|operations|common), budget resolution (lines 1-180)
    - package.json — name (@opengsd/gsd-aidlc-overlay), version (0.1.0), engines (Node>=22, npm>=10), bin (governance)
    - .gsd/capabilities/aidlc-governance/capability.json — governance.enabled default (line 29), token_budget (line 34), 6 steps with point/ref/when (lines 38-112)
    - .planning/config.json — runtime config shape (governance.token_budget lives here)
    - .planning/codebase/INTEGRATIONS.md — GSD capability integration, skill surface, consent
    - .planning/codebase/STACK.md — Node/npm version requirements
    - .claude/skills/aidlc-governance-discuss/SKILL.md — discuss:pre hook invocation flow
    - src/governance/consent.test.ts — the REAL CB-3 consent grant flow: grantConsent() uses `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw` (lines 253-283), which calls consent.recordProjectConsent() (loader's own consent module, content-hashed). Pre-consent: capability list shows status "inactive" + no hook fires (lines 322-326). Post-consent: status "active" + all 6 hooks fire (lines 330-365). Tamper → reverts to "inactive" + reason "no user consent record" (lines 372-376).
  </read_first>
  <action>
    Create `docs/onboarding.md` with these sections, in this order. Use exact CLI signatures from the read_first source files — do NOT invent flags or defaults.

    1. **Title + intro**: "GSD Governance Overlay — Onboarding Guide" and 1-2 sentence purpose (install, consent-activate, and first-run the governance overlay).

    2. **## Prerequisites**: Node.js `>=22.0.0` and npm `>=10.0.0` (source: `package.json` `engines` field, lines 6-9). Plus GSD Core installed (`@opengsd/gsd-core`) — the overlay extends GSD's discuss/plan/execute/verify/ship loop.

    3. **## Installation**: Three steps:
       - For end users: `npm install @opengsd/gsd-aidlc-overlay` — installs the published package.
       - For in-repo development: `npm install` then `npm run build` — compiles TypeScript to `dist/` via `tsc -p tsconfig.build.json`.
       - GSD Core installer (`gsd-tools`) auto-registers the capability from `.gsd/capabilities/aidlc-governance/capability.json` and skills from `.claude/skills/aidlc-governance-*/SKILL.md`. No manual registration needed.
       - Verify install: `node bin/governance.cjs build-index` should print `build-index: wrote rule-index.json (1 rule) from aidlc-rules` (source: `src/cli/commands/build-index.ts` lines 27-31).

    4. **## Consent Flow (CB-3)**: Two distinct steps, both documented as runnable commands (NOT prose only). Source the exact flow from `src/governance/consent.test.ts` grantConsent() (lines 253-283).
       - **Step A — Capability consent grant (the CB-3 grant):** `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw`. This writes a loader consent record (consent.recordProjectConsent with a content hash of the capability bundle) — the gate that flips the capability from "inactive" to "active" so the 6 hooks fire. Before this grant, `gsd-tools capability list --scope project --json` shows status "inactive" and NO governance hook fires at discuss:pre/plan:pre/execute:pre/verify:post/ship:pre. After the grant, status is "active" and all 6 hooks fire (discuss/plan/execute/verify/audit/ship). Tampering with capability.json invalidates the consent hash → reverts to "inactive" + reason "no user consent record". Document `gsd-tools capability list --scope project --json` as the check command (status field = active|inactive).
       - **Step B — Activation toggle (post-consent):** Set `governance.enabled: true` (or omit it — default true per capability.json line 29) in `.planning/config.json` under a `governance` block. This is the activation predicate checked AFTER consent — `governance.enabled: false` keeps the overlay dormant even with consent granted. `.planning/config.json` `governance.token_budget` (default 2000) also lives here.
       - State clearly: consent is the loader-level grant (capability install); governance.enabled is the per-project on/off switch. Both must be satisfied for hooks to fire. Name all 6 step entries (discuss:pre, plan:pre, execute:pre, verify:post x2 [verify + audit], ship:pre) with their loop point and skill ref from `capability.json` `steps` array (lines 38-112).

    5. **## First-Run Smoke Check**: A concrete, copy-pasteable chain proving governance is active. Use a fenced code block for each step:
       - Step 1: `npm run build` — compile TypeScript to `dist/`.
       - Step 2: `node bin/governance.cjs build-index` — builds `rule-index.json` from `aidlc-rules/`. Expected output: `build-index: wrote rule-index.json (1 rule) from aidlc-rules`.
       - Step 3: Create `task-signal.json` with content: `{ "taskType": "feature", "keywords": ["auth"], "paths": ["src/auth/login.ts"] }`.
       - Step 4: `node bin/governance.cjs select --phase inception --input task-signal.json` — selects rules matching phase `inception`. The shipped `require-mfa` rule has `triggers: {}` (always-in-phase) and `phases: [common]`, and the selector treats `common` as applying to every phase (select.ts inPhase, `record.phases.includes(config.phase) || record.phases.includes("common")`), so require-mfa appears in `selected[]` for `--phase inception`. This matches the locked CONTEXT.md smoke command (line 26).
       - Step 5: Assert non-empty — `selected` array contains `require-mfa`. Explain: non-empty selection proves the index built correctly and the selection engine matched a rule.

    6. **Next**: End with a cross-reference line: "Next: [Governance Workflow Guide](governance-workflow.md)" — links to DOC-02.

    Use exact CLI signatures from `src/cli/index.ts` usage text and the command files. All flag names, defaults, and phase values must match source.
  </action>
  <verify>
    <automated><![CDATA[
      set -e
      cd "$(git rev-parse --show-toplevel)"
      npm run build 2>&1 | tail -1
      node bin/governance.cjs build-index
      echo '{"taskType":"feature","keywords":["auth"],"paths":["src/auth/login.ts"]}' > task-signal.json
      OUT=$(node bin/governance.cjs select --phase inception --input task-signal.json)
      echo "$OUT" | grep -q '"selected"'
      echo "$OUT" | grep -q 'require-mfa'
      test "$(grep -c '## Prerequisites' docs/onboarding.md)" -ge 1
      test "$(grep -c 'governance build-index' docs/onboarding.md)" -ge 1
      test "$(grep -c 'governance select --phase' docs/onboarding.md)" -ge 1
      test "$(grep -c 'capability install' docs/onboarding.md)" -ge 1
      test "$(grep -c 'governance.enabled' docs/onboarding.md)" -ge 1
      test "$(grep -c 'discuss' docs/onboarding.md)" -ge 1
      test "$(grep -c 'ship' docs/onboarding.md)" -ge 1
      test "$(grep -c 'governance-workflow.md' docs/onboarding.md)" -ge 1
      rm -f task-signal.json
      echo "DOC-01 verify passed"
    ]]></automated>
  </verify>
  <acceptance_criteria>
    - docs/onboarding.md contains heading '## Prerequisites'
    - docs/onboarding.md contains a runnable 'governance build-index' command
    - docs/onboarding.md contains a runnable 'governance select --phase inception' command (per CONTEXT.md locked smoke command, line 26)
    - docs/onboarding.md documents the CB-3 consent grant as 'gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw' (the loader consent record, NOT governance.enabled)
    - docs/onboarding.md documents 'governance.enabled' as the separate post-consent activation toggle
    - docs/onboarding.md names both 'discuss' and 'ship' hooks (6-hook chain documented)
    - docs/onboarding.md contains 'governance-workflow.md' (Next cross-reference)
    - running the documented build-index command succeeds (writes rule-index.json)
    - running the documented select command with the documented sample signal returns non-empty selected[] containing require-mfa
  </acceptance_criteria>
  <done>docs/onboarding.md exists with Prerequisites, Installation, Consent Flow (capability install grant + governance.enabled toggle), First-Run Smoke Check sections; the documented smoke check chain (build-index + select --phase inception) runs as written and returns a non-empty selection; Next link to governance-workflow.md present.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/governance-workflow.md (DOC-02)</name>
  <files>docs/governance-workflow.md</files>
  <read_first>
    - src/cli/index.ts — CLI dispatcher usage text (lines 29-37) for all 5 subcommand signatures
    - src/cli/commands/build-index.ts — flags (root default aidlc-rules, out default rule-index.json), output string (lines 1-32)
    - src/cli/commands/select.ts — flags (index, input, phase, domains, budget, format), VALID_PHASES (line 34), budget resolution order (lines 132-148), renderText output format (lines 72-91), exit codes (lines 166-179)
    - src/cli/commands/inject.ts — flags (input), stdin fallback, structural guard, renderInjection output, exit codes (lines 1-124)
    - src/cli/commands/rule-detail.ts — positional id (exactly one), index flag, detailPath resolution, summary-only handling (lines 1-106)
    - src/cli/commands/eval.ts — shim delegating to eval-cli.ts, exit code 3 on parse error (lines 1-21)
    - src/select/eval-cli.ts — runDirect signature, parseArgs (phaseNumber positional + --json flag), exit codes 0/2/3, output format markdown/JSON (lines 261-307)
    - src/schema/task-signal.schema.json — TaskSignal fields (taskType enum, keywords array, paths array), additionalProperties false
    - src/types.ts — TaskSignal interface (lines 127-131), SelectionResult (lines 206-215), SelectedRule (lines 168-174), Severity/Phase/TaskType enums
    - aidlc-rules/enterprise/require-mfa.md — sample rule for rule-detail example output
    - .gsd/capabilities/aidlc-governance/capability.json — verify:post audit step (lines 84-95) and ship:pre step (lines 96-112) for gate chain documentation
    - .planning/codebase/INTEGRATIONS.md — GSD Loop Integration section (lines 119-126) for gate chain evidence file paths
  </read_first>
  <action>
    Create `docs/governance-workflow.md` with these sections. Use exact CLI signatures from the read_first source files — every flag name, default value, and exit code must match.

    1. **Title + intro**: "GSD Governance Overlay — Workflow Guide" and 1-2 sentence purpose (operate the 5 CLI commands and the audit/ship gate chain).

    2. **## Overview**: A table listing all 5 commands with one-line purpose. Source: `src/cli/index.ts` usage block (lines 31-36).

    3. **## TaskSignal Input Format**: Document the TaskSignal schema as a sample `task-signal.json` in a fenced JSON block:
       `{ "taskType": "feature", "keywords": ["auth", "mfa"], "paths": ["src/auth/login.ts"] }`
       Document all 3 required fields: `taskType` (enum: feature|bugfix|refactor|docs|test|infra|security|data — source: `src/schema/task-signal.schema.json` lines 12-22), `keywords` (string array), `paths` (string array, repo-relative POSIX paths). State: no free-form text field — callers pre-tokenize prose into keywords. Source: `src/types.ts` TaskSignal interface (lines 127-131).

    4. **## Commands** — 5 subsections, one per command. Each subsection has: signature (fenced code block), flags table (flag, type, default, description), copy-pasteable example (fenced code block), sample output (fenced block), exit codes list.
       - **### build-index**: Signature `governance build-index [--root <dir>] [--out <file>]`. Flags: `--root` (string, default `aidlc-rules`), `--out` (string, default `rule-index.json`). Example: `node bin/governance.cjs build-index`. Sample output: `build-index: wrote rule-index.json (1 rule) from aidlc-rules`. Exit codes: 0 success. Source: `src/cli/commands/build-index.ts`.
       - **### select**: Signature `governance select --phase <p> [--index <f>] [--input <f>] [--domains a,b] [--budget <n>] [--format json|text]`. Flags table: `--phase` (string, required, one of inception|construction|operations|common), `--index` (string, default rule-index.json), `--input` (string, file path; reads stdin if absent), `--domains` (string, comma-separated, default empty), `--budget` (string parsed as non-negative integer; default from .planning/config.json governance.token_budget or 2000), `--format` (string, json|text, default json). Example: `echo '{"taskType":"feature","keywords":["auth"],"paths":[]}' | node bin/governance.cjs select --phase inception`. Sample output: JSON SelectionResult with selected[], skipped[], budgetExceeded, budget. Exit codes: 0 normal, 1 budget exceeded (result still emitted). Source: `src/cli/commands/select.ts`.
       - **### inject**: Signature `governance inject [--input <file>]`. Flags: `--input` (string, file path; reads stdin if absent). Example: `node bin/governance.cjs select --phase inception --input task-signal.json | node bin/governance.cjs inject`. Sample output: the governance fragment with severity-sorted rule summaries and governance rule-detail pointers. Exit codes: 0 normal, 1 budget exceeded. Source: `src/cli/commands/inject.ts`.
       - **### rule-detail**: Signature `governance rule-detail <id> [--index <f>]`. Positional: `id` (required, exactly one). Flags: `--index` (string, default rule-index.json). Example: `node bin/governance.cjs rule-detail require-mfa`. Sample output for require-mfa (which has NO detailPath): the summary line `All privileged access requires multi-factor authentication.` followed by `(no separate detail file for require-mfa — the summary above is the full rule)`. Document BOTH behaviors: (a) a rule WITHOUT detailPath prints its summary + the no-detail message and exits 0 (require-mfa is the shipped example of this case), and (b) a rule WITH detailPath prints the Markdown body extracted via gray-matter. Exit codes: 0 success (both summary-only and body cases), 1 unknown id. Source: `src/cli/commands/rule-detail.ts` lines 79-86 (summary-only branch) and lines 98-105 (detailPath branch).
       - **### eval**: Signature `governance eval <phaseNumber> [--json]`. Positional: `phaseNumber` (required). Flags: `--json` (boolean, default false; emits JSON instead of markdown). Example: `node bin/governance.cjs eval 12`. Sample output: markdown report with aggregate recall/precision, per-case TP/FP/FN table, critical misses, precision offenders, corpus hash. Exit codes: 0 pass, 2 critical-recall regression (evidence persisted before exit), 3 parse/load error. Source: `src/cli/commands/eval.ts` and `src/select/eval-cli.ts`.

    5. **## End-to-End Worked Example**: A single top-to-bottom narrative chaining all 5 commands in a fenced code block sequence: build-index, `select --phase inception --input task-signal.json`, inject (piping select output), `rule-detail require-mfa` (prints summary + no-detail message — require-mfa has no detailPath), `eval 12 --json` (runs labeled eval corpus; exit 0 pass, 2 critical-recall regression, 3 parse/load error; evidence persists to .planning/governance/eval/12.json). Show each command + abbreviated output inline. This is one narrative, not separate examples.

    6. **## Gate Chain (Audit + Ship)**: Document the two gate-chain components:
       - **Audit gate**: The `aidlc-governance-audit` skill fires at `verify:post` and writes `GOVERNANCE.md` into the phase directory (`.planning/phases/{NN}-*/GOVERNANCE.md`). It reads selection-state.json + CONTEXT.md. Source: `capability.json` steps array (lines 84-95).
       - **Ship gate**: The `aidlc-governance-ship` skill fires at `ship:pre` and blocks release on missing or failing plan/verify/eval evidence or unresolved approval. It consumes `.planning/governance/gates/{NN}-plan.json`, `.planning/governance/gates/{NN}-verify.json`, `GOVERNANCE.md`, and `.planning/governance/approvals/{NN}.json`. Source: `capability.json` steps array (lines 96-112) and `.planning/codebase/INTEGRATIONS.md` GSD Loop Integration section (lines 119-126).

    7. **Next**: End with a cross-reference line: "Next: [Rule Authoring Guide](rule-authoring.md)" — links to DOC-03.

    Use exact signatures from the 5 command files. Do NOT invent flags, defaults, or exit codes.
  </action>
  <verify>
    <automated><![CDATA[
      set -e
      cd "$(git rev-parse --show-toplevel)"
      npm run build 2>&1 | tail -1
      node bin/governance.cjs build-index
      echo '{"taskType":"feature","keywords":["auth"],"paths":[]}' > task-signal.json
      node bin/governance.cjs select --phase inception --input task-signal.json > sel.json
      test "$(grep -c 'require-mfa' sel.json)" -ge 1
      node bin/governance.cjs inject --input sel.json | grep -q '<governance>'
      RD_OUT=$(node bin/governance.cjs rule-detail require-mfa)
      echo "$RD_OUT" | grep -q 'All privileged access'
      echo "$RD_OUT" | grep -q 'no separate detail file'
      EVAL_OUT=$(node bin/governance.cjs eval 12 --json)
      EVAL_EXIT=$?
      # eval exits 0 (pass) or 2 (critical-recall regression, evidence persisted); both prove the CLI ran + emitted JSON.
      # exit 3 = parse/load error → real failure. Reject 3 and any other non-{0,2} code.
      if [ "$EVAL_EXIT" != "0" ] && [ "$EVAL_EXIT" != "2" ]; then
        echo "FAIL: governance eval exited $EVAL_EXIT (expected 0 or 2, 3=parse/load error)" >&2
        exit 1
      fi
      echo "$EVAL_OUT" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const o=JSON.parse(d);if(!o||typeof o.phase==='undefined'){console.error('FAIL: eval JSON missing phase');process.exit(1)}console.log('PASS eval JSON has phase:',o.phase)})"
      test "$(grep -c 'governance build-index' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'governance select' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'governance inject' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'governance rule-detail' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'governance eval' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'task-signal' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'GOVERNANCE.md' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'ship' docs/governance-workflow.md)" -ge 1
      test "$(grep -c 'rule-authoring.md' docs/governance-workflow.md)" -ge 1
      rm -f task-signal.json sel.json
      echo "DOC-02 verify passed"
    ]]></automated>
  </verify>
  <acceptance_criteria>
    - docs/governance-workflow.md contains all 5 command names: 'governance build-index', 'governance select', 'governance inject', 'governance rule-detail', 'governance eval'
    - docs/governance-workflow.md contains a sample 'task-signal' input with taskType, keywords, paths fields
    - docs/governance-workflow.md contains 'GOVERNANCE.md' (audit gate documented)
    - docs/governance-workflow.md contains 'ship' (ship gate documented)
    - docs/governance-workflow.md contains 'rule-authoring.md' (Next cross-reference)
    - running each documented command example (build-index, select, inject, rule-detail, eval) succeeds against the built CLI
    - the end-to-end chain (build-index to select to inject to rule-detail to eval) runs without error
    - rule-detail require-mfa output contains the summary text 'All privileged access' and the 'no separate detail file' message (require-mfa has no detailPath)
  </acceptance_criteria>
  <done>docs/governance-workflow.md exists with all 5 command sections (signature + flags + example + output + exit codes), TaskSignal input format, end-to-end worked example (including eval), and gate chain documentation; every documented command example runs as written (build-index, select, inject, rule-detail, eval); rule-detail require-mfa shows summary-only output (no detailPath); Next link to rule-authoring.md present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-to-CLI | Documentation describes the CLI surface; drift between docs and code misleads users |
| docs-to-user | End user follows documented commands; incorrect commands waste time or cause false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | docs/onboarding.md, docs/governance-workflow.md | medium | mitigate | Dry-run every documented CLI command against the built binary in <verify>; grep assertions confirm exact command strings present |
| T-12-02 | Information Disclosure | docs/governance-workflow.md (sample outputs) | low | accept | Sample outputs are derived from the public rule corpus (require-mfa); no sensitive data in docs |
</threat_model>

<verification>
1. `npm run build` succeeds (CLI compiled to dist/)
2. `node bin/governance.cjs build-index` succeeds and writes rule-index.json
3. `node bin/governance.cjs select --phase inception --input task-signal.json` returns non-empty selection with require-mfa (per CONTEXT.md locked smoke command line 26)
4. `node bin/governance.cjs inject --input sel.json` emits a governance fragment
5. `node bin/governance.cjs rule-detail require-mfa` emits the summary + no-detail message (require-mfa has no detailPath)
6. docs/onboarding.md documents the CB-3 consent grant as `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw` AND governance.enabled as the separate activation toggle
7. docs/onboarding.md contains Prerequisites, Installation, Consent Flow, First-Run Smoke Check sections
8. docs/governance-workflow.md contains all 5 command signatures + flags tables + examples + gate chain
9. Both docs cross-reference the next doc in the chain
</verification>

<success_criteria>
- docs/onboarding.md is followable: a new user can install, consent-activate (capability install grant + governance.enabled toggle), and run the smoke check by following the doc alone
- docs/governance-workflow.md is followable: all 5 CLI commands + gate chain are documented with runnable examples
- Every documented CLI command example runs as-written against the built CLI
</success_criteria>

<output>
Create `.planning/phases/12-onboarding-rule-authoring-docs/12-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- docs/onboarding.md (created — DOC-01)
- docs/governance-workflow.md (created — DOC-02)
- docs/rule-authoring.md (created — DOC-03, Plan 02)
- README.md (created — cross-cutting, Plan 02)