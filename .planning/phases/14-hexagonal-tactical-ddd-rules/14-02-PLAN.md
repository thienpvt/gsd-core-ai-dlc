---
phase: 14-hexagonal-tactical-ddd-rules
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - aidlc-rules/domain/java-spring/java-spring-hex-layering.md
  - aidlc-rules/domain/java-spring/java-spring-ddd-tactical.md
  - aidlc-rules/domain/java-spring/details/java-spring-hex-layering-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-ddd-tactical-detail.md
  - rule-index.json
  - src/select/java-spring-hex-ddd.test.ts
autonomous: true
requirements:
  - JAVA-HEX-01
  - JAVA-DDD-01
must_haves:
  truths:
    - "Subscribing domains java-spring is required before HEX/DDD rules appear in select output"
    - "Two advisory domain rules ship under aidlc-rules/domain/java-spring/ with one-sentence summaries and detailPath targets"
    - "Construction domain/application/adapter/port paths inject hex-layering (inward deps; domain free of Spring/JPA/framework/gateway types)"
    - "Construction aggregate/entity/VO/domain-event paths inject ddd-tactical (AR per boundary; immutable VOs; past-tense events)"
    - "Unrelated README/docs/test/inception and keyword-only bank traps do not select HEX/DDD"
    - "rule-index.json rebuild includes both new ids with summaries and detailPath and no body canaries"
    - "npm test is green including java-spring-hex-ddd suite"
  artifacts:
    - path: aidlc-rules/domain/java-spring/java-spring-hex-layering.md
      provides: "Hexagonal layering advisory rule (JS-HEX-01)"
      contains: "java-spring-hex-layering"
    - path: aidlc-rules/domain/java-spring/java-spring-ddd-tactical.md
      provides: "Tactical DDD advisory rule (JS-DDD-01)"
      contains: "java-spring-ddd-tactical"
    - path: aidlc-rules/domain/java-spring/details/java-spring-hex-layering-detail.md
      provides: "Lazy detail prose for hexagonal layering"
    - path: aidlc-rules/domain/java-spring/details/java-spring-ddd-tactical-detail.md
      provides: "Lazy detail prose for tactical DDD"
    - path: rule-index.json
      provides: "Body-free production index including HEX/DDD pack rules"
  key_links:
    - from: aidlc-rules/domain/java-spring/java-spring-hex-layering.md
      to: rule-index.json
      via: "governance build-index --root aidlc-rules --out rule-index.json"
      pattern: "java-spring-hex-layering"
    - from: src/select/java-spring-hex-ddd.test.ts
      to: aidlc-rules/domain/java-spring
      via: "buildIndex + select domains gate"
      pattern: "java-spring-hex-layering|java-spring-ddd-tactical"
---

<objective>
Author the two Phase 14 advisory rules (hexagonal layering + tactical DDD) and their detail files under the existing `java-spring` pack, rebuild the production rule index, and turn the plan 01 hex-ddd suite GREEN without engine or dependency changes.

Purpose: Deliver JAVA-HEX-01 and JAVA-DDD-01 as path-primary, multi-token-triggered summaries so construction work gets layering/aggregate guidance without always-on architecture essays.
Output: Two rules + two details under `aidlc-rules/domain/java-spring/`, updated `rule-index.json`, green `java-spring-hex-ddd` tests and full `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-CONTEXT.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-RESEARCH.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-VALIDATION.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-01-SUMMARY.md
@src/select/java-spring-hex-ddd.test.ts
@aidlc-rules/domain/java-spring/java-spring-inbound-rest.md
@aidlc-rules/domain/java-spring/details/java-spring-inbound-rest-detail.md
@src/schema/frontmatter.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author hex-layering + ddd-tactical rules and detail files</name>
  <files>aidlc-rules/domain/java-spring/java-spring-hex-layering.md, aidlc-rules/domain/java-spring/java-spring-ddd-tactical.md, aidlc-rules/domain/java-spring/details/java-spring-hex-layering-detail.md, aidlc-rules/domain/java-spring/details/java-spring-ddd-tactical-detail.md</files>
  <action>
    Extend existing pack root `aidlc-rules/domain/java-spring/` (subscription name remains `java-spring`). Ship exactly two new content rules for this phase — no CQRS rule, no pack README, no empty always-on architecture rule, no production `src/` engine edits.

    Shared frontmatter defaults (locked CONTEXT + RESEARCH):
    - scope: domain
    - classification: advisory (omit enforcement — no binding theater)
    - phases: [construction] only
    - severity: medium
    - detailPath: details/&lt;id&gt;-detail.md (relative; loader skips details/)
    - exclude.taskType: [docs]
    - exclude.paths: **/*Test*, **/*Tests*, **/src/test/**
    - summary: exactly one sentence, inject-ready, &lt;= ~160 chars, no newlines

    **1) java-spring-hex-layering** — body heading `## Rule JS-HEX-01: Hexagonal Layering` (JAVA-HEX-01)
    - id: java-spring-hex-layering
    - summary (locked RESEARCH intent): Keep dependencies pointing inward: domain must not import Spring, JPA, or framework/gateway types; adapters implement ports at the edges.
    - triggers.keywords ONLY: hexagonal, ports-and-adapters, inbound-port, outbound-port
      - Never add bare port, domain, adapter, spring, or java needles (substring false positives / always-on noise)
    - triggers.paths: **/domain/**, **/application/**, **/adapter/**, **/adapters/**, **/port/**, **/ports/**
    - Body: short prose on inward dependency direction, domain purity (no Spring stereotypes, JPA, HTTP clients, gateway SDKs in domain), ports as interfaces at edges; ### Verification checklist; include `<!-- BODY_CANARY java-spring-hex-layering -->` matching plan 01
    - Detail: package map example (domain ← application ← adapters), when-not for small single-module CRUD (thin ports + one adapter without full ceremony), explicit note not to mandate CQRS (out of phase), verification checklist, same canary

    **2) java-spring-ddd-tactical** — body heading `## Rule JS-DDD-01: Tactical DDD` (JAVA-DDD-01)
    - id: java-spring-ddd-tactical
    - summary (locked RESEARCH intent): Model one aggregate root per consistency boundary; keep value objects immutable; name domain events in past tense.
    - triggers.keywords ONLY: aggregate-root, value-object, domain-event, tactical-ddd
      - Never add bare entity, event, aggregate, or ddd needles
    - triggers.paths (Entity tightened per RESEARCH discretion / user constraint):
      - **/domain/**
      - **/*Aggregate*
      - **/domain/**/*Entity*   (NOT bare **/*Entity* — avoids EntityManagerConfig / JpaEntityScanner)
      - **/*ValueObject*
      - **/*DomainEvent*
    - Body: one AR per consistency boundary; entities identity-based; immutable VOs; past-tense domain event names (OrderPlaced not PlaceOrder); keep strategic DDD out; ### Verification; `<!-- BODY_CANARY java-spring-ddd-tactical -->`
    - Detail: when-not for simple CRUD internal tools (do not invent aggregates per table); entities should live under domain packages; forward pointer only for strategic maps / CQRS deferred; verification checklist; canary

    Authoring constraints:
    - Match frontmatter schema (kebab id, required fields) — buildIndex/Ajv must accept
    - Never use empty triggers object
    - Both rules may select together on **/domain/** — do NOT mutual-exclude HEX vs DDD
    - Vendor-neutral content (no WSO2/gateway product names required in these two rules)
    - Align BODY_CANARY strings with plan 01 test expectations
    - Reference Phase 13 inbound-rest rule for prose/frontmatter shape; RESEARCH skeletons for full YAML

    Do not touch Phase 13 rule files except if a forward-pointer already exists (optional, not required for success).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node -e "const {buildIndex}=require('./dist-test/index/build.js'); const i=buildIndex('aidlc-rules'); const ids=i.rules.map(r=>r.id); for (const id of ['java-spring-hex-layering','java-spring-ddd-tactical']) { if (!ids.includes(id)) { console.error('missing '+id); process.exit(1);} const r=i.rules.find(x=>x.id===id); if (r.classification!=='advisory') { console.error(id+' not advisory'); process.exit(1);} if (!r.detailPath) { console.error(id+' missing detailPath'); process.exit(1);} if (!r.summary || r.summary.includes('\n') || r.summary.length&gt;160) { console.error(id+' bad summary'); process.exit(1);} } console.log('hex-ddd ids ok');"</automated>
  </verify>
  <done>
    Two rule files and two detail files exist; buildIndex loads both ids with scope domain, classification advisory, non-empty detailPath, one-sentence summaries &lt;=160 chars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild production rule-index.json</name>
  <files>rule-index.json</files>
  <action>
    After rules validate via buildIndex, rebuild the committed production index:

    `node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json`

    (If dist is stale, run `npm run build` first so bin/governance.cjs resolves compiled CLI.)

    Confirm:
    - Prior winners still present (enterprise require-mfa + four Phase 13 java-spring rules)
    - Exactly two new Phase 14 ids appear: java-spring-hex-layering, java-spring-ddd-tactical
    - details/ files are not separate index rules
    - Stringify of rule-index.json must not contain BODY_CANARY java-spring-hex-layering or BODY_CANARY java-spring-ddd-tactical

    Do not hand-edit rule-index.json structure; always regenerate from build-index.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json &amp;&amp; node -e "const fs=require('fs'); const j=fs.readFileSync('rule-index.json','utf8'); if (/BODY_CANARY java-spring-hex-layering|BODY_CANARY java-spring-ddd-tactical/.test(j)) { console.error('canary leak'); process.exit(1);} const i=JSON.parse(j); for (const id of ['java-spring-hex-layering','java-spring-ddd-tactical']) { if (!i.rules.some(r=>r.id===id)) { console.error('missing '+id); process.exit(1);} } const pack=i.rules.filter(r=>String(r.id).startsWith('java-spring')); if (pack.length&lt;6) { console.error('expected at least 6 java-spring rules got', pack.length); process.exit(1);} console.log('index ok', pack.map(r=>r.id).join(','));"</automated>
  </verify>
  <done>
    rule-index.json regenerated with both HEX/DDD records, no body canaries, detailPath pointers intact, Phase 13 pack ids retained.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN hex-ddd suite — fix content/tests until npm test passes</name>
  <files>src/select/java-spring-hex-ddd.test.ts, aidlc-rules/domain/java-spring/java-spring-hex-layering.md, aidlc-rules/domain/java-spring/java-spring-ddd-tactical.md, aidlc-rules/domain/java-spring/details/java-spring-hex-layering-detail.md, aidlc-rules/domain/java-spring/details/java-spring-ddd-tactical-detail.md</files>
  <behavior>
    - All plan 01 matrix tests pass against production aidlc-rules
    - domains=[] out-of-scope; path-primary HEX/DDD positives; CR keyword and EntityManagerConfig negatives
    - docs/test/README/inception excludes; inject summary-only quarantine
    - Full npm test green (Phase 13 pack suite still green)
  </behavior>
  <action>
    Run `npm test` (or targeted `node --test dist-test/select/java-spring-hex-ddd.test.js` after build:test). Drive failures to green by adjusting **content frontmatter/summaries/details** first — not the selection engine.

    Allowed test-file edits: only if canary strings, path fixtures, or assertion wording need alignment with final summaries; do not weaken subscription, CR negatives, EntityManagerConfig negatives, or inject quarantine assertions.

    If infra Entity* still selects DDD, confirm Entity path is `**/domain/**/*Entity*` not bare `**/*Entity*`.

    If keyword-only bank terms select HEX/DDD, remove any bare short needles that snuck into frontmatter.

    Confirm no production `src/**/*.ts` outside tests was modified for selection logic. Confirm zero new npm deps. Confirm no CQRS rule file was created.

    Optional CLI smoke (non-blocking if suite green): select with `--domains java-spring`, construction phase, and a domain path signal.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full npm test green. JAVA-HEX-01 and JAVA-DDD-01 proven by java-spring-hex-ddd suite. Engine frozen. Zero new npm deps. No CQRS. Pack + index ship both new rules.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule authors → rule-index.json → select/inject | Malformed or over-broad rules over-inject architecture guidance into agent context |
| Domain subscription boundary | Unsubscribed consumers must not receive java-spring HEX/DDD conventions |
| Content store vs engine src | Style guidance lives only in Markdown; engine remains frozen and vendor-neutral |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | Unsubscribed domain injection | high | mitigate | scope domain + domains gate; suite proves out-of-scope for HEX/DDD |
| T-14-02 | Spoofing | False-positive selection (bare needles / broad Entity) | high | mitigate | Multi-token keywords only; Entity glob `**/domain/**/*Entity*`; CR negatives in suite |
| T-14-03 | Information Disclosure | Body leak into index/inject | high | mitigate | details/ quarantine, one-sentence summaries, canary checks, rebuild gate |
| T-14-04 | Denial of Service | Essay summaries / always-on architecture | medium | mitigate | Non-empty specific triggers; construction-only; docs/test excludes; summary length asserts |
| T-14-05 | Spoofing | Binding theater without adapter | medium | mitigate | classification advisory on both rules; no enforcement field; no ArchUnit claim |
| T-14-SC | Tampering | Package installs | low | accept | Zero new packages this phase |
</threat_model>

<verification>
`npm test` green; buildIndex(aidlc-rules) returns both HEX/DDD domain rules; selection matrices pass; rule-index.json free of body canaries. Aligns with VALIDATION.md Wave 0 completion and all per-task rows.
</verification>

<success_criteria>
- Construction domain/application/adapter paths inject hexagonal layering (inward deps; domain free of Spring/JPA/framework/gateway types)
- Construction aggregate/entity/VO/domain-event paths inject tactical DDD (AR per boundary; immutable VOs; past-tense events)
- Unrelated README/non-Java/docs/test/inception tasks do not select HEX/DDD
- rule-index.json rebuilt; npm test green; no production engine edits; no new deps; no CQRS
</success_criteria>

<output>
Create `.planning/phases/14-hexagonal-tactical-ddd-rules/14-02-SUMMARY.md` when done
</output>
