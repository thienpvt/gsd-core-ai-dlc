---
phase: 18-verify-ship-wire-consumer-docs
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - .gsd/capabilities/aidlc-governance/capability.json
  - src/governance/phase-18-contract.test.ts
  - docs/java-spring-coverage.md
  - README.md
  - docs/onboarding.md
  - docs/governance-workflow.md
  - .claude/skills/aidlc-governance-discuss/SKILL.md
  - .claude/skills/aidlc-governance-plan/SKILL.md
  - .claude/skills/aidlc-governance-verify/SKILL.md
autonomous: true
requirements:
  - JAVA-DOC-01
must_haves:
  truths:
    - "Capability manifest declares governance.domains and governance.coverage_report_path as type string with default empty string (D-01, D-15)"
    - "Focused guide docs/java-spring-coverage.md shows string domains config, effective SelectionConfig.domains array, report path, evidence path, ship block, no-Java-tool statement (D-10..D-13)"
    - "Guide includes Gradle JaCoCo, Maven JaCoCo, and LCOV producer path examples without requiring overlay to run them (D-12)"
    - "Guide includes troubleshooting for rule absent, missing report, unknown suffix, zero lines, below 70%, absolute/root escape (D-13)"
    - "README, docs/onboarding.md, and docs/governance-workflow.md each link to the focused guide (D-10)"
    - "Contract test locks manifest keys/types/defaults and guide content + three discovery links (D-15)"
    - "Zero new npm dependencies; no format/threshold config documented as configurable"
  artifacts:
    - path: .gsd/capabilities/aidlc-governance/capability.json
      provides: "two additive string config slices"
      contains: "governance.domains|governance.coverage_report_path"
    - path: docs/java-spring-coverage.md
      provides: "focused Java/Spring consumer coverage guide"
      contains: "java-spring|coverage_report_path|jacoco|70%"
    - path: src/governance/phase-18-contract.test.ts
      provides: "manifest + docs discoverability/content lock"
      contains: "governance.domains|java-spring-coverage.md|onboarding"
    - path: README.md
      provides: "root discovery link"
      contains: "docs/java-spring-coverage.md"
    - path: docs/onboarding.md
      provides: "onboarding discovery link"
      contains: "java-spring-coverage.md"
    - path: docs/governance-workflow.md
      provides: "workflow/gate-chain discovery link"
      contains: "java-spring-coverage.md"
  key_links:
    - from: capability.json config slices
      to: .planning/config.json federation
      via: "host dotted keys nest under governance"
      pattern: "governance.domains"
    - from: README / onboarding / governance-workflow
      to: docs/java-spring-coverage.md
      via: "single focused guide, no full onboarding duplication"
      pattern: "java-spring-coverage.md"
    - from: guide config example
      to: Phase 18-01 runtime reader
      via: "string domains + coverage_report_path match readGovernanceConfig"
      pattern: "coverage_report_path"
---

## Artifacts this phase produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `governance.domains` | capability config key | string, default `""` |
| `governance.coverage_report_path` | capability config key | string, default `""` |
| `docs/java-spring-coverage.md` | consumer guide | New focused doc |
| `src/governance/phase-18-contract.test.ts` | contract tests | Manifest + docs lock |
| README / onboarding / workflow links | discovery | Three entrypoints |
| Optional skill marshal notes | skill prompts | Only if current prose misstates auto-routing |

## Frozen files (do not modify)

- `src/enforcement/coverage-report.ts`
- `src/enforcement/run-adapter.ts`
- `src/enforcement/adapters.ts`
- `src/enforcement/types.ts`
- `src/governance/ship-gate-hook.ts`
- Gate schemas
- No new npm dependencies
- Do not re-implement runtime routing here (owned by 18-01)

<objective>
Declare capability config keys, ship focused consumer guide, wire three discovery links, and lock all with contract tests (D-01 capability surface, D-10..D-13, D-15).

Purpose: JAVA-DOC-01 — consumer can subscribe domain pack and configure report path from docs alone without reading engine source.
Output: capability.json slices + docs/java-spring-coverage.md + three links + phase-18-contract.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-verify-ship-wire-consumer-docs/18-CONTEXT.md
@.planning/phases/18-verify-ship-wire-consumer-docs/18-RESEARCH.md
@.planning/phases/18-verify-ship-wire-consumer-docs/18-PATTERNS.md
@.planning/phases/18-verify-ship-wire-consumer-docs/18-01-PLAN.md
@.gsd/capabilities/aidlc-governance/capability.json
@README.md
@docs/onboarding.md
@docs/governance-workflow.md
@docs/rule-authoring.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — phase-18 contract tests for capability + docs</name>
  <files>src/governance/phase-18-contract.test.ts</files>
  <read_first>
    - .planning/phases/18-verify-ship-wire-consumer-docs/18-CONTEXT.md (D-01, D-10..D-13, D-15)
    - .planning/phases/18-verify-ship-wire-consumer-docs/18-RESEARCH.md (doc sections list)
    - .gsd/capabilities/aidlc-governance/capability.json (existing enabled/token_budget shape)
    - README.md Documentation section
    - docs/onboarding.md Activation toggle / Hook chain
    - docs/governance-workflow.md Gate Chain section
    - Any existing static contract test pattern (e.g. config-no-warnings or similar under src/)
  </read_first>
  <behavior>
    - capability.json config has governance.domains type string default ""
    - capability.json config has governance.coverage_report_path type string default ""
    - existing governance.enabled and governance.token_budget still present
    - docs/java-spring-coverage.md exists
    - guide contains exact config keys domains and coverage_report_path as strings
    - guide states effective SelectionConfig.domains: ["java-spring"] (or equivalent quoted form)
    - guide mentions Gradle jacoco / jacocoTestReport path and Maven jacoco.xml path
    - guide mentions LCOV .info or .lcov
    - guide mentions evidence path .planning/governance/gates/ and ship block
    - guide states overlay never invokes Maven/Gradle/JDK
    - guide troubleshooting covers: rule not selected, missing report, unknown suffix, zero lines, below 70%, absolute or root escape
    - guide does NOT claim configurable threshold or format setting
    - README.md links docs/java-spring-coverage.md
    - docs/onboarding.md links java-spring-coverage.md
    - docs/governance-workflow.md links java-spring-coverage.md
  </behavior>
  <action>
    Create `src/governance/phase-18-contract.test.ts` only (RED). No production/docs edits yet. Covers D-01 capability declaration, D-10..D-13 content, D-15 contract lock.

    - Read files from process.cwd() with readFileSync / existsSync / JSON.parse.
    - Assert capability config keys and types/defaults exactly.
    - Assert guide path exists and content canaries for each required section (use stable substrings, not full prose dumps).
    - Assert three discovery files contain the guide link (README uses `docs/java-spring-coverage.md`; docs pages may use relative `java-spring-coverage.md`).
    - Must NOT spawn Maven/Gradle/JDK.
    - Must NOT import or execute createCoverageAdapter.

    RED proof: tests fail because guide/manifest keys missing. Commit: `test(18-02): RED phase-18 capability and docs contract`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/governance/phase-18-contract.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - phase-18-contract.test.ts exists with assertions for manifest keys, guide canaries, three links.
    - Focused run RED (missing guide/keys), not compile error.
    - No Maven/Gradle execution in test body.
  </acceptance_criteria>
  <done>Contract RED suite ready for GREEN docs + capability.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — capability config + focused guide + three discovery links</name>
  <files>.gsd/capabilities/aidlc-governance/capability.json, docs/java-spring-coverage.md, README.md, docs/onboarding.md, docs/governance-workflow.md, .claude/skills/aidlc-governance-discuss/SKILL.md, .claude/skills/aidlc-governance-plan/SKILL.md, .claude/skills/aidlc-governance-verify/SKILL.md</files>
  <read_first>
    - src/governance/phase-18-contract.test.ts (RED canaries — make them pass)
    - .gsd/capabilities/aidlc-governance/capability.json
    - README.md Documentation section
    - docs/onboarding.md Activation toggle section (config example around governance.enabled)
    - docs/governance-workflow.md Gate Chain section
    - .claude/skills/aidlc-governance-discuss/SKILL.md
    - .claude/skills/aidlc-governance-plan/SKILL.md
    - .claude/skills/aidlc-governance-verify/SKILL.md
    - .planning/phases/18-verify-ship-wire-consumer-docs/18-RESEARCH.md documentation section list
  </read_first>
  <action>
    Make contract tests GREEN. Implements D-01 (capability surface), D-10..D-13, D-15.

    **1. capability.json (D-01):**
    Add beside existing keys (do not remove enabled/token_budget):

    ```json
    "governance.domains": {
      "type": "string",
      "default": "",
      "description": "Comma-separated domain subscriptions, for example java-spring."
    },
    "governance.coverage_report_path": {
      "type": "string",
      "default": "",
      "description": "Project-relative path to a consumer-produced JaCoCo XML or LCOV report."
    }
    ```

    Host accepts only boolean/string/number/enum — never array type.

    **2. docs/java-spring-coverage.md (D-10..D-13) — required sections:**
    1. Subscribe + configure example:
       ```json
       {
         "governance": {
           "enabled": true,
           "domains": "java-spring",
           "coverage_report_path": "build/reports/jacoco/test/jacocoTestReport.xml"
         }
       }
       ```
       Explicitly state this resolves to SelectionConfig.domains: ["java-spring"] (string config, array semantics — D-01).
    2. Binding selection boundary: construction task + Java production paths; exclude docs/test/infra and test/generated/build/target (align Phase 17 rule).
    3. Gradle JaCoCo producer: CI runs `./gradlew test jacocoTestReport` before GSD verify; default report path as above.
    4. Maven JaCoCo: CI runs `mvn test`; usual path `target/site/jacoco/jacoco.xml`.
    5. LCOV: existing producer writes `.info` or `.lcov`; set coverage_report_path to that relative path. Do not invent a Java LCOV generator requirement.
    6. Evidence: `.planning/governance/gates/{NN}-verify.json`; failures durable; ship blocks on fail (D-11).
    7. Troubleshooting table rows: rule not selected; missing report; unknown suffix; zero lines; below 70%; absolute/out-of-root path (D-13).
    8. Explicit: overlay reads reports only; never invokes Maven, Gradle, or JDK (D-12).
    9. Format inference only (.xml / .info / .lcov); threshold fixed inclusive 70% — not configurable (D-04 docs fidelity).
    Keep guide focused; do not paste full onboarding.

    **3. Discovery links (D-10):**
    - README.md Documentation list: add bullet to `docs/java-spring-coverage.md` (Java/Spring domain + coverage gate).
    - docs/onboarding.md: near Activation toggle / config example, link `java-spring-coverage.md` for domain subscribe + report path.
    - docs/governance-workflow.md: in Gate Chain (verify/ship), link `java-spring-coverage.md` for binding coverage evidence path.

    **4. Skills (optional, only if needed):**
    - discuss/plan skills: one short note that base domains default from project `governance.domains` when not overridden.
    - verify skill: note automatic coverage-report routing when `java-spring-unit-line-coverage` is selected; report path from `governance.coverage_report_path`.
    - Do not paste implementation logic or decision trees into skills. Skip skill edits if current prose already accurate after 18-01.

    **Do not:** add dependencies, document multi-adapter orchestration, env-var overrides, dynamic loading, or configurable threshold/format.

    GREEN: contract test passes. Commit: `docs(18-02): java-spring coverage consumer guide and capability config`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/governance/phase-18-contract.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - capability.json has both string keys with empty defaults; enabled/token_budget intact.
    - docs/java-spring-coverage.md covers all D-10..D-13 sections including troubleshooting and no-Java-tool statement.
    - README, onboarding, governance-workflow each link the guide.
    - phase-18-contract.test.js fully green.
    - package.json deps unchanged.
  </acceptance_criteria>
  <done>JAVA-DOC-01 discoverability + capability surface complete (D-01, D-10..D-13, D-15).</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate</name>
  <files></files>
  <read_first>
    - src/governance/phase-18-contract.test.ts
    - .planning/phases/18-verify-ship-wire-consumer-docs/18-01-PLAN.md success criteria
  </read_first>
  <action>
    Run full `npm test` to prove 18-01 wire + 18-02 docs/capability do not regress prior suites (inventory, coverage adapter, ship, stubs).

    If failures:
    - Prefer fixing contract canaries that over-specified prose.
    - Do not weaken 18-01 security assertions (bypass, fail-loud config, durable empty-path fail).
    - Do not edit frozen enforcement files or ship production unless a true regression is proven.

    Confirm zero new package.json dependencies vs pre-phase baseline.

    Optional: if skill files were edited, ensure no accidental code fences that re-state incorrect adapter defaults (generic-exit-ci always).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (platform skips only, no fails).
    - phase-18-contract + 18-01 focused suites still green.
    - No new npm deps in package.json.
    - Frozen files still unmodified relative to 18-01 end state.
  </acceptance_criteria>
  <done>Phase 18 plans green end-to-end; ready for verify-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability.json → federated project config | Declared types constrain host validation of consumer settings |
| consumer docs → operator actions | Docs can mislead into wrong config types or false sense of enforcement |
| guide producer commands → CI | Commands are examples only; must not be executed by overlay tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Spoofing / Confusion | capability config types | medium | mitigate | Declare string (not array) for domains; contract test locks type/default (D-01, D-15); docs show string + effective array |
| T-18-09 | Tampering / Misconfiguration | docs/java-spring-coverage.md | medium | mitigate | Contract canaries for config keys, fail-closed semantics, fixed 70%, no-Java-tool statement |
| T-18-10 | Bypass via docs gap | discovery entrypoints | medium | mitigate | Three required links + contract asserts; consumer need not read src/ |
| T-18-11 | Elevation via tool invocation | doc producer examples | high | mitigate | Explicit overlay-never-invokes statement; tests inspect text only, never spawn Maven/Gradle/JDK (D-12) |
| T-18-12 | Information Disclosure | docs leaking internal bypass recipes | low | mitigate | Document bypass rejection behavior at high level; do not teach how to force generic-exit-ci while binding selected |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new dependencies (D-15); no install tasks |
| T-18-01..05 | (runtime) | covered in 18-01 | high | mitigate | Plan 02 does not re-open runtime routing; depends_on 18-01 |
</threat_model>

<verification>
```bash
npm run build:test && node --test "dist-test/governance/phase-18-contract.test.js"
npm test
```
</verification>

<success_criteria>
- JAVA-DOC-01 satisfied: subscribe + report path documented and discoverable
- Capability keys declared string/empty defaults
- Contract tests lock manifest + guide + three links
- Full suite green; zero new deps
- D-01 (manifest), D-10..D-13, D-15 covered
</success_criteria>

<output>
Create `.planning/phases/18-verify-ship-wire-consumer-docs/18-02-SUMMARY.md` when done
</output>
