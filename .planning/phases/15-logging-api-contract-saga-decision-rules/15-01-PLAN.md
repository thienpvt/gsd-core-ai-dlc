---
phase: 15-logging-api-contract-saga-decision-rules
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/select/java-spring-log-api-evt.test.ts
autonomous: true
requirements:
  - JAVA-LOG-01
  - JAVA-API-01
  - JAVA-EVT-01
must_haves:
  truths:
    - "domains=[] never selects java-spring-logging-audit, java-spring-api-contract, or java-spring-saga-outbox (out-of-scope)"
    - "Subscribed construction + logging/config/*Log*/aop paths or multi-token log keywords can select logging-audit"
    - "Subscribed construction + api/openapi/*Resource/web paths or multi-token API keywords can select api-contract"
    - "Subscribed construction + outbox/saga/messaging paths or multi-token saga/outbox keywords can select saga-outbox"
    - "Bare log/logger and bare rest keyword traps select none of the three LOG/API/EVT rules"
    - "docs taskType, test paths, README/non-Java paths, and inception phase select none of the three"
    - "When selected, inject contains one-sentence summaries only — no BODY_CANARY and no ## Rule JS-LOG-01 / JS-API-01 / JS-EVT-01 essays"
  artifacts:
    - path: src/select/java-spring-log-api-evt.test.ts
      provides: "RED (then GREEN with plan 02) node:test suite for JAVA-LOG-01 / JAVA-API-01 / JAVA-EVT-01 matrices + bare-needle negatives"
      contains: "java-spring-logging-audit|java-spring-api-contract|java-spring-saga-outbox|JAVA-LOG-01|JAVA-API-01|JAVA-EVT-01"
  key_links:
    - from: src/select/java-spring-log-api-evt.test.ts
      to: aidlc-rules (via buildIndex)
      via: "buildIndex(path.resolve(cwd,'aidlc-rules')) then select/renderInjection"
      pattern: "buildIndex\\(|select\\(|renderInjection\\("
---

<objective>
Write the Phase 15 LOG/API/EVT proof suite first (TDD RED): path-primary and multi-token keyword selection for logging-audit, api-contract, and saga-outbox; bare-needle and CR-style false-positive negatives; docs/test/inception excludes; inject summary-only quarantine against the real `aidlc-rules` root.

Purpose: Lock JAVA-LOG-01, JAVA-API-01, and JAVA-EVT-01 as failing automated tests before content lands so rule authoring cannot drift from CONTEXT contracts (no bare log/rest, construction-only, docs/test excludes, advisory summaries).
Output: `src/select/java-spring-log-api-evt.test.ts` encoding the full matrix; suite fails until plan 02 content exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-logging-api-contract-saga-decision-rules/15-CONTEXT.md
@.planning/research/FEATURES.md
@src/select/java-spring-hex-ddd.test.ts
@src/select/java-spring-pack.test.ts
@src/select/select.ts
@src/inject/inject.ts
@src/index/build.ts
@src/index/precedence.test.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED log-api-evt suite — path matrices, bare-needle negatives, inject quarantine</name>
  <files>src/select/java-spring-log-api-evt.test.ts</files>
  <behavior>
    - Hygiene: buildIndex(aidlc-rules) succeeds; JSON.stringify(index) lacks BODY_CANARY for each of the three ids; details/ files are not separate index rules
    - Subscription: domains=[] + rich log/api/saga signal (logging path + openapi + saga keywords) → none of LOG_API_EVT_IDS selected; each skipped with reason out-of-scope
    - JAVA-LOG-01 positive paths (domains=["java-spring"], phase construction): .../logging/... selects logging-audit; .../config/*Log* selects logging-audit; .../aop/... selects logging-audit
    - JAVA-LOG-01 keyword positives (paths empty): correlation-id, trace-id, mdc, audit-log, structured-logging each select logging-audit
    - JAVA-API-01 positive paths: .../api/... selects api-contract; .../openapi/... selects api-contract; .../*Resource.java selects api-contract; .../web/... selects api-contract
    - JAVA-API-01 keyword positives: openapi, api-version, error-envelope, swagger-spec each select api-contract
    - JAVA-EVT-01 positive paths: .../outbox/... selects saga-outbox; .../saga/... selects saga-outbox; .../messaging/... selects saga-outbox
    - JAVA-EVT-01 keyword positives: saga, outbox, transactional-outbox, choreography, orchestration, distributed-transaction each select saga-outbox
    - Bare-needle negatives (keyword-only, paths []): ["log"] → none of three; ["logger"] → none; ["rest"] → none of three (api-contract must not fire on bare rest)
    - Unrelated/CR-style: README.md / non-Java path only → none; taskType docs + matching path → none; **/src/test/** or *Test* path → none; phase inception + matching path → none (out-of-phase)
    - Summary contract (once records exist): each LOG/API/EVT record is scope domain, classification advisory, detailPath non-empty, summary one line no newline length &lt;= 160; LOG summary encodes correlation|trace|PII|secret|audit language; API summary encodes openapi|version|error envelope|correlationId language; EVT summary encodes saga|outbox|when-not|ACID|plain language
    - Inject quarantine: when any of the three selected, renderInjection contains that rule summary and does NOT contain BODY_CANARY tokens or "## Rule JS-LOG-01" / "## Rule JS-API-01" / "## Rule JS-EVT-01"
  </behavior>
  <action>
    Create sibling suite `src/select/java-spring-log-api-evt.test.ts` (do NOT extend Phase 13 PACK_IDS or Phase 14 HEX_DDD_IDS inventories — keeps prior suite assertions stable per Phase 14 pattern and CONTEXT proof decision).

    Mirror patterns from `src/select/java-spring-hex-ddd.test.ts`: `node:test` + `node:assert/strict`, `buildIndex` from `../index/build.js`, `select` from `./select.js`, `renderInjection` from `../inject/inject.js`, types from `../types.js`.

    Constants (locked CONTEXT Rule Inventory + Triggers):
    - PACK_ROOT = path.resolve(process.cwd(), "aidlc-rules")
    - LOG_API_EVT_IDS = ["java-spring-logging-audit", "java-spring-api-contract", "java-spring-saga-outbox"]
    - BODY_CANARIES for each id: `BODY_CANARY java-spring-logging-audit`, `BODY_CANARY java-spring-api-contract`, `BODY_CANARY java-spring-saga-outbox`
    - RULE_ESSAY_HEADINGS: "## Rule JS-LOG-01", "## Rule JS-API-01", "## Rule JS-EVT-01"
    - SUBSCRIBED = { phase: "construction", domains: ["java-spring"] }
    - UNSUBSCRIBED = { phase: "construction", domains: [] }

    Prefer production `aidlc-rules` root via buildIndex — missing pack content is the expected RED cause. Do not invent a duplicate fixture store.

    Path fixtures to encode (bank-realistic Java paths under a fictional module, e.g. `src/main/java/com/bank/payments/...`):
    - logging/CorrelationMdcFilter.java, config/LogbackConfig.java, aop/AuditLoggingAspect.java
    - api/v1/PaymentResource.java (or *Resource.java), openapi/OpenApiConfig.java, web/PaymentWebAdvice.java
    - outbox/OutboxPublisher.java, saga/PaymentSagaOrchestrator.java, messaging/PaymentOutboxListener.java
    - README.md, docs-only and test paths for exclude cases
    - Optional discretion path (if suite wants filter coverage later): filter/CorrelationIdFilter.java — only assert if plan 02 elects **/filter/**; do not require filter path positive in RED matrix unless handoff comment locks it

    Document in a file-level handoff comment for plan 02:
    - LOG_API_EVT_IDS, BODY_CANARY strings, essay headings
    - Locked keyword lists from CONTEXT (no bare log/logger/rest)
    - Locked path globs from CONTEXT
    - Shared frontmatter defaults: scope domain, classification advisory, phases construction only, severity medium, exclude docs + test paths, one-sentence summary &lt;=160
    - Saga when-NOT contract: single-service ACID → plain call; same-TX DB+message → outbox; multi-service business TX → saga
    - Inventory note: plan 02 grows precedence.test.ts winners 7 → 10 (require-mfa + nine java-spring rules)

    Do not modify any production `src/**` modules outside this new test file. Do not author rule Markdown in this plan. Do not edit precedence.test.ts inventory here (plan 02). Zero new npm deps. Engine stays frozen.

    Expected RED: suite fails because LOG/API/EVT index entries are missing or selection/summary assertions fail.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-log-api-evt.test.js</automated>
  </verify>
  <done>
    Test file exists, compiles, and fails for the right reasons (missing LOG/API/EVT rules or failed content assertions). Named tests cover JAVA-LOG-01, JAVA-API-01, JAVA-EVT-01 matrices, bare-needle negatives, excludes, inject quarantine.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm RED and document expected GREEN handoff</name>
  <files>src/select/java-spring-log-api-evt.test.ts</files>
  <action>
    Run the targeted log-api-evt suite after `npm run build:test` and confirm failures are content-missing / assertion failures — not import or TypeScript errors.

    If the suite fails to compile due to import paths, fix imports only inside the test file to match project `.js` suffix conventions used in `java-spring-hex-ddd.test.ts`.

    Do not author production rules in this plan (plan 02 owns content). Do not rebuild `rule-index.json` here. Do not change `src/index/precedence.test.ts` inventory yet.

    Ensure the file-level comment lists LOG_API_EVT_IDS, BODY_CANARY tokens, essay headings, locked keyword/path sets, saga when-NOT decision table intent, and the 7→10 inventory handoff so plan 02 authors match tests exactly.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-log-api-evt.test.js ; test $? -ne 0</automated>
  </verify>
  <done>
    Suite is RED with clear missing-content failures. No production rule files, index rebuild, or inventory lock change in this plan. Handoff comment documents canaries and ids for plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → aidlc-rules / src filesystem | Tests read rule store; must not execute untrusted code from rules |
| Future inject path ← selected summaries | Essay-length or always-on LOG/API/EVT summaries would bloat model context (core-value threat) |
| Domain subscription gate | Unsubscribed consumers must not receive java-spring LOG/API/EVT guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | Domain pack without subscribe | high | mitigate | domains=[] → LOG/API/EVT out-of-scope asserts in suite (JAVA-LOG-01 / JAVA-API-01 / JAVA-EVT-01) |
| T-15-02 | Spoofing | Selection false positives (bare log/rest needles) | high | mitigate | Keyword-only negatives for bare log, logger, rest; multi-token keyword contract only per CONTEXT |
| T-15-03 | Information Disclosure | Index/inject body leak; PII guidance theater without quarantine | high | mitigate | BODY_CANARY and essay-heading absence asserts on index JSON and renderInjection; summary-only inject |
| T-15-04 | Denial of Service | Always-on logging/API essays / over-broad bare needles | medium | mitigate | Non-empty path/keyword positives required; construction-only; summary length &lt;=160; docs/test/README negatives |
| T-15-05 | Spoofing | Saga cargo-cult / missing when-NOT | medium | mitigate | Suite summary contract requires when-not/ACID/plain language for EVT rule once content exists |
| T-15-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs zero packages; no supply-chain surface |
</threat_model>

<verification>
Targeted log-api-evt suite runs under node:test; RED proven before content plan. Aligns with Nyquist: every production behavior has an automated failing test first under tdd_mode.
</verification>

<success_criteria>
- `src/select/java-spring-log-api-evt.test.ts` exists with named tests for JAVA-LOG-01, JAVA-API-01, JAVA-EVT-01 matrices plus bare-needle negatives
- `npm run build:test` succeeds
- Log-api-evt suite fails (RED) until plan 02 lands content
- No production `src/` changes outside the new test file; zero new deps; Phase 13/14 suites left intact; inventory lock unchanged in this plan
</success_criteria>

<output>
Create `.planning/phases/15-logging-api-contract-saga-decision-rules/15-01-SUMMARY.md` when done
</output>
