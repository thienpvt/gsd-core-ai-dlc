---
phase: 17-coverage-parser-binding-gateadapter
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/select/java-spring-coverage.test.ts
  - aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md
  - aidlc-rules/domain/java-spring/details/java-spring-unit-line-coverage-detail.md
  - src/index/precedence.test.ts
  - src/select/starter-examples.test.ts
autonomous: true
requirements:
  - JAVA-COV-01
must_haves:
  truths:
    - "Index contains binding rule id java-spring-unit-line-coverage with classification binding, enforcement coverage-report, severity high, phases construction"
    - "Heading is ## Rule JS-COV-01: Unit Line Coverage; summary is one sentence ≤160 chars"
    - "Positive triggers are paths-only (**/src/main/java/**, **/src/main/**/*.java); no positive taskType or keywords — engine OR-combines positive axes, so taskType positives would over-select non-Java work"
    - "Java production paths select under construction + java-spring domain regardless of feature/bugfix/refactor taskType"
    - "docs/test/infra taskType excluded even when path matches Java production globs"
    - "feature/bugfix/refactor on non-Java paths (e.g. src/main/ts/foo.ts, README.md) do not select"
    - "paths under src/test, generated, build, target do not select"
    - "BODY_CANARY java-spring-unit-line-coverage present in body and detail; absent from rule-index serialization and renderInjection"
    - "Real-corpus inventory grows 10 → 11 winners in both precedence.test.ts and starter-examples.test.ts"
  artifacts:
    - path: aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md
      provides: "binding rule frontmatter + body + BODY_CANARY"
      contains: "java-spring-unit-line-coverage|classification: binding|enforcement: coverage-report|JS-COV-01"
    - path: aidlc-rules/domain/java-spring/details/java-spring-unit-line-coverage-detail.md
      provides: "measurement contract detail + BODY_CANARY"
      contains: "BODY_CANARY java-spring-unit-line-coverage|covered \\* 100|LINE"
    - path: src/select/java-spring-coverage.test.ts
      provides: "binding metadata, paths-only triggers, exclude + non-Java negatives, quarantine suite"
      contains: "JAVA-COV-01|java-spring-unit-line-coverage|BODY_CANARY"
    - path: src/index/precedence.test.ts
      provides: "inventory lock expectedIds includes java-spring-unit-line-coverage length 11"
    - path: src/select/starter-examples.test.ts
      provides: "INVENTORY_COUNT = 11"
  key_links:
    - from: aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md
      to: src/index/build.ts
      via: "buildIndex loads binding+enforcement fields"
      pattern: "enforcement|classification"
    - from: src/select/java-spring-coverage.test.ts
      to: src/select/select.ts
      via: "select() paths-only positive + exclude matrices"
      pattern: "select\\(|isSelected"
    - from: src/index/precedence.test.ts
      to: aidlc-rules
      via: "expectedIds length 11 includes coverage rule"
      pattern: "java-spring-unit-line-coverage"
---

<objective>
Author the binding `java-spring-unit-line-coverage` rule + detail, lock selection/metadata/quarantine with a sibling TDD suite, and bump corpus inventory 10 → 11 (JAVA-COV-01).

Purpose: Binding ≥70% obligation is selectable under construction for Java production paths without circular test-only triggers and without over-selecting non-Java work.
Output: rule + detail under `aidlc-rules/domain/java-spring/`, `src/select/java-spring-coverage.test.ts`, inventory lock updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-CONTEXT.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-RESEARCH.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-PATTERNS.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-VALIDATION.md
@aidlc-rules/domain/java-spring/java-spring-api-contract.md
@aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md
@src/select/java-spring-pack.test.ts
@src/index/precedence.test.ts
@src/select/starter-examples.test.ts
@src/index/build.ts
@src/schema/classification.test.ts
@src/select/select.ts
</context>

## Artifacts this phase produces

| Path | Kind | Symbols / notes |
|------|------|-----------------|
| `aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md` | rule | id `java-spring-unit-line-coverage`; heading `## Rule JS-COV-01: Unit Line Coverage` |
| `aidlc-rules/domain/java-spring/details/java-spring-unit-line-coverage-detail.md` | detail | measurement contract; BODY_CANARY |
| `src/select/java-spring-coverage.test.ts` | test | sibling suite (does not extend pack/hex/log inventories) |
| `src/index/precedence.test.ts` | test mod | expectedIds + length 11 |
| `src/select/starter-examples.test.ts` | test mod | `INVENTORY_COUNT = 11` + assertion prose |

**Out of scope:** parser/adapter code (plan 01); verify-gate-hook auto-select (Phase 18); docs entrypoints (Phase 18); production `src/` engine edits beyond the two inventory test files + new coverage suite.

**Trigger design note (engine-compatible realization of locked Java-production-work intent):** Selection positive axes OR-combine (taskType → keywords → paths). Populating positive `triggers.taskType` with feature/bugfix/refactor would select the rule on any such task even without a Java path. Paths-only positives (`**/src/main/java/**`, `**/src/main/**/*.java`) plus `exclude.taskType` docs/test/infra and exclude paths is therefore the only correct realization of "Java production work" under the current engine.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — coverage rule suite + inventory locks expect 11</name>
  <files>src/select/java-spring-coverage.test.ts, src/index/precedence.test.ts, src/select/starter-examples.test.ts</files>
  <read_first>
    - src/select/java-spring-pack.test.ts (PACK_ROOT, BODY_CANARY hygiene, select helpers, summary ≤160)
    - src/select/select.ts (positive axes OR-combine; exclude after match)
    - src/index/precedence.test.ts lines 120-149 (expectedIds inventory lock)
    - src/select/starter-examples.test.ts lines 1-30 and 221-228 (INVENTORY_COUNT)
    - aidlc-rules/domain/java-spring/java-spring-api-contract.md (frontmatter shape analog)
    - src/schema/classification.test.ts (binding requires enforcement)
    - .planning/phases/17-coverage-parser-binding-gateadapter/17-CONTEXT.md Binding Rule Contract
  </read_first>
  <behavior>
    - buildIndex(aidlc-rules) includes id java-spring-unit-line-coverage
    - record.classification === "binding"; record.enforcement === "coverage-report"; record.severity === "high"; record.scope === "domain"; phases include construction
    - summary one line, length ≤160, ends with sentence punctuation
    - Positive (paths-only): path `src/main/java/com/acme/Foo.java` or `src/main/Foo.java` under domains java-spring + phase construction → selected (taskType may be feature, bugfix, refactor, or other non-excluded)
    - Negative exclude.taskType: docs|test|infra with matching Java production path → NOT selected
    - Negative non-Java production work: feature/bugfix/refactor with paths such as `src/main/ts/app.ts`, `src/main/python/app.py`, `README.md`, `package.json` → NOT selected
    - Negative exclude paths: `src/test/java/FooTest.java`, `generated/...`, `build/...`, `target/...` with any taskType → NOT selected
    - BODY_CANARY java-spring-unit-line-coverage in rule body file and detail file; JSON.stringify(index) and renderInjection lack canary and lack `## Rule JS-COV-01`
    - precedence expectedIds length 11 including java-spring-unit-line-coverage
    - starter-examples INVENTORY_COUNT === 11
  </behavior>
  <action>
    Write failing suite + inventory expectation bumps BEFORE authoring the rule (RED).

    **src/select/java-spring-coverage.test.ts** (sibling suite pattern from java-spring-pack.test.ts):
    - Imports: node:test, node:assert/strict, node:fs/path, buildIndex, select, renderInjection, types as needed.
    - Constants: RULE_ID = `java-spring-unit-line-coverage`; BODY_CANARY = `BODY_CANARY java-spring-unit-line-coverage`; PACK_ROOT = aidlc-rules; SUBSCRIBED = `{ phase: "construction", domains: ["java-spring"] }`.
    - Tests:
      1. Index contains RULE_ID with classification binding, enforcement coverage-report, severity high, scope domain, detailPath set, non-empty summary ≤160 single line.
      2. Positive path selects: path `src/main/java/com/acme/Foo.java` and path `src/main/Foo.java` → isSelected (use a non-excluded taskType such as feature for the signal; selection must be path-driven, not taskType-driven).
      3. Negative exclude.taskType: docs, test, infra with production Java path → not selected.
      4. Negative non-Java feature/bugfix/refactor: taskType feature|bugfix|refactor with non-Java paths (`src/main/ts/app.ts`, `src/main/python/app.py`, `docs/guide.md`, `package.json`) → not selected. This locks the paths-only design against the over-select bug of positive taskType OR-combine.
      5. Negative exclude paths: `src/test/java/FooTest.java`, `generated/...`, `build/...`, `target/...` → not selected.
      6. Hygiene: read rule file + detail file contain BODY_CANARY; JSON.stringify(buildIndex(...)) does not; renderInjection on a selecting signal does not include canary or `## Rule JS-COV-01`.
      7. Optional: assert rule file heading includes `## Rule JS-COV-01: Unit Line Coverage`.
      8. Assert frontmatter/index record has no positive taskType trigger list (paths present; taskType trigger empty or absent).

    **Inventory locks (only these two files):**
    - `src/index/precedence.test.ts`: append `"java-spring-unit-line-coverage"` to expectedIds array (before `.sort()`); update comment from nine java-spring → ten java-spring domain rules (mfa + 10).
    - `src/select/starter-examples.test.ts`: set `INVENTORY_COUNT = 11`; update comments/assertion messages that hardcode 10 for winner count (do not alter JAVA-EX-01 layout cases or D-10 backstop logic).

    Do NOT create the rule/detail markdown yet.

    **RED proof (mandatory):**
    ```
    npm run build:test
    node --test "dist-test/select/java-spring-coverage.test.js" "dist-test/index/precedence.test.js" "dist-test/select/starter-examples.test.js"
    ```
    `npm run build:test` MUST succeed (tests compile; no new production modules required in this plan). Focused `node --test` MUST exit non-zero because the rule is missing and inventory is still 10 — assert failures / missing id, not unrelated compile errors. Capture failing test names in SUMMARY. Commit `test(17-02): RED binding coverage rule suite and inventory 11`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; (node --test "dist-test/select/java-spring-coverage.test.js" "dist-test/index/precedence.test.js" "dist-test/select/starter-examples.test.js"; test $? -ne 0)</automated>
  </verify>
  <done>Suite + inventory expectations committed; build:test green; focused command RED on missing rule / inventory mismatch.</done>
  <acceptance_criteria>
    - java-spring-coverage.test.ts exists with binding metadata, paths-only positives, exclude.taskType negatives, non-Java feature/bugfix/refactor negatives, exclude-path negatives, quarantine cases
    - precedence.test.ts expectedIds includes java-spring-unit-line-coverage
    - starter-examples.test.ts INVENTORY_COUNT is 11
    - npm run build:test exits 0
    - Focused node --test exits non-zero (RED on behavior, not build breakage)
    - No rule md files created yet in this task
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — author binding rule + detail; suite and inventory pass</name>
  <files>aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md, aidlc-rules/domain/java-spring/details/java-spring-unit-line-coverage-detail.md</files>
  <read_first>
    - src/select/java-spring-coverage.test.ts (exact assertions)
    - aidlc-rules/domain/java-spring/java-spring-api-contract.md
    - aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md
    - src/index/build-guards.test.ts (binding requires enforcement)
    - src/select/select.ts (positive OR-combine; why no positive taskType)
    - .planning/phases/17-coverage-parser-binding-gateadapter/17-RESEARCH.md binding frontmatter sample
  </read_first>
  <behavior>
    - All java-spring-coverage tests pass (paths-only select; docs/test/infra exclude; non-Java feature/bugfix/refactor do not select)
    - precedence and starter-examples inventory tests pass at 11
    - buildIndex does not throw on binding rule (enforcement present)
  </behavior>
  <action>
    Author exactly two markdown files. Zero production engine source edits.

    **aidlc-rules/domain/java-spring/java-spring-unit-line-coverage.md** frontmatter (exact field values per D binding rule contract, paths-only positives):
    - id: `java-spring-unit-line-coverage`
    - scope: `domain`
    - triggers: paths-only positives — do NOT set positive `taskType` or `keywords` (engine OR-combines positive axes; positive taskType would over-select non-Java work). This is the only engine-compatible realization of the locked Java-production-work intent.
    - triggers.paths: exactly `"**/src/main/java/**"`, `"**/src/main/**/*.java"`
    - triggers.exclude.taskType: docs, test, infra
    - triggers.exclude.paths: `"**/src/test/**"`, `"**/generated/**"`, `"**/build/**"`, `"**/target/**"`
    - phases: [construction]
    - severity: high
    - summary: one sentence ≤160 chars requiring unit-test line coverage ≥70% verified by a real coverage report (no newline)
    - classification: binding
    - enforcement: coverage-report
    - detailPath: `details/java-spring-unit-line-coverage-detail.md`

    Body:
    - Heading exactly `## Rule JS-COV-01: Unit Line Coverage`
    - Short requirement paragraph (≥70% aggregate unit line coverage for new/changed Java production; fail-closed missing/malformed/below-threshold)
    - `### Verification` checklist (report present; ≥70% integer cross-mult; zero-line fail; missing/malformed fail)
    - HTML comment canary: `<!-- BODY_CANARY java-spring-unit-line-coverage -->`

    **details/java-spring-unit-line-coverage-detail.md**:
    - Restate measurement: line only; threshold `covered * 100 >= total * 70`; formats JaCoCo root LINE + LCOV LF/LH
    - JaCoCo: sole report direct-child LINE counter; do not sum nested
    - LCOV: aggregate LF/LH; reject duplicate/incomplete/LH&gt;LF
    - Fail-closed list; when applies / when not (mirror path positives + exclude taskType/paths; non-Java paths out of scope)
    - Include `BODY_CANARY java-spring-unit-line-coverage` token (comment or plain text consistent with other details)
    - No production src/ vendor product strings required; format names jacoco/lcov allowed in rule content

    GREEN proof:
    ```
    npm run build:test
    node --test "dist-test/select/java-spring-coverage.test.js" "dist-test/index/precedence.test.js" "dist-test/select/starter-examples.test.js"
    npm test
    ```
    Commit `feat(17-02): add binding java-spring-unit-line-coverage rule`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/select/java-spring-coverage.test.js" "dist-test/index/precedence.test.js" "dist-test/select/starter-examples.test.js"</automated>
  </verify>
  <done>Binding rule selectable on Java production paths only; inventory 11; BODY_CANARY quarantined; full metadata GREEN.</done>
  <acceptance_criteria>
    - Both md files exist at exact paths
    - Frontmatter has paths-only positives; no positive taskType/keywords; exclude.taskType docs/test/infra; exclude paths test/generated/build/target
    - Frontmatter enforcement is coverage-report and classification is binding
    - java-spring-coverage + precedence + starter-examples focused tests pass
    - Full npm test green when run after plan 01 also green (if plan 01 not yet executed, at least these three files pass)
    - No edits to verify-gate-hook.ts or adapters.ts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule pack markdown → buildIndex | Author-controlled frontmatter crosses into selectable index |
| detailPath → filesystem | Relative detail pointer must stay in-pack (existing detail-path guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-10 | Tampering | binding rule without enforcement | high | mitigate | classification binding + enforcement coverage-report; existing D-15 buildIndex/schema reject missing enforcement |
| T-17-11 | Information Disclosure | body leak into inject | medium | mitigate | one-sentence summary only; BODY_CANARY quarantine tests; detailPath lazy load |
| T-17-12 | Elevation of Privilege | circular coverage on test-only edits | medium | mitigate | exclude taskType test/docs/infra and paths src/test/generated/build/target |
| T-17-13 | Spoofing | wrong rule id token for gate mapping | high | mitigate | stable id java-spring-unit-line-coverage matches adapter finding token contract in plan 01 |
| T-17-14 | Tampering | over-select via positive taskType OR-combine | medium | mitigate | paths-only positives; no positive taskType/keywords; non-Java feature/bugfix/refactor negatives in suite |
| T-17-SC | Tampering | npm installs | high | accept | Zero new packages — content-only plan |
</threat_model>

<verification>
Focused: `npm run build:test &amp;&amp; node --test "dist-test/select/java-spring-coverage.test.js" "dist-test/index/precedence.test.js" "dist-test/select/starter-examples.test.js"`
Full: `npm test`
</verification>

<success_criteria>
1. Binding rule java-spring-unit-line-coverage indexes with enforcement coverage-report and severity high.
2. Paths-only positives select Java production paths; docs/test/infra and test/generated/build/target excluded; non-Java feature/bugfix/refactor do not select.
3. BODY_CANARY quarantined from index and inject.
4. Inventory locks at 11 in precedence.test.ts and starter-examples.test.ts.
</success_criteria>

<output>
Create `.planning/phases/17-coverage-parser-binding-gateadapter/17-02-SUMMARY.md` when done
</output>
