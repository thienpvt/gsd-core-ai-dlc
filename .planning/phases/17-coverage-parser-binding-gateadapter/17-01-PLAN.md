---
phase: 17-coverage-parser-binding-gateadapter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/enforcement/parse-jacoco.ts
  - src/enforcement/parse-jacoco.test.ts
  - src/enforcement/parse-lcov.ts
  - src/enforcement/parse-lcov.test.ts
  - src/enforcement/coverage-report.ts
  - src/enforcement/coverage-report.test.ts
  - test/fixtures/coverage/jacoco/pass-70.xml
  - test/fixtures/coverage/jacoco/fail-below-70.xml
  - test/fixtures/coverage/jacoco/zero-lines.xml
  - test/fixtures/coverage/jacoco/malformed-structure.xml
  - test/fixtures/coverage/jacoco/malformed-dtd.xml
  - test/fixtures/coverage/jacoco/duplicate-root-line.xml
  - test/fixtures/coverage/jacoco/negative-counter.xml
  - test/fixtures/coverage/lcov/pass-70.info
  - test/fixtures/coverage/lcov/fail-below-70.info
  - test/fixtures/coverage/lcov/zero-lines.info
  - test/fixtures/coverage/lcov/malformed.info
  - test/fixtures/coverage/lcov/duplicate-lf.info
  - test/fixtures/coverage/lcov/lh-gt-lf.info
autonomous: true
requirements:
  - JAVA-COV-02
  - JAVA-COV-03
must_haves:
  truths:
    - "parseJacoco returns {covered,total} from the sole report-root LINE counter and ignores nested package/class/method counters"
    - "parseJacoco rejects malformed structure and DTD/entity constructs as separate failure classes"
    - "parseLcov aggregates LF/LH across complete end_of_record records and rejects duplicates/incomplete/LH>LF"
    - "createCoverageAdapter({projectRoot,reportPath,format?}) returns GateAdapter name coverage-report"
    - "Threshold uses integer cross-multiplication covered*100 >= total*70; exactly 70% passes; total===0 fails"
    - "Missing/unreadable/out-of-root/symlink-escape/unknown-format/oversized/malformed/zero-line/below-threshold return valid GateResult status fail via runAdapter — not throw"
    - "Oversized path uses a temporary projectRoot and a report sized MAX_COVERAGE_REPORT_BYTES+1 via ftruncateSync; no committed oversized blob"
    - "Pass reports return status pass and empty findings; finding id on fail contains java-spring-unit-line-coverage; severity high"
    - "STUB_NAMES, ADAPTERS, ECHO_ADAPTERS remain exactly seven entries; adapters.ts untouched"
    - "Zero new npm deps; Node stdlib only; no Maven/JDK shell-out; no GateRequest schema change"
  artifacts:
    - path: src/enforcement/parse-jacoco.ts
      provides: "export function parseJacoco(xml: string): LineCounter"
      contains: "parseJacoco|LineCounter"
    - path: src/enforcement/parse-lcov.ts
      provides: "export function parseLcov(text: string): LineCounter"
      contains: "parseLcov|LF|LH|end_of_record"
    - path: src/enforcement/coverage-report.ts
      provides: "createCoverageAdapter factory + MAX_COVERAGE_REPORT_BYTES = 8 MiB"
      contains: "createCoverageAdapter|coverage-report|MAX_COVERAGE_REPORT_BYTES"
    - path: src/enforcement/coverage-report.test.ts
      provides: "runAdapter integration + fail-closed matrix"
      contains: "runAdapter|java-spring-unit-line-coverage|coverage-report"
    - path: test/fixtures/coverage/jacoco/pass-70.xml
      provides: "exactly-70% JaCoCo root LINE counter fixture"
    - path: test/fixtures/coverage/jacoco/malformed-structure.xml
      provides: "broken report structure (unclosed tag / no report root)"
    - path: test/fixtures/coverage/jacoco/malformed-dtd.xml
      provides: "DTD/entity declaration rejection fixture"
    - path: test/fixtures/coverage/lcov/pass-70.info
      provides: "exactly-70% LCOV LF/LH fixture"
  key_links:
    - from: src/enforcement/coverage-report.ts
      to: src/enforcement/run-adapter.ts
      via: "tests call runAdapter(adapter, request) not adapter.evaluate alone"
      pattern: "runAdapter\\("
    - from: src/enforcement/coverage-report.ts
      to: src/rules/detail-path.ts
      via: "mirror realpath + escapesRoot containment for reportPath under projectRoot"
      pattern: "realpathSync|projectRoot"
    - from: src/enforcement/coverage-report.ts
      to: src/enforcement/parse-jacoco.ts
      via: "format jacoco/xml dispatch"
      pattern: "parseJacoco"
    - from: src/enforcement/coverage-report.ts
      to: src/enforcement/parse-lcov.ts
      via: "format lcov/info dispatch"
      pattern: "parseLcov"
    - from: coverage fail finding id
      to: src/governance/verify-gate-hook.ts deriveRuleGateStatuses
      via: "id token java-spring-unit-line-coverage:coverage-report"
      pattern: "java-spring-unit-line-coverage:coverage-report"
---

<objective>
Ship pure JaCoCo/LCOV line parsers and a factory-built real `coverage-report` GateAdapter that fails closed through `runAdapter()` on missing/low/malformed coverage (JAVA-COV-02, JAVA-COV-03).

Purpose: First non-stub enforcement adapter; binding ≥70% line metric with durable fail GateResult evidence.
Output: `parse-jacoco.ts`, `parse-lcov.ts`, `coverage-report.ts` + tests + 13 fixtures under `test/fixtures/coverage/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-CONTEXT.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-RESEARCH.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-PATTERNS.md
@.planning/phases/17-coverage-parser-binding-gateadapter/17-VALIDATION.md
@src/enforcement/adapters.ts
@src/enforcement/run-adapter.ts
@src/enforcement/run-adapter.test.ts
@src/enforcement/types.ts
@src/enforcement/validate-gate-result.ts
@src/schema/gate-result.schema.json
@src/rules/detail-path.ts
@src/governance/verify-gate-hook.ts
</context>

## Artifacts this phase produces

| Path | Kind | Symbols / notes |
|------|------|-----------------|
| `src/enforcement/parse-jacoco.ts` | production | `export interface LineCounter { covered: number; total: number }`; `export function parseJacoco(xml: string): LineCounter` — throws on malformed |
| `src/enforcement/parse-lcov.ts` | production | `export function parseLcov(text: string): LineCounter` — throws on malformed; may re-export/share `LineCounter` from parse-jacoco or local identical shape |
| `src/enforcement/coverage-report.ts` | production | `export const MAX_COVERAGE_REPORT_BYTES = 8 * 1024 * 1024`; `export interface CoverageAdapterConfig { projectRoot: string; reportPath: string; format?: "jacoco" \| "lcov" }`; `export function createCoverageAdapter(config: CoverageAdapterConfig): GateAdapter` with `name: "coverage-report"`; export factory only — no static configured instance |
| `src/enforcement/parse-jacoco.test.ts` | test | pure parser matrix |
| `src/enforcement/parse-lcov.test.ts` | test | pure parser matrix |
| `src/enforcement/coverage-report.test.ts` | test | `runAdapter` pass/fail matrix + path/size/format security cases |
| `test/fixtures/coverage/jacoco/*.xml` | fixture | 7 files listed in frontmatter |
| `test/fixtures/coverage/lcov/*.info` | fixture | 6 files listed in frontmatter |

**Claude's Discretion (locked here):** 8 MiB ceiling; three production files; factory-only export (no static registry map).

**Out of scope (Phase 18 / deferred):** `verify-gate-hook.ts` auto-select, consumer report-path config/docs, `GateRequest` schema changes, Maven/JDK invocation, `STUB_NAMES` mutation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — fixtures + parser/adapter tests + not-implemented stubs</name>
  <files>src/enforcement/parse-jacoco.ts, src/enforcement/parse-lcov.ts, src/enforcement/coverage-report.ts, src/enforcement/parse-jacoco.test.ts, src/enforcement/parse-lcov.test.ts, src/enforcement/coverage-report.test.ts, test/fixtures/coverage/jacoco/pass-70.xml, test/fixtures/coverage/jacoco/fail-below-70.xml, test/fixtures/coverage/jacoco/zero-lines.xml, test/fixtures/coverage/jacoco/malformed-structure.xml, test/fixtures/coverage/jacoco/malformed-dtd.xml, test/fixtures/coverage/jacoco/duplicate-root-line.xml, test/fixtures/coverage/jacoco/negative-counter.xml, test/fixtures/coverage/lcov/pass-70.info, test/fixtures/coverage/lcov/fail-below-70.info, test/fixtures/coverage/lcov/zero-lines.info, test/fixtures/coverage/lcov/malformed.info, test/fixtures/coverage/lcov/duplicate-lf.info, test/fixtures/coverage/lcov/lh-gt-lf.info</files>
  <read_first>
    - src/enforcement/run-adapter.test.ts (makeValidGateRequest + runAdapter assert patterns)
    - src/enforcement/adapters.ts (GateAdapter shape; STUB_NAMES size 7 must stay untouched)
    - src/enforcement/types.ts (GateRequest, GateResult, GateFinding)
    - src/rules/detail-path.ts (escapesRoot + realpathSync containment pattern to mirror)
    - src/governance/verify-gate-hook.ts (deriveRuleGateStatuses regex — finding id must contain rule token)
    - .planning/phases/17-coverage-parser-binding-gateadapter/17-CONTEXT.md (locked measurement + fail-closed contracts)
    - .planning/phases/17-coverage-parser-binding-gateadapter/17-RESEARCH.md (fixture templates + parser contracts)
  </read_first>
  <behavior>
    - parseJacoco extracts sole report-root LINE counter: nested package/class LINE ignored; covered+missed = total
    - parseJacoco throws on 0 root LINE, &gt;1 root LINE, negative/non-integer/unsafe-integer attributes
    - parseJacoco throws on malformed-structure fixture (broken structure / no report root)
    - parseJacoco throws on malformed-dtd fixture (DOCTYPE, entity declaration, or entity reference present)
    - parseLcov aggregates LF→total LH→covered across complete SF…end_of_record records; ignores DA for totals
    - parseLcov throws on duplicate LF/LH in one record, incomplete/unterminated record, LH&gt;LF, non-integer/negative values
    - createCoverageAdapter name is exactly coverage-report; evaluate via runAdapter yields schema-valid pass for pass-70 fixtures (jacoco xml + lcov info)
    - Exactly-70% (covered*100 == total*70) → status pass, findings []
    - Below-70%, zero-line, missing file, out-of-root path, unknown suffix, oversized (&gt; MAX_COVERAGE_REPORT_BYTES), malformed → status fail, findings.length===1, findings[0].id matches /java-spring-unit-line-coverage/, severity high, evaluatedBy coverage-report
    - Oversized case: temporary projectRoot (os.tmpdir mkdtempSync); report file openSync + ftruncateSync(MAX_COVERAGE_REPORT_BYTES + 1); runAdapter fail; cleanup; no repo blob
    - STUB_NAMES/ADAPTERS/ECHO_ADAPTERS remain size 7 (assert by re-import; do not edit adapters.ts)
  </behavior>
  <action>
    Create fixtures, failing behavior tests, and minimal compiling production stubs. Do NOT implement real parse/adapter logic yet (RED proof required).

    **Production stubs (correct signatures; deliberate not-implemented bodies):**
    - `src/enforcement/parse-jacoco.ts`: export `interface LineCounter { covered: number; total: number }` and `export function parseJacoco(xml: string): LineCounter` that throws `new Error("not implemented")`.
    - `src/enforcement/parse-lcov.ts`: export `export function parseLcov(text: string): LineCounter` (import or redeclare LineCounter) that throws `new Error("not implemented")`.
    - `src/enforcement/coverage-report.ts`: export `MAX_COVERAGE_REPORT_BYTES = 8 * 1024 * 1024`, `CoverageAdapterConfig` interface, and `createCoverageAdapter(config)` returning `{ name: "coverage-report", evaluate: async () => { throw new Error("not implemented"); } }`.
    - Stubs exist so `npm run build:test` succeeds. Behavior tests must fail on assertions / thrown not-implemented, not on missing-module compile errors.

    **Fixtures (exact paths — 13 total: 7 jacoco + 6 lcov):**
    - `test/fixtures/coverage/jacoco/pass-70.xml` — root LINE missed=3 covered=7 (total 10 → exactly 70%); include nested package/class LINE counters with different values so a wrong nested-sum would fail
    - `test/fixtures/coverage/jacoco/fail-below-70.xml` — root LINE missed=4 covered=6 (60%)
    - `test/fixtures/coverage/jacoco/zero-lines.xml` — root LINE missed=0 covered=0
    - `test/fixtures/coverage/jacoco/malformed-structure.xml` — broken structure only (unclosed tag or no report root); NO DTD/entity content
    - `test/fixtures/coverage/jacoco/malformed-dtd.xml` — well-formed-looking shell that includes a DOCTYPE and/or entity declaration/reference so DTD/entity rejection is proven independently of structure breakage
    - `test/fixtures/coverage/jacoco/duplicate-root-line.xml` — two direct-child report LINE counters
    - `test/fixtures/coverage/jacoco/negative-counter.xml` — root LINE with negative missed or covered
    - `test/fixtures/coverage/lcov/pass-70.info` — records whose LF sum=10 LH sum=7
    - `test/fixtures/coverage/lcov/fail-below-70.info` — LF=10 LH=6
    - `test/fixtures/coverage/lcov/zero-lines.info` — LF=0 LH=0 complete record
    - `test/fixtures/coverage/lcov/malformed.info` — SF opened without end_of_record
    - `test/fixtures/coverage/lcov/duplicate-lf.info` — two LF lines in one record
    - `test/fixtures/coverage/lcov/lh-gt-lf.info` — LH=5 LF=3

    **Tests (node:test + node:assert/strict only):**
    - Import the stub modules (they compile) and assert real behavior contracts so tests fail RED.
    - Parser tests call `parseJacoco`/`parseLcov` directly (expect throws on malformed-structure, malformed-dtd, and other reject cases; expect concrete counters on pass fixtures).
    - Separate test cases: one loads malformed-structure.xml; one loads malformed-dtd.xml — do not collapse into a single "malformed" case.
    - Adapter tests MUST use `runAdapter(createCoverageAdapter(...), request)` never bare evaluate for schema assertions.
    - GateRequest helper: gateId verify, phase construction, rules include id `java-spring-unit-line-coverage` severity high.
    - Include path-containment cases: reportPath `../../../etc/passwd`, absolute path, directory path.
    - **Oversized case (exact strategy):** `fs.mkdtempSync(path.join(os.tmpdir(), "cov-oversized-"))` as `projectRoot`; create report file under that root with `fs.openSync` + `fs.ftruncateSync(fd, MAX_COVERAGE_REPORT_BYTES + 1)` (or write zero then ftruncate); close; call `runAdapter(createCoverageAdapter({ projectRoot, reportPath: relativeName, format: "jacoco" }), request)`; assert status fail; `fs.rmSync(projectRoot, { recursive: true, force: true })` in finally. Never commit a multi-MiB fixture to the repository.
    - Assert finding id contains token `java-spring-unit-line-coverage` (stable id `java-spring-unit-line-coverage:coverage-report` per D fail-closed contract).
    - Do not modify `src/enforcement/adapters.ts`, `run-adapter.ts`, `verify-gate-hook.ts`, or any GateRequest schema.

    **RED proof (mandatory before any GREEN commit):**
    ```
    npm run build:test
    node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js" "dist-test/enforcement/coverage-report.test.js"
    ```
    `npm run build:test` MUST succeed (stubs compile). Focused `node --test` MUST exit non-zero because behavior assertions fail (not-implemented throws or wrong results). Do not treat arbitrary compile failure as RED. Capture the failing test names in SUMMARY. Commit stubs+tests+fixtures with message `test(17-01): RED coverage parser and adapter contracts`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; (node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js" "dist-test/enforcement/coverage-report.test.js"; test $? -ne 0)</automated>
  </verify>
  <done>13 fixtures, three test files, three not-implemented production stubs exist; build:test green; focused node --test RED on behavior assertions.</done>
  <acceptance_criteria>
    - 13 fixture files exist at exact paths above (malformed-structure.xml and malformed-dtd.xml both present; no single malformed.xml)
    - Three production stubs export correct symbols and throw not implemented
    - Three test files import parseJacoco, parseLcov, createCoverageAdapter, runAdapter
    - npm run build:test exits 0
    - Focused node --test exits non-zero with failing behavior tests (not missing-module errors)
    - adapters.ts byte-identical to pre-task (no stub registry edits)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — pure JaCoCo and LCOV parsers</name>
  <files>src/enforcement/parse-jacoco.ts, src/enforcement/parse-lcov.ts</files>
  <read_first>
    - src/enforcement/parse-jacoco.test.ts (contracts just written)
    - src/enforcement/parse-lcov.test.ts
    - test/fixtures/coverage/jacoco/pass-70.xml
    - test/fixtures/coverage/jacoco/malformed-structure.xml
    - test/fixtures/coverage/jacoco/malformed-dtd.xml
    - test/fixtures/coverage/lcov/pass-70.info
    - .planning/phases/17-coverage-parser-binding-gateadapter/17-RESEARCH.md Pattern 3 + Pitfall 1/2
  </read_first>
  <behavior>
    - parseJacoco(pass-70 xml) → {covered:7, total:10}; ignores nested counters
    - parseJacoco throws on duplicate-root, negative, malformed-structure, malformed-dtd, missing root LINE
    - parseLcov(pass-70) → {covered:7, total:10}
    - parseLcov throws on duplicate LF, LH&gt;LF, incomplete record
  </behavior>
  <action>
    Replace not-implemented stubs in the two parser modules with real implementations (no adapter yet if Task 1 adapter tests still fail — acceptable; this task greens parser tests).

    **parse-jacoco.ts (per D measurement boundary + D parser contract):**
    - Keep exported `LineCounter` and `parseJacoco(xml: string): LineCounter`.
    - Structure-aware scan: identify the single direct-child root LINE counter of the report element. Do NOT use a global regex that can match nested package/class/method/sourcefile counters.
    - Approach: track element nesting depth while scanning tags; when depth is 1 (direct child of report) and tag is counter with type=LINE, capture missed/covered; reject if count of such root LINE counters is not exactly 1.
    - Reject if input contains DTD declarations, entity declarations, or entity references (fail closed — no entity expansion). malformed-dtd fixture must throw; malformed-structure fixture must throw for independent reason.
    - Parse missed/covered as non-negative safe integers only (`Number.isSafeInteger`, ≥0). total = missed + covered using safe arithmetic (reject if sum exceeds Number.MAX_SAFE_INTEGER).
    - Throw Error with clear message on any rejection (adapter maps throws → fail GateResult later).

    **parse-lcov.ts:**
    - Export `parseLcov(text: string): LineCounter` (import or duplicate LineCounter type).
    - Line-based split on `\n` (tolerate trailing `\r`).
    - Record state machine: `SF:` opens; require exactly one `LF:` and one `LH:` before `end_of_record`; reject duplicate LF/LH; reject incomplete (missing LF or LH); reject unterminated open record at EOF; reject LH &gt; LF; ignore DA/BRDA/FN/FNDA for totals.
    - Values must be non-negative safe integers; aggregate with overflow-safe addition into covered/total.
    - Empty file with zero records → {covered:0,total:0} (adapter fails closed on zero-line later).

    Zero new npm deps. No XML DOM libraries.

    After GREEN parsers:
    ```
    npm run build:test &amp;&amp; node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js"
    ```
    Must pass. Commit `feat(17-01): implement JaCoCo and LCOV line parsers`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js"</automated>
  </verify>
  <done>Both pure parser test files green; root-counter, structure, DTD/entity, and LF/LH contracts hold.</done>
  <acceptance_criteria>
    - parseJacoco.test.js and parse-lcov.test.js all pass
    - malformed-structure and malformed-dtd each cause throws
    - No dependency added to package.json
    - No general XML parser import
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — createCoverageAdapter factory + fail-closed runAdapter matrix</name>
  <files>src/enforcement/coverage-report.ts, src/enforcement/coverage-report.test.ts</files>
  <read_first>
    - src/enforcement/coverage-report.test.ts
    - src/enforcement/parse-jacoco.ts
    - src/enforcement/parse-lcov.ts
    - src/enforcement/adapters.ts (factory return shape only — do not edit)
    - src/enforcement/run-adapter.ts
    - src/rules/detail-path.ts (containment)
    - src/schema/gate-result.schema.json (required fields, additionalProperties false)
  </read_first>
  <behavior>
    - runAdapter(createCoverageAdapter({projectRoot, reportPath: jacoco pass-70})) → pass
    - runAdapter missing/below/zero/malformed/out-of-root/oversized/unknown-format → fail with rule-token finding
    - Oversized: temp projectRoot + ftruncateSync(MAX+1) fails closed then cleans up
    - evaluatedBy === coverage-report; gateId echoes request; evaluatedAt strict ISO ms Z
    - adapters.test.ts still asserts ADAPTERS.size === 7
  </behavior>
  <action>
    Replace the not-implemented body of `src/enforcement/coverage-report.ts` with the real factory per D parser/format + fail-closed contracts:

    1. Keep `export const MAX_COVERAGE_REPORT_BYTES = 8 * 1024 * 1024` (8 MiB explicit ceiling).
    2. Keep `export interface CoverageAdapterConfig { projectRoot: string; reportPath: string; format?: "jacoco" | "lcov" }`.
    3. `export function createCoverageAdapter(config: CoverageAdapterConfig): GateAdapter` returning `{ name: "coverage-report", evaluate }`.
    4. Inside evaluate (catch all expected error classes → valid fail result; programming faults may throw):
       - Resolve reportPath: if absolute, fail closed (reject absolute). Relative resolve against projectRoot.
       - Lexical containment (mirror detail-path escapesRoot against projectRoot) then realpathSync both projectRoot and target when target exists; re-check containment; fail closed on escape/symlink out.
       - statSync: reject directories, missing, unreadable → fail; if size &gt; MAX_COVERAGE_REPORT_BYTES → fail before readFileSync.
       - Format: if config.format set use it; else infer `.xml`→jacoco, `.info`/`.lcov`→lcov; else fail unknown suffix. No content sniffing.
       - readFileSync utf8; dispatch parseJacoco/parseLcov; on throw → fail malformed.
       - If total === 0 → fail zero-line.
       - Threshold: `covered * 100 >= total * 70` with safe integers only; if either product would exceed MAX_SAFE_INTEGER, fail closed rather than overflow.
       - Pass → `{ gateId, status:"pass", findings:[], evaluatedBy:"coverage-report", evaluatedAt: new Date().toISOString() }`.
       - Fail helper: one finding `{ id: "java-spring-unit-line-coverage:coverage-report", severity:"high", message:&lt;reason&gt;, evidence?: { path: project-relative safe path } }` — only attach evidence.path when a safe in-project relative path exists.
       - Never status waived.
    5. Export factory only — no static preconfigured adapter instance, no addition to STUB_NAMES/ADAPTERS/ECHO_ADAPTERS.
    6. Do not edit adapters.ts, run-adapter.ts, verify-gate-hook.ts, GateRequest schemas.

    Confirm the oversized test still uses temp projectRoot + ftruncateSync(MAX+1) + cleanup (no repo blob).

    GREEN proof:
    ```
    npm run build:test &amp;&amp; node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js" "dist-test/enforcement/coverage-report.test.js" "dist-test/enforcement/adapters.test.js"
    ```
    Full suite: `npm test`.
    Commit `feat(17-01): add coverage-report GateAdapter factory`.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js" "dist-test/enforcement/coverage-report.test.js" "dist-test/enforcement/adapters.test.js"</automated>
  </verify>
  <done>Full coverage-report + parser + adapters regression green; fail-closed matrix holds through runAdapter.</done>
  <acceptance_criteria>
    - coverage-report.test.js all pass via runAdapter
    - adapters.test.js still green (7 stubs)
    - createCoverageAdapter is the only registration seam (grep STUB_NAMES still length 7 in adapters.ts)
    - MAX_COVERAGE_REPORT_BYTES === 8388608
    - Finding id on fail is java-spring-unit-line-coverage:coverage-report
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer reportPath → filesystem read | Untrusted path string crosses into Node fs APIs |
| report file bytes → parsers | Untrusted report content crosses into parseJacoco/parseLcov |
| adapter output → runAdapter/schema | Adapter-produced GateResult must be schema-valid before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | coverage-report path resolve | high | mitigate | Reject absolute reportPath; lexical escapesRoot + realpathSync containment vs projectRoot (symlink escape closed) |
| T-17-02 | Tampering | reportPath traversal (`..`) | high | mitigate | Same containment as T-17-01 before any read |
| T-17-03 | Denial of Service | oversized report read | high | mitigate | statSync size check; reject if &gt; MAX_COVERAGE_REPORT_BYTES (8 MiB) before readFileSync |
| T-17-04 | Denial of Service | XML entity/DTD expansion | high | mitigate | No DOM/XML parser; reject DTD/entity constructs (malformed-dtd fixture); narrow structure scan only |
| T-17-05 | Tampering | malformed counters / unsafe integers | medium | mitigate | Non-negative safe-integer validation; overflow-safe total/threshold math; fail closed |
| T-17-06 | Elevation of Privilege | silent pass on error | high | mitigate | All error classes return status fail GateResult; only programming faults throw; runAdapter validates schema |
| T-17-07 | Tampering | format sniffing ambiguity | medium | mitigate | Suffix-only inference; unknown suffix fails closed |
| T-17-08 | Information Disclosure | evidence path leakage | medium | mitigate | evidence.path only when safe project-relative path exists |
| T-17-09 | Tampering | nested JaCoCo counter double-count | medium | mitigate | Sole direct-child root LINE counter; reject 0 or &gt;1 |
| T-17-SC | Tampering | npm installs | high | accept | Zero new packages this plan — no install surface |
</threat_model>

<verification>
Focused: `npm run build:test &amp;&amp; node --test "dist-test/enforcement/parse-jacoco.test.js" "dist-test/enforcement/parse-lcov.test.js" "dist-test/enforcement/coverage-report.test.js" "dist-test/enforcement/adapters.test.js"`
Full: `npm test`
</verification>

<success_criteria>
1. Pure parsers implement root-LINE (JaCoCo) and LF/LH aggregate (LCOV) contracts with throws on malformed structure and DTD/entity.
2. createCoverageAdapter name coverage-report; factory config only; 8 MiB ceiling.
3. runAdapter pass at exactly 70%; fail closed for missing/low/zero/malformed/path/size/format classes with finding id containing java-spring-unit-line-coverage.
4. Oversized path uses temp projectRoot + ftruncateSync(MAX+1) + cleanup; no committed blob.
5. STUB_NAMES/ADAPTERS/ECHO_ADAPTERS remain 7; zero new npm deps; no schema/hook edits.
</success_criteria>

<output>
Create `.planning/phases/17-coverage-parser-binding-gateadapter/17-01-SUMMARY.md` when done
</output>
