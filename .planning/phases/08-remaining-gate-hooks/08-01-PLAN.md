---
phase: 08-remaining-gate-hooks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/governance/paths.ts
  - src/governance/gate-evidence-store.ts
  - src/governance/gate-evidence-store.test.ts
autonomous: true
requirements:
  - GATE-03
  - GATE-04
  - GATE-05
requirements_addressed:
  - GATE-03
  - GATE-04
  - GATE-05
must_haves:
  truths:
    - "Gate evidence is stored under .planning/governance/gates/{NN}-{gate}.json per D-10."
    - "Each evidence file contains request, result, and metadata per D-11."
    - "Evidence writes use atomicWriteFile and malformed evidence fails loud per D-12."
  artifacts:
    - path: "src/governance/paths.ts"
      provides: "gateEvidencePath(projectRoot, phaseNumber, gateId)"
      exports: ["gateEvidencePath"]
    - path: "src/governance/gate-evidence-store.ts"
      provides: "GateEvidence read/write store"
      exports: ["GateEvidence", "writeGateEvidence", "readGateEvidence"]
    - path: "src/governance/gate-evidence-store.test.ts"
      provides: "Wave 0 evidence-store RED coverage"
  key_links:
    - from: "src/governance/gate-evidence-store.ts"
      to: "src/governance/atomic-write.ts"
      via: "writeGateEvidence delegates JSON persistence to atomicWriteFile"
      pattern: "atomicWriteFile\\("
    - from: "src/governance/gate-evidence-store.ts"
      to: "src/enforcement/validate-gate-result.ts"
      via: "read/write validation asserts persisted result shape"
      pattern: "validateGateResult\\("
---

<objective>
Create the durable gate evidence store that Phase 8 hooks share.

Purpose: Plan, verify, and ship gates need one fixed, atomic, loud-on-malformed state path before hook modules can safely write per-gate pass/fail records.
Output: `gateEvidencePath`, `GateEvidence`, `writeGateEvidence`, `readGateEvidence`, and Wave 0 test coverage for D-10, D-11, D-12.
</objective>

<execution_context>
@C:/Users/thienpv/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thienpv/.codex/gsd-core/templates/summary.md
@C:/Users/thienpv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-remaining-gate-hooks/08-CONTEXT.md
@.planning/phases/08-remaining-gate-hooks/08-RESEARCH.md
@.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
@.planning/phases/08-remaining-gate-hooks/08-PATTERNS.md
@.planning/phases/06-v1-0-tech-debt-fold-in/06-01-SUMMARY.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-04-SUMMARY.md
@src/governance/paths.ts
@src/governance/state-store.ts
@src/governance/atomic-write.ts
@src/enforcement/types.ts
@src/enforcement/validate-gate-result.ts
@src/governance/audit-artifact.ts
</context>

## Artifacts This Phase Produces

Files:
- `src/governance/gate-evidence-store.ts`
- `src/governance/gate-evidence-store.test.ts`
- `src/governance/plan-hook.ts`
- `src/governance/plan-hook.test.ts`
- `src/governance/verify-gate-hook.ts`
- `src/governance/verify-gate-hook.test.ts`
- `src/governance/ship-gate-hook.ts`
- `src/governance/ship-gate-hook.test.ts`
- `.claude/skills/aidlc-governance-plan/SKILL.md`
- `.claude/skills/aidlc-governance-verify/SKILL.md`
- `.claude/skills/aidlc-governance-ship/SKILL.md`

Modified files:
- `src/governance/paths.ts`
- `.gsd/capabilities/aidlc-governance/capability.json`
- `src/governance/audit-hook-contract.test.ts`
- `src/governance/consent.test.ts`
- `src/governance/consent-verify-post.test.ts`

Symbols:
- `gateEvidencePath(projectRoot, phaseNumber, gateId)`
- `GateEvidence`, `GateEvidenceMetadata`
- `writeGateEvidence(projectRoot, phaseNumber, evidence)`
- `readGateEvidence(projectRoot, phaseNumber, gateId)`
- `planHook(args)`, `PlanHookArgs`, `PlanHookResult`
- `verifyGateHook(args)`, `VerifyGateHookArgs`, `VerifyGateHookResult`
- `shipGateHook(args)`, `ShipGateHookArgs`, `ShipGateHookResult`

## Source Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Plan, verify, and ship gates consume Phase 7 contracts and produce per-rule pass/fail records | 08-01..08-05 | COVERED | Store + three hooks + manifest wiring |
| REQ | GATE-03 | Plan gate surfaces summary-only rules from selection engine | 08-02, 08-05 | COVERED | D-01, D-02, D-03 |
| REQ | GATE-04 | Verify gate routes evidence through adapters and records pass/fail | 08-03, 08-05 | COVERED | D-04, D-05, D-06 |
| REQ | GATE-05 | Ship gate blocks on incomplete prior gates | 08-04, 08-05 | COVERED | D-07, D-08, D-09 |
| RESEARCH | - | Atomic evidence store under .planning/governance/gates | 08-01 | COVERED | No new package install |
| RESEARCH | - | Plan hook reuses select + renderInjection | 08-02 | COVERED | No second selector |
| RESEARCH | - | Verify hook uses runAdapter + ADAPTERS | 08-03 | COVERED | No real scanner integration |
| RESEARCH | - | Ship hook fails closed on missing/failing evidence | 08-04 | COVERED | No APPR-01 capture |
| RESEARCH | - | Capability manifest/consent contract tests cover hook refs | 08-05 | COVERED | Existing hooks preserved |
| CONTEXT | D-01, D-02, D-03 | Plan gate signal/output | 08-02 | COVERED | Cited in tasks |
| CONTEXT | D-04, D-05, D-06 | Verify adapter flow | 08-03 | COVERED | Cited in tasks |
| CONTEXT | D-07, D-08, D-09 | Ship blocking policy | 08-04, 08-05 | COVERED | Cited in tasks |
| CONTEXT | D-10, D-11, D-12 | Gate evidence storage | 08-01 | COVERED | Cited in tasks |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for gate evidence store</name>
  <files>src/governance/gate-evidence-store.test.ts</files>
  <read_first>
    @src/governance/state-store.test.ts
    @src/governance/atomic-write.test.ts
    @src/governance/paths.ts
    @src/enforcement/run-adapter.test.ts
    @.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
  </read_first>
  <behavior>
    - `gateEvidencePath(root, "08", "plan")` resolves to `.planning/governance/gates/08-plan.json` per D-10.
    - `readGateEvidence(root, "08", "plan")` returns null when evidence is missing.
    - `writeGateEvidence` round-trips a valid `GateEvidence` containing `GateRequest`, validated `GateResult`, and metadata per D-11.
    - malformed JSON, missing `request`, missing `result`, missing `metadata`, mismatched request/result gate id, and non-ISO `metadata.writtenAt` throw loud errors.
    - successful writes leave no `*.tmp*` siblings and never create `.planning/governance/selection-state.json`, per D-12.
  </behavior>
  <action>Create `src/governance/gate-evidence-store.test.ts` first. Use `node:test`, `node:assert/strict`, `mkdtempSync`, `rmSync`, and the existing fixture style from `state-store.test.ts`. The RED run must fail because `gate-evidence-store.ts` and `gateEvidencePath` do not exist yet. Cover validation map item `08-W0-01` and requirements GATE-03, GATE-04, GATE-05.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/gate-evidence-store.test.js"</automated>
  </verify>
  <acceptance_criteria>Tests exist, compile fails for missing implementation in RED, and every D-10/D-11/D-12 behavior above is asserted.</acceptance_criteria>
  <done>RED gate committed as `test(08-01): add failing tests for gate evidence store`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implementation for fixed-path atomic evidence</name>
  <files>src/governance/paths.ts, src/governance/gate-evidence-store.ts, src/governance/gate-evidence-store.test.ts</files>
  <read_first>
    @src/governance/paths.ts
    @src/governance/state-store.ts
    @src/governance/atomic-write.ts
    @src/enforcement/types.ts
    @src/enforcement/validate-gate-result.ts
  </read_first>
  <action>Add `gateEvidencePath(projectRoot: string, phaseNumber: string, gateId: GateId)` to `src/governance/paths.ts`; validate `phaseNumber` against `^\d{2}(?:\.\d+)?$` and build paths through `governanceDir(projectRoot)`. Create `src/governance/gate-evidence-store.ts` with `GateEvidenceMetadata`, `GateEvidence`, `writeGateEvidence`, and `readGateEvidence`. Persist JSON through `atomicWriteFile` only, validate `GateResult` with `validateGateResult`, require `request.gateId === result.gateId`, require `metadata.phase === phaseNumber`, require strict `YYYY-MM-DDTHH:mm:ss.sssZ` `metadata.writtenAt`, and throw `malformed gate evidence at <path>:` errors for malformed persisted files. Missing files return null. Do not import or call `writeSelection`; plan evidence must never overwrite execute selection state per D-03 and D-10.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/gate-evidence-store.test.js"</automated>
  </verify>
  <acceptance_criteria>All gate-evidence-store tests pass; `gateEvidencePath` is the single path helper; writes are atomic; malformed evidence fails loud; missing evidence returns null.</acceptance_criteria>
  <done>GREEN gate committed as `feat(08-01): implement gate evidence store`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook -> filesystem | Gate hooks write durable workflow evidence under `.planning/governance/gates/`. |
| Filesystem -> ship gate | Persisted JSON is read back later to decide whether release may proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `readGateEvidence` | high | mitigate | Validate object shape, `GateResult`, phase number, gate id matching, and strict metadata timestamp before returning evidence. |
| T-08-02 | Repudiation | `writeGateEvidence` | high | mitigate | Store request, result, metadata.source, metadata.phase, and metadata.writtenAt in a fixed per-phase/per-gate file. |
| T-08-03 | Denial of Service | Evidence path construction | medium | mitigate | Reject non-phase-number path input and use `gateId: GateId` union through `gateEvidencePath`. |
| T-08-04 | Tampering | Evidence writes | high | mitigate | Delegate writes to `atomicWriteFile`; tests assert no temp leftovers and no fixed temp collision. |
| T-08-SC | Tampering | package installs | low | accept | No package install task exists; Phase 8 uses existing Node stdlib and existing dependencies only. |
</threat_model>

<verification>
Plan-level checks:
- `npm run build:test && node --test "dist-test/governance/gate-evidence-store.test.js"`
- `npm test` after Wave 1 merge.
</verification>

<success_criteria>
- GATE-03/GATE-04/GATE-05 have a shared durable evidence store.
- D-10/D-11/D-12 are implemented exactly.
- Wave 0 item `08-W0-01` is green.
- No source file outside listed files is modified.
</success_criteria>

<output>
Create `.planning/phases/08-remaining-gate-hooks/08-01-SUMMARY.md` when done.
</output>
