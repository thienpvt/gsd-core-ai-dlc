---
phase: 08-remaining-gate-hooks
plan: 04
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/governance/ship-gate-hook.ts
  - src/governance/ship-gate-hook.test.ts
autonomous: true
requirements:
  - GATE-05
requirements_addressed:
  - GATE-05
must_haves:
  truths:
    - "Ship gate fails closed when required plan or verify evidence is missing or failed per D-07."
    - "Ship gate composes with existing ship:pre gates and does not replace them per D-08."
    - "Ship gate does not implement APPR-01 approval capture, rollback evidence model, or full audit enrichment per D-09."
  artifacts:
    - path: "src/governance/ship-gate-hook.ts"
      provides: "ship:pre fail-closed evidence gate"
      exports: ["ShipGateHookArgs", "ShipGateHookResult", "shipGateHook"]
    - path: "src/governance/ship-gate-hook.test.ts"
      provides: "Wave 0 GATE-05 ship gate RED coverage"
  key_links:
    - from: "src/governance/ship-gate-hook.ts"
      to: "src/governance/gate-evidence-store.ts"
      via: "shipGateHook reads plan and verify evidence, then writes ship evidence"
      pattern: "readGateEvidence\\("
---

<objective>
Implement the ship gate blocking policy for GATE-05.

Purpose: Release readiness must fail closed when Phase 8 gate evidence is missing or failed, so incomplete governance cannot silently pass to ship.
Output: `shipGateHook(args)` plus tests proving D-07, D-08, D-09.
</objective>

<execution_context>
@C:/Users/thienpv/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thienpv/.codex/gsd-core/templates/summary.md
@C:/Users/thienpv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-remaining-gate-hooks/08-CONTEXT.md
@.planning/phases/08-remaining-gate-hooks/08-RESEARCH.md
@.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
@.planning/phases/08-remaining-gate-hooks/08-PATTERNS.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-01-SUMMARY.md
@src/governance/gate-evidence-store.ts
@src/governance/execute-hook.ts
@src/governance/audit-artifact.ts
@src/enforcement/types.ts
</context>

## Artifacts This Phase Produces

Files and symbols are listed in `08-01-PLAN.md`; this plan owns `src/governance/ship-gate-hook.ts`, `src/governance/ship-gate-hook.test.ts`, `ShipGateHookArgs`, `ShipGateHookResult`, and `shipGateHook(args)`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for ship fail-closed matrix</name>
  <files>src/governance/ship-gate-hook.test.ts</files>
  <read_first>
    @src/governance/execute-hook.test.ts
    @src/governance/gate-evidence-store.test.ts
    @src/governance/audit-artifact.test.ts
    @.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
  </read_first>
  <behavior>
    - missing plan evidence throws an error naming `.planning/governance/gates/08-plan.json`.
    - missing verify evidence throws an error naming `.planning/governance/gates/08-verify.json`.
    - malformed plan or verify evidence propagates the store's malformed-evidence error.
    - `result.status: "fail"` in either plan or verify evidence blocks ship and does not write ship evidence.
    - pass/waived plan and verify evidence writes `.planning/governance/gates/08-ship.json` with `gateId: "ship"` and `evaluatedBy: "aidlc-governance-ship"`.
    - tests assert no APPR-01 approval object, rollback model, or audit enrichment fields are required by this hook per D-09.
  </behavior>
  <action>Create `src/governance/ship-gate-hook.test.ts` before production code. Use `writeGateEvidence` to seed plan/verify cases and `readGateEvidence` to assert ship writes. Table-test missing, malformed, fail, waived, and pass cases. Cover validation item `08-W0-04` and requirement GATE-05.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/ship-gate-hook.test.js"</automated>
  </verify>
  <acceptance_criteria>RED tests fail before `ship-gate-hook.ts` exists and pin D-07/D-09 behavior.</acceptance_criteria>
  <done>RED gate committed as `test(08-04): add failing tests for ship gate hook`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implementation for shipGateHook</name>
  <files>src/governance/ship-gate-hook.ts, src/governance/ship-gate-hook.test.ts</files>
  <read_first>
    @src/governance/gate-evidence-store.ts
    @src/governance/paths.ts
    @src/enforcement/types.ts
  </read_first>
  <action>Create `src/governance/ship-gate-hook.ts`. Define `ShipGateHookArgs` with `projectRoot` and `phaseNumber`. Read required plan and verify evidence using `readGateEvidence`; null means throw `ship gate: missing governance evidence <path>`. Treat `result.status === "fail"` as blocking and include the gate id plus finding ids/messages in the error. Treat `pass` and `waived` as non-blocking because waiver is an explicit GateResult status from Phase 7. Build a ship `GateRequest` from verify evidence's phase/taskSignal/rules with `gateId: "ship"` and current ISO `requestedAt`; build pass `GateResult` evaluated by `aidlc-governance-ship`; write ship evidence through `writeGateEvidence`. Do not add approval capture, rollback evidence shape, or full audit enrichment fields, per D-09. Add direct runner `<projectRoot> <phaseNumber>` that fails with `process.exitCode = 1` and stderr on blocking conditions.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/ship-gate-hook.test.js"</automated>
  </verify>
  <acceptance_criteria>Ship hook fails closed on missing/malformed/failing plan or verify evidence and writes valid ship evidence only after required prior gates pass or waive.</acceptance_criteria>
  <done>GREEN gate committed as `feat(08-04): implement ship gate hook`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence store -> ship decision | Persisted plan/verify evidence controls whether release can proceed. |
| Ship hook -> evidence store | Ship gate writes final pass evidence consumed by Phase 9. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Repudiation | missing prior evidence | high | mitigate | Fail closed on null plan/verify evidence and name the exact missing evidence path. |
| T-08-14 | Tampering | malformed prior evidence | high | mitigate | Use `readGateEvidence`; malformed JSON or invalid result shape throws before ship decision. |
| T-08-15 | Elevation of Privilege | failed prior evidence | high | mitigate | Block on `result.status === "fail"` and include finding details in the error. |
| T-08-16 | Tampering | capability composition | medium | mitigate | Manifest plan 08-05 adds ship as an additive `ship:pre` step and tests existing hooks remain present per D-08. |
| T-08-SC | Tampering | package installs | low | accept | No package install task exists. |
</threat_model>

<verification>
Plan-level checks:
- `npm run build:test && node --test "dist-test/governance/ship-gate-hook.test.js"`
- `npm test` after Wave 2 merge.
</verification>

<success_criteria>
- GATE-05 is satisfied by a ship:pre-ready hook module.
- D-07/D-09 are covered in tests and implementation; D-08 is completed in 08-05 manifest wiring.
- Wave 0 item `08-W0-04` is green.
- Ship evidence is written only after required prior evidence passes or waives.
</success_criteria>

<output>
Create `.planning/phases/08-remaining-gate-hooks/08-04-SUMMARY.md` when done.
</output>
