---
phase: 09-complete-audit-record-approval
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/schema/approval.schema.json
  - src/enforcement/validate-approval.ts
  - src/enforcement/validate-approval.test.ts
  - src/governance/approval-store.ts
  - src/governance/approval-store.test.ts
  - src/governance/paths.ts
autonomous: true
requirements: [APPR-01]
user_setup: []

must_haves:
  truths:
    - "Approval schema (draft 2020-12) validates the D-05 10-field shape; decidedBy/decidedAt/rationale are optional (D-07 — blank until human resolves)"
    - "validateApproval throws on any malformed approval (Ajv 2020 strict + x-binding keyword registered before compile — 5th instance of validate.ts pattern)"
    - "writeApproval round-trips ApprovalRecord through atomicWriteFile; readApproval returns null when absent (mirrors readGateEvidence)"
    - "writeApproval rejects decision other than pending when decidedBy is absent (D-07 anti-auto-approve invariant)"
    - "readApproval re-validates parsed JSON via assertApproval (loud-fail on tampered disk state)"
    - "approvalPath and testEvidencePath helpers added to paths.ts (single-sourced, PHASE_NUMBER_RE-validated, .planning/governance/{approvals,tests}/{NN}.json layout)"
  artifacts:
    - "src/schema/approval.schema.json"
    - "src/enforcement/validate-approval.ts"
    - "src/enforcement/validate-approval.test.ts"
    - "src/governance/approval-store.ts"
    - "src/governance/approval-store.test.ts"
    - "src/governance/paths.ts (extended: approvalPath + testEvidencePath)"
  key_links:
    - "approval-store.writeApproval → atomicWriteFile (TD-03 race fix inherited)"
    - "approval-store.assertApproval → validateApproval (ENF-02 boundary)"
    - "approvalPath/testEvidencePath → governanceDir (single-sourced layout convention)"
---

<objective>
Implement the APPR-01 human approval checkpoint: a draft 2020-12 JSON Schema for the D-05 10-field approval shape, an Ajv 2020 runtime validator (validate-approval.ts — 5th instance of the validate.ts pattern), and a durable approval store (approval-store.ts — clone of gate-evidence-store.ts) persisting under .planning/governance/approvals/{NN}.json. Also extend paths.ts with approvalPath + testEvidencePath (Plan 02 consumes testEvidencePath without modifying paths.ts).

Purpose: APPR-01 requires approvals produced/consumed through the tool-agnostic contract layer. Routing approval persistence through validateApproval inherits the ENF-02 malformed-hard-fail boundary for free; cloning gate-evidence-store gives proven atomic write + loud-fail read ladder.
Output: 5 new files + paths.ts extension. Approval store round-trips a pending approval; rejects auto-decided approvals (D-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-complete-audit-record-approval/09-CONTEXT.md
@.planning/phases/09-complete-audit-record-approval/09-RESEARCH.md
@.planning/phases/09-complete-audit-record-approval/09-PATTERNS.md
@.planning/phases/09-complete-audit-record-approval/09-VALIDATION.md

@src/governance/gate-evidence-store.ts
@src/enforcement/validate-gate-result.ts
@src/schema/gate-result.schema.json
@src/governance/paths.ts
@src/governance/atomic-write.ts
</context>

<feature>
  <name>Approval store + schema + validator (APPR-01)</name>
  <files>src/schema/approval.schema.json, src/enforcement/validate-approval.ts, src/enforcement/validate-approval.test.ts, src/governance/approval-store.ts, src/governance/approval-store.test.ts, src/governance/paths.ts</files>
  <behavior>
    - Approval schema: 10 properties per D-05; 7 required (approvalId, phase, gateId, artifactPath, requestedBy, requestedAt, decision); 3 optional (decidedBy, decidedAt, rationale).
    - decision enum is pending|approved|rejected|waived (distinct from GateResult.status — Pitfall 3).
    - validateApproval: Ajv 2020 strict + addFormats + addKeyword("x-binding") BEFORE compile; throws `invalid approval:\n${errors}` on malformed.
    - Post-Ajv D-07 invariant: if decision is not the pending value, decidedBy MUST be present and non-empty (prevents model auto-approval).
    - approval-store.writeApproval(projectRoot, phaseNumber, approval): asserts → atomicWriteFile → no temp siblings left.
    - approval-store.readApproval(projectRoot, phaseNumber): null when missing; throws `malformed approval at ${absPath}: ${detail}` on bad JSON / shape / timestamp.
    - approvalPath: PHASE_NUMBER_RE-validated, returns .planning/governance/approvals/{NN}.json.
    - testEvidencePath: same shape, .planning/governance/tests/{NN}.json (Plan 02 consumes; helper lives here to keep paths.ts as the single path source).
  </behavior>
  <implementation>
    Clone gate-evidence-store.ts 1:1: swap GateEvidence→ApprovalRecord, assertEvidence→assertApproval, gateEvidencePath→approvalPath. Replace the validateGateResult call inside assertApproval with validateApproval. Keep the existsSync→null / try-readFileSync / try-JSON.parse / assertXxx four-rung read ladder verbatim. validate-approval.ts clones validate-gate-result.ts lines 22-44 verbatim (Ajv 2020 + addFormats + addKeyword pre-compile), drops the lineRange invariant, adds the D-07 decidedBy invariant. approval.schema.json clones gate-result.schema.json header convention (x-binding: binding, additionalProperties: false, strict ISO 8601 pattern on timestamps per TD-01).
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>RED: Write failing approval schema + validator + store tests</name>
  <files>src/schema/approval.schema.json, src/enforcement/validate-approval.test.ts, src/governance/approval-store.test.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (sections: approval.schema.json, validate-approval.ts, approval-store.ts, approval-store.test.ts — line-by-line clones specified)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-05 10-field shape, D-07 anti-auto-approve invariant)
    - src/enforcement/validate-gate-result.ts (full — Ajv 2020 + addFormats + addKeyword("x-binding") pre-compile pattern, formatErrors shape)
    - src/enforcement/validate-gate-result.test.ts (acceptance/rejection test idiom)
    - src/governance/gate-evidence-store.ts (full — durable-store template: writeGateEvidence/readGateEvidence/assertEvidence/fail + 4-rung read ladder)
    - src/governance/gate-evidence-store.test.ts (full — withTempRoot + fixture builder + malformed-JSON test idiom to port 1:1)
    - src/schema/gate-result.schema.json (full — header convention: $schema, $id, title, description, type, additionalProperties:false, x-binding)
    - src/governance/paths.ts (full — PHASE_NUMBER_RE + gateEvidencePath template to clone for approvalPath + testEvidencePath)
  </read_first>
  <action>
    Per D-05 and D-15: author approval.schema.json with the 10-field shape (7 required, 3 optional per D-07). Header convention matches gate-result.schema.json exactly: $schema draft 2020-12, $id "https://gsd.dev/schemas/approval.schema.json", title "Human Approval Checkpoint", description cites APPR-01 + D-08 blocking semantics + "x-binding: binding", additionalProperties:false, x-binding:"binding". decision enum is the four-value approval vocab (NOT the gate-status enum — Pitfall 3). Timestamp fields (requestedAt, decidedAt) use the strict ISO-8601 pattern already proven in gate-result.schema.json lines 30-34.

    Port gate-evidence-store.test.ts to approval-store.test.ts 1:1 using the test-case mapping in 09-PATTERNS.md lines 487-501 (approvalPath under .planning/governance/approvals/{NN}.json, round-trip, malformed-JSON loud-fail, missing-required-field loud-fail, metadata-phase-mismatch → approvalId/gateId/decision-enum violations). Use the fixture builder `approval(overrides)` shape from 09-PATTERNS.md lines 473-484.

    Add D-07-specific tests with NO analog: writeApproval rejects a non-pending decision when decidedBy is absent; writeApproval accepts the pending decision with decidedBy absent. These two tests are the anti-auto-approve trust boundary.

    Port validate-gate-result.test.ts to validate-approval.test.ts: schema-valid approval passes; each missing required field throws; each enum violation throws; the D-07 invariant (non-pending decision without decidedBy) throws post-Ajv. Drop the lineRange tests (approval has no line ranges).

    Schema file MUST exist for the validator tests to import it. Validator and store implementations are NOT written in this task — tests MUST fail in RED state (imports resolve to non-existent modules → compilation fails, or assertions fail against unimplemented stubs).

    Commit: `test(09-01): add failing approval schema + validator + store tests`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test 2>&1 | findstr /R "error TS2305 error TS2307 Cannot find module" && echo "RED confirmed (imports fail as expected)" || echo "Build resolved all imports — verify tests actually fail against unimplemented targets"</automated>
  </verify>
  <acceptance_criteria>
    - src/schema/approval.schema.json exists with "x-binding": "binding", "additionalProperties": false, "const"-less schema_version N/A, decision enum with four approval values, 7 required fields exactly
    - src/enforcement/validate-approval.test.ts and src/governance/approval-store.test.ts exist
    - Tests reference symbols not yet implemented (validateApproval, writeApproval, readApproval, ApprovalRecord, approvalPath) — RED state
    - D-07 anti-auto-approve test present (rejects non-pending decision without decidedBy)
    - Test file paths under src/** match the dist-test mirror convention
  </acceptance_criteria>
  <done>RED confirmed: build:test fails because targets unimplemented; tests written against the D-05 10-field shape and D-07 invariant</done>
</task>

<task type="tdd">
  <name>GREEN: Implement approval schema + validator + store + paths helpers</name>
  <files>src/enforcement/validate-approval.ts, src/governance/approval-store.ts, src/governance/paths.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (approval-store.ts, validate-approval.ts, paths.ts, approval.schema.json sections — exact code excerpts)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-05 shape, D-06 routing through runAdapter, D-07 pending-only hook writes)
    - src/enforcement/validate-gate-result.ts (full — 5th instance of this pattern: Ajv 2020 constructor options, addFormats, addKeyword before compile, formatErrors)
    - src/governance/gate-evidence-store.ts (full — writeGateEvidence/readGateEvidence/assertEvidence/fail/assertObject/assertString/assertGateId/assertTimestamp ladder)
    - src/governance/paths.ts (full — governanceDir + PHASE_NUMBER_RE + gateEvidencePath template)
    - src/enforcement/types.ts (GateId type — approval.gateId reuses this enum)
  </read_first>
  <action>
    Extend src/governance/paths.ts: add approvalPath(projectRoot, phaseNumber) and testEvidencePath(projectRoot, phaseNumber) cloning gateEvidencePath exactly (PHASE_NUMBER_RE validation → path.join(governanceDir(...), "approvals"|"tests", `${phaseNumber}.json`)). One file per phase per kind. Plan 02 consumes testEvidencePath without re-modifying paths.ts.

    Implement src/enforcement/validate-approval.ts per 09-PATTERNS.md lines 421-450: clone validate-gate-result.ts verbatim (Ajv 2020 strict + addFormats + addKeyword x-binding before compile), import schema from "../schema/approval.schema.json", export validateApproval(result: unknown): asserts result is ApprovalRecord. Replace the post-Ajv lineRange invariant with the D-07 invariant: if decision is any value other than the pending literal, decidedBy MUST be present and non-empty (prevents the model from auto-deciding approvals — Pitfall 5). Clone formatErrors verbatim.

    Implement src/governance/approval-store.ts per 09-PATTERNS.md lines 28-68: clone gate-evidence-store.ts shape. Export ApprovalRecord interface (mirrors D-05 field list; type gateId as GateId from enforcement/types.ts; decision as the 4-value approval union), writeApproval(projectRoot, phaseNumber, approval) and readApproval(projectRoot, phaseNumber): ApprovalRecord | null. assertApproval calls validateApproval (NOT validateGateResult — approval has its own validator). Keep the 4-rung read ladder (existsSync→null, try-readFileSync, try-JSON.parse, assertApproval) verbatim. fail(filePath, detail) message format: `malformed approval at ${filePath}: ${detail}`.

    TypeScript types: ApprovalRecord lives in approval-store.ts (single source of truth); validate-approval.ts imports it via `import type { ApprovalRecord } from "../governance/approval-store.js"` to avoid circular runtime dep.

    Commit: `feat(09-01): implement approval store + schema + validator`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test && node --test "dist-test/enforcement/validate-approval.test.js" "dist-test/governance/approval-store.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:test exits 0
    - node --test on the two new test files exits 0 (all RED tests now GREEN)
    - src/governance/paths.ts contains approvalPath and testEvidencePath exports
    - src/enforcement/validate-approval.ts exports validateApproval with asserts-result-is-ApprovalRecord signature
    - src/governance/approval-store.ts exports ApprovalRecord, writeApproval, readApproval
    - D-07 test passes: writeApproval throws on non-pending decision without decidedBy
    - D-07 test passes: writeApproval accepts pending decision with decidedBy absent
    - readApproval returns null when file absent
    - Malformed-JSON test passes (throws with "malformed approval at" prefix)
  </acceptance_criteria>
  <done>GREEN: all approval tests pass; schema + validator + store + paths helpers implemented per D-05/D-07/D-15</done>
</task>

<task type="tdd">
  <name>REFACTOR: Confirm full-suite green + delete dead defense</name>
  <files>(no source edits unless refactor needed)</files>
  <read_first>
    - src/governance/approval-store.ts (self-review: any duplication with gate-evidence-store worth extracting? Per PATTERNS anti-pattern guidance, keep them separate — extracting a generic store hurts readability)
    - src/enforcement/validate-approval.ts (self-review: formatErrors is duplicated across 5 validators — that is intentional, do NOT extract)
  </read_first>
  <action>
    Run the full suite (npm test). If green and no duplication warrants extraction (PATTERNS.md anti-pattern: keep stores separate; formatErrors duplication is intentional per Phase 7 decision), commit is a no-op. If any test outside approval-specific files regressed, fix the regression source (likely an unrelated test catching the new approvalPath/testEvidencePath exports via a wildcard import — narrow the wildcard). Do NOT extract a generic store or generic validator — both duplications are deliberate (one crash doesn't take down sibling validators/stores).

    Commit (only if refactor applied): `refactor(09-01): narrow wildcard import / clean up approval store`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (full suite green: existing tests + new approval tests)
    - No regression in any prior-phase test
    - approvalPath and testEvidencePath exports visible in dist-test/governance/paths.js
  </acceptance_criteria>
  <done>REFACTOR: full suite green; no duplication worth extracting; Plan 01 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → approval-store.readApproval | Tampered/malformed JSON on disk crosses here; assertApproval re-validates after parse |
| Model → approval-store.writeApproval | Untrusted caller may attempt to write decision=approved with no decidedBy (Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Spoofing (Elevation of Privilege) | approval-store.writeApproval | high | mitigate | D-07 post-Ajv invariant in validateApproval: non-pending decision requires non-empty decidedBy. Test asserts model cannot auto-approve. |
| T-09-01-02 | Tampering | approval-store.readApproval | medium | mitigate | assertApproval re-validates parsed JSON via Ajv; tampered disk state fails loud with absolute path context (mirrors readGateEvidence). |
| T-09-01-03 | Repudiation (audit integrity) | validate-approval output | low | accept | Approval is a durable record under .planning/governance/approvals/{NN}.json with strict ISO 8601 timestamps (TD-01 shape); repudiation surface is the same as existing gate evidence. |
| T-09-01-04 | Information Disclosure | approval-store error messages | low | accept | Error messages include absolute file paths (matches gate-evidence-store convention); no secrets in approval payloads by design. |
</threat_model>

<verification>
- Approval schema is valid draft 2020-12 (`$schema` key present; Ajv compiles without warning)
- validateApproval throws on each missing required field, each enum violation, and the D-07 invariant
- writeApproval → readApproval round-trips byte-identical content
- readApproval returns null on missing file (not throw)
- approvalPath and testEvidencePath reject non-2-digit phase numbers
- Full suite (npm test) green
</verification>

<success_criteria>
- APPR-01 schema + store + validator implemented through the tool-agnostic contract layer (JSON Schema draft 2020-12 + Ajv 2020 + x-binding keyword registered pre-compile)
- D-05 10-field shape enforced; D-07 anti-auto-approve invariant blocks non-pending decisions without decidedBy
- D-15 new schema published; runtime validation via the existing Ajv pattern (5th instance)
- Plan 02 can consume testEvidencePath from paths.ts without re-modifying it
</success_criteria>

<output>
Create `.planning/phases/09-complete-audit-record-approval/09-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Symbols (exported):**
- `ApprovalRecord` (interface) — src/governance/approval-store.ts
- `writeApproval(projectRoot, phaseNumber, approval): void` — src/governance/approval-store.ts
- `readApproval(projectRoot, phaseNumber): ApprovalRecord | null` — src/governance/approval-store.ts
- `validateApproval(result: unknown): asserts result is ApprovalRecord` — src/enforcement/validate-approval.ts
- `approvalPath(projectRoot, phaseNumber): string` — src/governance/paths.ts
- `testEvidencePath(projectRoot, phaseNumber): string` — src/governance/paths.ts (consumed by Plan 02)

**Schemas:**
- `src/schema/approval.schema.json` (draft 2020-12, x-binding: binding, 10 fields per D-05)

**Test files:**
- `src/enforcement/validate-approval.test.ts`
- `src/governance/approval-store.test.ts`

**Decisions implemented:** D-05 (10-field shape), D-06 (contract layer routing via validateApproval), D-07 (anti-auto-approve invariant), D-15 (new approval schema), partial D-14 (approval-store module), partial D-02/D-06 (paths.ts layout convention for approvals/ + tests/).
