---
phase: 09-complete-audit-record-approval
plan: 03
type: tdd
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/schema/audit-artifact.schema.json
  - src/governance/audit-artifact.ts
  - src/governance/audit-enrich.ts
  - src/governance/audit-enrich.test.ts
  - src/governance/audit-artifact.test.ts
autonomous: true
requirements: [AUDIT-03, AUDIT-05, AUDIT-06]
user_setup: []

must_haves:
  truths:
    - "Audit artifact schema bumps schema_version.const 1→2 (NOT enum) — Pitfall 1; v1 records FAIL validation against v2 (forward-incompatible by design — regeneration mandatory)"
    - "4 optional v2 fields appended AFTER rules_skipped in properties AND in the TS interface: requirements_covered, tests_executed, remaining_risks, approvals (D-09 byte-stability — V8 insertion-order preservation)"
    - "v1-only input regenerated under v2 produces byte-identical output for the v1 subset (string-compare, NOT deep-equal — Pitfall 2)"
    - "extractRequirementsCovered machine-extracts REQ-IDs from REQUIREMENTS.md traceability table via regex (D-10 — never executor tags, never model narration)"
    - "collectRemainingRisks aggregates VERIFICATION.md gaps + CONTEXT.md <deferred> items; NEVER returns [] — emits explicit none-identified row when empty (D-11)"
    - "summarizeApprovals projects ApprovalRecord[] → ApprovalSummary[] (D-12 — single-sourced from approval store, never re-queried through adapter at audit time)"
    - "buildAuditRecord accepts optional enrichment payload; the 4 new fields are spread conditionally (only when enrichment data is present)"
  artifacts:
    - "src/schema/audit-artifact.schema.json (v1→v2 bump)"
    - "src/governance/audit-enrich.ts (NEW: 3 pure helpers)"
    - "src/governance/audit-enrich.test.ts (NEW)"
    - "src/governance/audit-artifact.ts (MODIFIED: GovernanceAudit v2 + buildAuditRecord enrichment hook)"
    - "src/governance/audit-artifact.test.ts (MODIFIED: add v1-byte-stability string-compare test)"
  key_links:
    - "audit-enrich.extractRequirementsCovered ← REQUIREMENTS.md traceability table (regex)"
    - "audit-enrich.collectRemainingRisks ← VERIFICATION.md + CONTEXT.md <deferred> (regex)"
    - "audit-enrich.summarizeApprovals ← approval-store.ApprovalRecord[] (Plan 01)"
    - "audit-artifact.buildAuditRecord ← audit-enrich output (optional enrichment payload)"
    - "v1 byte-stability test: renderGovernanceMarkdown(v1Input) === renderGovernanceMarkdown(v1Input) (string compare)"
---

<objective>
Bump the audit artifact to v2 and implement the three enrichment helpers (AUDIT-03 requirements covered, AUDIT-05 remaining risks, AUDIT-06 approval summary). The 4 optional v2 fields are appended after the existing 7 to preserve v1 byte-stability (D-09); v1-only input must regenerate byte-identical output. enrichment helpers are pure functions (no I/O) — extracted from REQUIREMENTS.md traceability, VERIFICATION.md + CONTEXT.md <deferred>, and the approval store (read by the hook, not re-queried through the adapter per D-12).

Purpose: AUDIT-03/05/06 complete the audit record to the full enterprise SDLC evidence set. D-14 requires enrichment logic in a separate audit-enrich.ts module so buildAuditRecord stays thin and the v1 subset stays byte-stable.
Output: 2 new files + 3 modified. Plan 04's verify:post audit hook will call these helpers to build the enrichment payload.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-complete-audit-record-approval/09-CONTEXT.md
@.planning/phases/09-complete-audit-record-approval/09-RESEARCH.md
@.planning/phases/09-complete-audit-record-approval/09-PATTERNS.md
@.planning/phases/09-complete-audit-record-approval/09-VALIDATION.md

@src/governance/audit-artifact.ts
@src/governance/audit-artifact.test.ts
@src/schema/audit-artifact.schema.json
@.planning/REQUIREMENTS.md
</context>

<feature>
  <name>Audit v2 enrichment (AUDIT-03/05/06)</name>
  <files>src/schema/audit-artifact.schema.json, src/governance/audit-artifact.ts, src/governance/audit-enrich.ts, src/governance/audit-enrich.test.ts, src/governance/audit-artifact.test.ts</files>
  <behavior>
    - extractRequirementsCovered(requirementsMd: string, phaseNumber: string): RequirementsCoveredEntry[] — regex over the Traceability table rows where the Phase column matches phaseNumber; returns {reqId, title, status} entries. Never returns [] for a phase with mapped REQ-IDs (REQUIREMENTS.md is the source of truth).
    - collectRemainingRisks(verificationMd: string, contextDeferredSection: string): RemainingRiskEntry[] — aggregates gaps from VERIFICATION.md + items from CONTEXT.md <deferred> section. NEVER returns [] — emits [{id: "none-identified", severity: "low", detail: "no risks found", source: "none-identified"}] when empty (D-11).
    - summarizeApprovals(approvals: ApprovalRecord[]): ApprovalSummary[] — projects to {approvalId, gateId, decision, decidedBy?}. Empty input → [{approvalId: "none-required", gateId: "ship", decision: "waived", ...}] placeholder (D-11 analog for approvals). Single-sourced from approval store (D-12 — never re-queries adapter).
    - GovernanceAudit.schema_version literal type changes from 1 to 2.
    - buildAuditRecord(record, enrichment?) — the 4 new fields are spread ONLY when present in the enrichment payload; absent enrichment → byte-identical to v1 output (Pitfall 2).
    - audit-artifact.schema.json: const 1 → const 2 (Pitfall 1); 4 new optional properties appended AFTER rules_skipped; matching $defs entries; NONE of the 4 new fields added to required array.
    - A v1 fixture (schema_version: 1) MUST FAIL validation against the v2 schema (forward-incompatible by design).
  </behavior>
  <implementation>
    audit-enrich.ts is pure helpers only — no I/O (D-14). Clone the assertOneOf/assertString/assertRecordObject guard family from audit-artifact.ts lines 71-126 for any input-shape validation (none expected — inputs are strings + typed arrays). Regex over REQUIREMENTS.md traceability table: parse `| {REQ-ID} | {Phase} | {Status} |` rows where Phase column matches phaseNumber. Regex over VERIFICATION.md for gap markers (e.g., `- [ ]` unchecked items, "TODO", "GAP", "Flagged" — adapt to actual VERIFICATION.md format). Regex over CONTEXT.md <deferred> section: extract list items between <deferred> and </deferred>. buildAuditRecord: add an optional second arg `enrichment?` and spread the 4 fields conditionally — `...(enrichment?.requirements_covered ? { requirements_covered } : {})` for each. Field order in the return literal MUST be: existing 7 first (unchanged order), then the 4 new fields in D-09 order (requirements_covered, tests_executed, remaining_risks, approvals) when present.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>RED: Write failing audit-enrich tests + v1-byte-stability test</name>
  <files>src/governance/audit-enrich.test.ts, src/governance/audit-artifact.test.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (audit-enrich.ts section lines 115-159 — guard idiom + module shape + never-empty placeholder; audit-enrich.test.ts section lines 542-571 — specific test cases to port)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-09 byte-stability, D-10 REQ-ID extraction, D-11 risks never empty, D-12 approvals from store)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Common Pitfalls #1 schema_version const, #2 field-order drift, #6 silent-empty; Code Examples "Audit Artifact v2 Type Extension" lines 412-459)
    - src/governance/audit-artifact.ts (full — GovernanceAudit lines 40-48, buildAuditRecord lines 195-220, renderGovernanceMarkdown line 222-224, assertOneOf/assertString/assertTimestamp guards)
    - src/governance/audit-artifact.test.ts (full — test idiom: withTempRoot, fixture builders, "renderGovernanceMarkdown is deterministic" lines 291-298 to extend)
    - src/schema/audit-artifact.schema.json (full — required array lines 8-16, properties lines 17-52, schema_version const line 20)
    - .planning/REQUIREMENTS.md (Traceability table lines 73-95 — the parser target format)
    - .planning/phases/08-remaining-gate-hooks/VERIFICATION.md OR .planning/phases/07-*/VERIFICATION.md (real VERIFICATION.md format to parse — confirm regex against actual file shape, not assumed)
  </read_first>
  <action>
    Write src/governance/audit-enrich.test.ts per 09-PATTERNS.md lines 542-571:
    - extractRequirementsCovered returns REQ-IDs from a phase's traceability rows (fixture: markdown with `| AUDIT-03 | 09 |` etc.; assert reqId list matches for phase 09; assert other-phase REQ-IDs are NOT returned)
    - extractRequirementsCovered handles empty traceability gracefully (returns explicit none-identified row OR throws — planner discretion; document choice)
    - collectRemainingRisks emits none-identified row when both inputs are empty (D-11 — never [])
    - collectRemainingRisks aggregates VERIFICATION.md gaps AND CONTEXT <deferred> items (assert length >= 2 when both have content; assert none-identified absent)
    - summarizeApprovals projects ApprovalRecord[] → ApprovalSummary[] (fixture: 2 approvals → 2 summaries with {approvalId, gateId, decision, decidedBy?})
    - summarizeApprovals empty input → none-required placeholder row (D-11 analog)

    Extend src/governance/audit-artifact.test.ts:
    - Add a v1-byte-stability test: build a GovernanceRecord fixture, call buildAuditRecord(record) WITHOUT enrichment, then call renderGovernanceMarkdown, capture the output string. Call buildAuditRecord(record) again (no enrichment), render again, capture. Assert the two strings are byte-identical via `first === second` (NOT deep-equal — Pitfall 2). The v1 subset MUST regenerate identically under v2.
    - Add a schema-rejection test: import the v2 schema, compile via Ajv 2020, assert that a fixture with schema_version: 1 FAILS validation (Pitfall 1 — forward-incompatible by design).
    - Add a v2-acceptance test: a fixture with schema_version: 2 and the 4 optional fields PASSES validation.

    Implementations NOT written in this task — tests MUST fail in RED state.

    Commit: `test(09-03): add failing audit-enrich + v1-byte-stability tests`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test 2>&1 | findstr /R "error TS2307 Cannot find module" && echo "RED confirmed" || echo "Verify tests actually fail against unimplemented targets"</automated>
  </verify>
  <acceptance_criteria>
    - src/governance/audit-enrich.test.ts exists with parser tests (extractRequirementsCovered, collectRemainingRisks, summarizeApprovals) + D-11 never-empty assertions
    - src/governance/audit-artifact.test.ts has new tests: v1-byte-stability string-compare, schema rejects v1, schema accepts v2-with-optionals
    - Tests reference extractRequirementsCovered, collectRemainingRisks, summarizeApprovals, v2 GovernanceAudit type — unimplemented → RED
    - D-11 never-empty test present for collectRemainingRisks
    - Byte-stability test uses `===` string compare (NOT deepEqual)
  </acceptance_criteria>
  <done>RED confirmed: tests cover enrichment helpers + v2 schema + v1 byte-stability + D-11 never-empty</done>
</task>

<task type="tdd">
  <name>GREEN: Implement audit-enrich helpers + v2 schema bump + buildAuditRecord enrichment hook</name>
  <files>src/schema/audit-artifact.schema.json, src/governance/audit-artifact.ts, src/governance/audit-enrich.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (audit-enrich.ts lines 115-159 — exact module shape; audit-artifact.ts MODIFY section lines 163-210 — type extension + buildAuditRecord enrichment hook)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Code Examples "Audit Artifact v2 Type Extension" lines 412-459; Common Pitfalls #1, #2, #6)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-09 byte-stability, D-10 REQ-IDs from traceability, D-11 risks never empty, D-12 approvals from store not adapter, D-14 separate module, D-15 schema bump)
    - src/governance/audit-artifact.ts (full — GovernanceAudit interface lines 40-48, buildAuditRecord lines 195-220, renderGovernanceMarkdown line 222)
    - src/schema/audit-artifact.schema.json (full — required lines 8-16 UNCHANGED, properties lines 17-52 append AFTER rules_skipped, $defs section add 4 new entries)
    - src/governance/approval-store.ts (Plan 01 — ApprovalRecord type to consume in summarizeApprovals)
    - src/governance/test-evidence.ts (Plan 02 — TestEvidenceSummary type to consume in tests_executed)
    - .planning/REQUIREMENTS.md (Traceability table — regex target for extractRequirementsCovered)
  </read_first>
  <action>
    Implement src/governance/audit-enrich.ts per 09-PATTERNS.md lines 115-159. Three pure exported helpers:

    extractRequirementsCovered(requirementsMd, phaseNumber): regex `/^\| ([A-Z]+-\d+) \| (\d{2}(?:\.\d+)?) \| ([^|]+) \|/gm` over the Traceability table; filter rows where capture group 2 === phaseNumber; map to {reqId: group1, title: group3.trim(), status: "complete" | "pending" | "partial" derived from the Status column text}. RESEARCH §Assumptions A5 confirms the prefix convention.

    collectRemainingRisks(verificationMd, contextDeferredSection): scan VERIFICATION.md for unchecked items (`- [ ]`, "TODO", "GAP", "Flagged" — adapt to actual file shape you read in read_first). Scan CONTEXT.md <deferred> section: extract list items between the <deferred> and </deferred> sentinel tokens. Map each to {id, severity: "medium" default, detail, source: "VERIFICATION.md" | "CONTEXT.md<deferred>"}. If both inputs yield zero risks, return [{id: "none-identified", severity: "low", detail: "no risks found in VERIFICATION.md or CONTEXT.md <deferred>", source: "none-identified"}]. NEVER return [].

    summarizeApprovals(approvals: ApprovalRecord[]): ApprovalSummary[] — map each to {approvalId, gateId, decision, decidedBy?}. If input is empty, return [{approvalId: "none-required", gateId: "ship", decision: "waived"}] placeholder (D-11 analog).

    Modify src/schema/audit-artifact.schema.json: change line 20 from `"const": 1` to `"const": 2` (Pitfall 1 — NOT enum). Append 4 properties AFTER rules_skipped (line 52): requirements_covered (array of $ref), tests_executed ($ref), remaining_risks (array of $ref), approvals (array of $ref). Add matching $defs entries mirroring the TS interfaces in 09-RESEARCH.md Code Examples lines 416-443. Do NOT add any of the 4 to the required array (D-09). x-binding stays "advisory" (audit records, not enforces).

    Modify src/governance/audit-artifact.ts: bump GovernanceAudit.schema_version literal from 1 to 2. Add 4 optional typed fields to the interface AFTER rules_skipped (D-09 field order — V8 insertion order preservation, Pitfall 2). Import RequirementsCoveredEntry, TestEvidenceSummary, RemainingRiskEntry, ApprovalSummary types from audit-enrich.ts (or define in audit-enrich.ts and re-export — planner discretion). Extend buildAuditRecord to accept an optional second arg `enrichment?: { requirements_covered?, tests_executed?, remaining_risks?, approvals? }` and spread the 4 fields conditionally AFTER the existing 7 — `...(enrichment?.requirements_covered ? { requirements_covered: enrichment.requirements_covered } : {})` for each. Change the returned schema_version literal from 1 to 2. Leave writeGovernanceAudit and renderGovernanceMarkdown unchanged (they consume GovernanceAudit; the v2 shape flows through automatically).

    Commit: `feat(09-03): implement audit v2 enrichment + bump schema_version`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test && node --test "dist-test/governance/audit-enrich.test.js" "dist-test/governance/audit-artifact.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:test exits 0
    - node --test on audit-enrich.test.js + audit-artifact.test.js exits 0 (RED → GREEN)
    - src/schema/audit-artifact.schema.json line 20 contains `"const": 2` (NOT enum)
    - src/schema/audit-artifact.schema.json required array still has exactly 7 entries (no v2 fields added)
    - src/governance/audit-artifact.ts GovernanceAudit interface has schema_version: 2 literal
    - src/governance/audit-artifact.ts buildAuditRecord accepts optional enrichment arg
    - v1-byte-stability test passes (string `===` compare, NOT deepEqual)
    - Schema-rejection test passes (v1 fixture fails Ajv validation)
    - extractRequirementsCovered returns AUDIT-03/04/05/06 + APPR-01 for phase 09 traceability rows
    - collectRemainingRisks never returns [] (none-identified row emitted when empty)
    - summarizeApprovals empty input → none-required placeholder
  </acceptance_criteria>
  <done>GREEN: v2 schema + enrichment helpers + buildAuditRecord hook implemented; v1 byte-stability preserved; D-11 never-empty enforced</done>
</task>

<task type="tdd">
  <name>REFACTOR: Full-suite green + v1 historical GOVERNANCE.md policy</name>
  <files>(no source edits unless refactor needed)</files>
  <read_first>
    - src/governance/audit-artifact.ts (self-review: is the enrichment-arg shape clean? Could it be a single object type? If so, define AuditEnrichment interface for clarity — small refactor OK)
    - src/governance/audit-enrich.ts (self-review: any regex catastrophic-backtracking risk? Anchor patterns with `^` and use the `m` flag — confirmed in implementation)
    - .planning/phases/08-remaining-gate-hooks/GOVERNANCE.md (existing v1 audit file — confirm policy: leave as v1, do NOT regenerate under v2; only Phase 9+ emits v2 per RESEARCH §Open Questions #3)
  </read_first>
  <action>
    Run npm test. If green and no refactor warranted, no-op. If the AuditEnrichment arg shape would benefit from a named interface, extract it (small refactor, optional). Do NOT regenerate historical v1 GOVERNANCE.md files under v2 — only Phase 9+ emits v2 (RESEARCH §Open Questions #3). The v1-byte-stability test already proves a v1-shaped input regenerates byte-identical output for the v1 subset; that is the contract, not historical file migration.

    Commit (only if refactor applied): `refactor(09-03): extract AuditEnrichment interface`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (full suite green)
    - No regression in any prior-phase test
    - Existing "renderGovernanceMarkdown is deterministic" test (audit-artifact.test.ts lines 291-298) still passes
    - Historical v1 GOVERNANCE.md files under .planning/phases/01..08/ NOT modified
  </acceptance_criteria>
  <done>REFACTOR: full suite green; Plan 03 complete; v1 byte-stability contract verified by string-compare test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md → extractRequirementsCovered | Bounded markdown format; deterministic regex; RESEARCH §Don't Hand-Roll confirms no AST dep warranted |
| VERIFICATION.md + CONTEXT.md → collectRemainingRisks | Bounded markdown; regex extraction; never-empty invariant prevents silent audit gaps |
| Approval store → summarizeApprovals | Typed ApprovalRecord[] from Plan 01; pure projection (no adapter re-query per D-12) |
| buildAuditRecord enrichment payload → GovernanceAudit v2 | The 4 optional fields cross from helper output into the persisted audit; conditional spread guarantees v1 byte-stability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Repudiation (audit integrity) | buildAuditRecord v1 byte-stability | high | mitigate | D-09 + Pitfall 2: 4 new fields appended AFTER existing 7, spread only when present. Test uses string `===` compare (NOT deepEqual). |
| T-09-03-02 | Tampering (data integrity) | audit-artifact.schema.json v2 bump | high | mitigate | Pitfall 1: const 1 → const 2 (NOT enum). v1 records FAIL v2 validation — regeneration mandatory, no silent v1/v8 coexistence. |
| T-09-03-03 | Information Disclosure (audit gap) | collectRemainingRisks | medium | mitigate | D-11: never returns []. Emits explicit none-identified row so reviewers distinguish "researched and found none" from "not checked". |
| T-09-03-04 | Tampering | summarizeApprovals | low | accept | Pure projection over typed ApprovalRecord[] from the approval store (Plan 01); no disk I/O; no adapter re-query (D-12). |
</threat_model>

<verification>
- Audit schema v2 rejects v1 records (forward-incompatible — Pitfall 1)
- v1-only input regenerates byte-identical output under v2 (string `===`, Pitfall 2)
- extractRequirementsCovered returns AUDIT-03/04/05/06 + APPR-01 for phase 09 (real REQUIREMENTS.md)
- collectRemainingRisks never returns []
- summarizeApprovals empty input → none-required placeholder
- buildAuditRecord accepts optional enrichment payload
- Full suite (npm test) green
</verification>

<success_criteria>
- AUDIT-03 (requirements covered), AUDIT-05 (remaining risks), AUDIT-06 (approvals summary) implemented as pure helpers in audit-enrich.ts
- D-09 v2 byte-stability verified by string-compare test
- D-10 REQ-IDs machine-extracted from REQUIREMENTS.md traceability
- D-11 risks never silently empty
- D-12 approvals single-sourced from store, not re-queried through adapter
- D-14 audit-enrich.ts is a separate module (buildAuditRecord stays thin)
- D-15 audit-artifact.schema.json bumped to v2 with 4 optional fields
- Plan 04 can consume buildAuditRecord + audit-enrich helpers to emit enriched GOVERNANCE.md
</success_criteria>

<output>
Create `.planning/phases/09-complete-audit-record-approval/09-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Symbols (exported):**
- `RequirementsCoveredEntry` (interface) — {reqId, title, status}
- `RemainingRiskEntry` (interface) — {id, severity, detail, source}
- `ApprovalSummary` (interface) — {approvalId, gateId, decision, decidedBy?}
- `extractRequirementsCovered(requirementsMd, phaseNumber): RequirementsCoveredEntry[]` — pure
- `collectRemainingRisks(verificationMd, contextDeferredSection): RemainingRiskEntry[]` — pure, never-empty
- `summarizeApprovals(approvals: ApprovalRecord[]): ApprovalSummary[]` — pure, never-empty
- `buildAuditRecord(record, enrichment?): GovernanceAudit` — extended signature (backward-compatible)

**Modified symbols:**
- `GovernanceAudit` interface — schema_version: 2 + 4 optional fields appended

**Schemas:**
- `src/schema/audit-artifact.schema.json` (v1→v2: const 2, 4 optional properties, 4 new $defs)

**Test files:**
- `src/governance/audit-enrich.test.ts` (NEW)
- `src/governance/audit-artifact.test.ts` (MODIFIED: v1-byte-stability + schema v2 tests)

**Decisions implemented:** D-09 (v2 byte-stable bump), D-10 (REQ-IDs from traceability), D-11 (risks never empty), D-12 (approvals from store not adapter), D-14 (audit-enrich module separate), D-15 (audit-artifact schema v2). Requirements covered: AUDIT-03, AUDIT-05, AUDIT-06.