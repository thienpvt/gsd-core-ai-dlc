---
phase: 09-complete-audit-record-approval
plan: 04
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/governance/ship-gate-hook.ts
  - src/governance/ship-gate-hook.test.ts
  - .gsd/capabilities/aidlc-governance/capability.json
autonomous: true
requirements: [AUDIT-06, APPR-01]
user_setup: []

must_haves:
  truths:
    - "Ship gate reads approval via readApprovalOrFail: if absent, creates a pending approval for the ship gate itself (D-07) and throws 'pending approval created — human resolution required' (one approval per phase for v2.0 per RESEARCH A2)"
    - "Ship gate calls assertNoBlockingApprovals: pending OR rejected decision throws (D-08 fail-closed, mirrors GATE-05 assertNonBlocking); approved OR waived proceeds"
    - "writePendingApproval writes decision='pending' with decidedBy/decidedAt ABSENT (not empty string — D-07 anti-auto-approve); approvalId is `ship-{phaseNumber}`"
    - "Existing ship evidence (GateEvidence under gates/{NN}-ship.json) is UNCHANGED — approval state lives in a separate file (approvals/{NN}.json); existing test 'assert.equal(\"approvals\" in asRecord, false)' on ship evidence stays green"
    - "Capability manifest verify:post audit step consumes tests/{NN}.json + approvals/{NN}.json + REQUIREMENTS.md + VERIFICATION.md + CONTEXT.md (D-13)"
    - "Capability manifest ship:pre step produces approvals/{NN}.json and consumes GOVERNANCE.md (D-16 — no new capability, extend existing one)"
  artifacts:
    - "src/governance/ship-gate-hook.ts (MODIFIED: readApprovalOrFail + assertNoBlockingApprovals + writePendingApproval)"
    - "src/governance/ship-gate-hook.test.ts (MODIFIED: approval-blocking tests)"
    - ".gsd/capabilities/aidlc-governance/capability.json (MODIFIED: verify:post + ship:pre produces/consumes arrays)"
  key_links:
    - "ship-gate-hook.readApprovalOrFail → approval-store.readApproval + writeApproval (Plan 01)"
    - "ship-gate-hook.assertNoBlockingApprovals ← ApprovalRecord.decision enum (Plan 01)"
    - "ship-gate-hook ship evidence write → gate-evidence-store.writeGateEvidence (unchanged)"
    - "capability.json verify:post audit step consumes → tests/{NN}.json + approvals/{NN}.json (D-13)"
    - "capability.json ship:pre step produces → approvals/{NN}.json (D-16)"
---

<objective>
Extend the ship gate to block on pending/rejected approvals (D-08, GATE-05 mirror) and create pending approval requests when none exist (D-07). The ship gate reads from the approval store (Plan 01), not the adapter — D-12 single-sourcing. Also extend the capability manifest: verify:post audit step consumes test evidence + approvals + requirements/risks sources (D-13); ship:pre step produces approvals and consumes the enriched GOVERNANCE.md (D-16 — extend existing capability, no new one).

Purpose: AUDIT-06 enforcement + APPR-01 ship integration. This completes the approval lifecycle: ship gate creates pending → human resolves out-of-band → ship gate re-runs and blocks until decision is approved/waived. The manifest edit makes the new durable state visible to the GSD loop host.
Output: 3 modified files. After this plan, the full Phase 9 audit+approval surface is wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-complete-audit-record-approval/09-CONTEXT.md
@.planning/phases/09-complete-audit-record-approval/09-RESEARCH.md
@.planning/phases/09-complete-audit-record-approval/09-PATTERNS.md
@.planning/phases/09-complete-audit-record-approval/09-VALIDATION.md

@src/governance/ship-gate-hook.ts
@src/governance/ship-gate-hook.test.ts
@src/governance/approval-store.ts
@src/governance/audit-artifact.ts
@src/governance/audit-enrich.ts
@src/governance/test-evidence.ts
@.gsd/capabilities/aidlc-governance/capability.json
</context>

<feature>
  <name>Ship-gate approval blocking + capability manifest (AUDIT-06 enforcement + APPR-01 ship integration)</name>
  <files>src/governance/ship-gate-hook.ts, src/governance/ship-gate-hook.test.ts, .gsd/capabilities/aidlc-governance/capability.json</files>
  <behavior>
    - readApprovalOrFail(projectRoot, phaseNumber): ApprovalRecord — if readApproval returns null, write a pending approval (approvalId `ship-{phaseNumber}`, gateId "ship", artifactPath = gateEvidencePath for ship, requestedBy "aidlc-governance-ship", requestedAt = new Date().toISOString(), decision "pending", decidedBy/decidedAt ABSENT) and throw `ship gate: {phaseNumber} pending approval created — human resolution required`. If readApproval returns a record, return it.
    - assertNoBlockingApprovals(approval): void — throws `ship gate: approval {approvalId} is {decision} — human resolution required` when decision is the pending or rejected value (D-08). Approved/waived proceeds.
    - shipGateHook: after existing plan/verify evidence checks, call readApprovalOrFail then assertNoBlockingApprovals. The existing ship evidence write (writeGateEvidence) runs ONLY after approval checks pass — approval blocking is BEFORE the ship evidence write, matching the fail-closed prior-evidence pattern.
    - Existing ship evidence (GateEvidence) shape UNCHANGED — no approval fields added to it. Approval state lives in approvals/{NN}.json. Existing test asserting no approval fields on ship evidence stays green.
    - Capability manifest verify:post audit step (lines 84-92): extends consumes to add tests/{NN}.json, approvals/{NN}.json, .planning/REQUIREMENTS.md, .planning/phases/{NN}-*/VERIFICATION.md, .planning/phases/{NN}-*/CONTEXT.md (D-13).
    - Capability manifest ship:pre step (lines 93-106): extends produces to add approvals/{NN}.json; extends consumes to add .planning/phases/{NN}-*/GOVERNANCE.md (D-16).
    - No new capability, no new hook point (D-13, D-16).
  </behavior>
  <implementation>
    Clone the readRequiredEvidence + assertNonBlocking pattern from ship-gate-hook.ts lines 18-45 for the approval check. writePendingApproval is a thin helper: construct the ApprovalRecord literal with decidedBy/decidedAt OMITTED (not empty string — D-07), call writeApproval from Plan 01. Insert the approval checks in shipGateHook body AFTER assertNonBlocking(planEvidence) and assertNonBlocking(verifyEvidence), BEFORE the evidence construction block. Manifest edits are JSON array extensions only — do not restructure steps or add new ones.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>RED: Write failing ship-gate approval-blocking tests</name>
  <files>src/governance/ship-gate-hook.test.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (ship-gate-hook.ts MODIFY section lines 214-272 — readApprovalOrFail + assertNoBlockingApprovals + writePendingApproval proposed code; existing test extension point lines 270-272)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-07 pending-only hook writes, D-08 fail-closed on pending/rejected)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Code Examples "Ship-Gate Approval Check" lines 462-480; Assumptions A2 — one approval for ship gate per phase; Common Pitfalls #5 — auto-decide)
    - src/governance/ship-gate-hook.ts (full — readRequiredEvidence lines 18-30, assertNonBlocking lines 39-45, shipGateHook lines 47-78)
    - src/governance/ship-gate-hook.test.ts (full — existing test idiom: withTempRoot, fixture builders, the `assert.equal("approvals" in asRecord, false)` assertion on ship evidence at lines 177-184 that MUST stay green)
    - src/governance/approval-store.ts (Plan 01 — readApproval, writeApproval, ApprovalRecord shape)
    - src/governance/gate-evidence-store.ts (writeGateEvidence — used by existing tests to set up plan/verify evidence so the ship gate gets past those checks to reach the approval check)
  </read_first>
  <action>
    Extend src/governance/ship-gate-hook.test.ts with new test cases. Each test must first set up valid plan + verify gate evidence (via writeGateEvidence) so the ship gate reaches the approval check — the existing tests show this setup idiom.

    Add tests:
    - "ship gate creates a pending approval when none exists and throws" — no approval file present; call shipGateHook; assert.throws with `/pending approval created.*human resolution required/i`; assert the approval file exists at approvalPath with decision "pending" and decidedBy ABSENT (D-07). Use a deepEqual/strictEqual check that decidedBy is undefined (not empty string).
    - "ship gate blocks on pending approval" — write an approval with decision "pending" (decidedBy absent); call shipGateHook; assert.throws with `/approval.*pending.*human resolution/i`; assert ship evidence was NOT written (gateEvidencePath for ship does not exist).
    - "ship gate blocks on rejected approval" — write an approval with decision "rejected" + decidedBy "human-approver" + decidedAt timestamp; call shipGateHook; assert.throws with `/approval.*rejected/i`; assert ship evidence NOT written.
    - "ship gate proceeds on approved" — write an approval with decision "approved" + decidedBy + decidedAt; call shipGateHook; assert no throw; assert ship evidence WAS written (gateEvidencePath for ship exists).
    - "ship gate proceeds on waived" — same as approved but decision "waived"; assert no throw + ship evidence written.
    - "ship evidence record has NO approval fields" — after the approved-case passes, read the ship GateEvidence and assert `!("approvals" in asRecord)` and `!("decision" in asRecord)` (existing test line 177-184 stays green; approval state is in a separate file).

    Implementations NOT written — tests MUST fail in RED state (readApprovalOrFail, assertNoBlockingApprovals, writePendingApproval not yet in ship-gate-hook.ts).

    Commit: `test(09-04): add failing ship-gate approval-blocking tests`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test 2>&1 | findstr /R "error TS2307 Cannot find module" && echo "RED confirmed" || echo "Verify tests fail against unimplemented targets"</automated>
  </verify>
  <acceptance_criteria>
    - src/governance/ship-gate-hook.test.ts has 6 new approval-blocking tests (create-pending, block-pending, block-rejected, proceed-approved, proceed-waived, no-approval-fields-on-ship-evidence)
    - Tests reference readApprovalOrFail / assertNoBlockingApprovals / writePendingApproval — unimplemented → RED
    - D-07 test asserts decidedBy is ABSENT (undefined) on the pending approval created by the hook, not empty string
    - D-08 test asserts pending and rejected both throw; approved and waived both proceed
    - Existing ship-evidence-has-no-approval-fields test still passes (not regressed)
  </acceptance_criteria>
  <done>RED confirmed: approval-blocking tests fail against unimplemented ship-gate-hook extensions</done>
</task>

<task type="tdd">
  <name>GREEN: Implement ship-gate approval blocking + extend capability manifest</name>
  <files>src/governance/ship-gate-hook.ts, .gsd/capabilities/aidlc-governance/capability.json</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (ship-gate-hook.ts section lines 214-272 — exact proposed code for readApprovalOrFail + assertNoBlockingApprovals + writePendingApproval; capability.json section lines 576-619 — exact JSON array extensions)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-07, D-08, D-13, D-16)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Code Examples lines 462-480; Assumptions A2 — one approval per phase for v2.0)
    - src/governance/ship-gate-hook.ts (full — insert approval checks after line 51 assertNonBlocking calls, before line 53 evidence construction)
    - src/governance/approval-store.ts (Plan 01 — readApproval, writeApproval, ApprovalRecord)
    - src/governance/paths.ts (Plan 01 — approvalPath, gateEvidencePath)
    - .gsd/capabilities/aidlc-governance/capability.json (full — verify:post audit step lines 84-92, ship:pre step lines 93-106)
  </read_first>
  <action>
    Modify src/governance/ship-gate-hook.ts: import readApproval, writeApproval, ApprovalRecord from "./approval-store.js" and gateEvidencePath from "./paths.js" (already imported). Add three helpers per 09-PATTERNS.md lines 237-262:

    writePendingApproval(projectRoot, phaseNumber): constructs ApprovalRecord with approvalId `ship-${phaseNumber}`, phase "construction" (or derive from verifyEvidence.request.phase — use that for consistency), gateId "ship", artifactPath = gateEvidencePath(projectRoot, phaseNumber, "ship"), requestedBy "aidlc-governance-ship", requestedAt = new Date().toISOString(), decision "pending". decidedBy and decidedAt are OMITTED (not empty string — D-07 anti-auto-approve). Call writeApproval.

    readApprovalOrFail(projectRoot, phaseNumber): ApprovalRecord — call readApproval; if null, call writePendingApproval then throw `ship gate: ${phaseNumber} pending approval created — human resolution required`; else return the record.

    assertNoBlockingApprovals(approval): void — if approval.decision is the pending or rejected value, throw `ship gate: approval ${approval.approvalId} is ${approval.decision} — human resolution required`.

    In shipGateHook body: after the two existing assertNonBlocking calls (line 51), insert:
    `const approval = readApprovalOrFail(args.projectRoot, args.phaseNumber);`
    `assertNoBlockingApprovals(approval);`
    The existing ship evidence construction + writeGateEvidence call runs AFTER these checks pass. Do NOT add approval fields to the ship GateEvidence record (D-12 — approval state lives in approvals/{NN}.json).

    Modify .gsd/capabilities/aidlc-governance/capability.json per 09-PATTERNS.md lines 580-619:
    - verify:post audit step (the one with ref.skill "aidlc-governance-audit", lines 84-92): extend consumes array to add ".planning/governance/tests/{NN}.json", ".planning/governance/approvals/{NN}.json", ".planning/REQUIREMENTS.md", ".planning/phases/{NN}-*/VERIFICATION.md", ".planning/phases/{NN}-*/CONTEXT.md" (D-13).
    - ship:pre step (lines 93-106): extend produces to add ".planning/governance/approvals/{NN}.json"; extend consumes to add ".planning/phases/{NN}-*/GOVERNANCE.md" (D-16).
    - Do NOT add new steps, new capabilities, or new hook points. Array edits only.

    Commit: `feat(09-04): ship-gate approval blocking + manifest extension`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test && node --test "dist-test/governance/ship-gate-hook.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:test exits 0
    - node --test dist-test/governance/ship-gate-hook.test.js exits 0 (RED → GREEN)
    - src/governance/ship-gate-hook.ts contains readApprovalOrFail, assertNoBlockingApprovals, writePendingApproval
    - shipGateHook calls readApprovalOrFail + assertNoBlockingApprovals AFTER plan/verify checks, BEFORE ship evidence write
    - Ship GateEvidence record has NO approval fields (existing assertion stays green)
    - .gsd/capabilities/aidlc-governance/capability.json verify:post audit step consumes array includes tests/{NN}.json + approvals/{NN}.json + REQUIREMENTS.md
    - .gsd/capabilities/aidlc-governance/capability.json ship:pre step produces array includes approvals/{NN}.json
    - No new capability or hook point added (D-13, D-16)
    - D-07: writePendingApproval writes decision "pending" with decidedBy ABSENT (undefined, not "")
    - D-08: pending/rejected throws; approved/waived proceeds
  </acceptance_criteria>
  <done>GREEN: ship-gate approval blocking + manifest extension implemented; D-07/D-08/D-13/D-16 satisfied</done>
</task>

<task type="tdd">
  <name>REFACTOR: Full-suite green + end-to-end approval lifecycle check</name>
  <files>(no source edits unless refactor needed)</files>
  <read_first>
    - src/governance/ship-gate-hook.ts (self-review: is the approval-phase derivation clean? Using verifyEvidence.request.phase is consistent; confirm no hardcoded "construction" that would break for inception/operations phases)
    - .gsd/capabilities/aidlc-governance/capability.json (self-review: are the produces/consumes arrays sorted/consistent with existing entries? Match the existing style)
  </read_first>
  <action>
    Run npm test. If green, no-op. If any test regressed, fix the source. Additionally, run a quick end-to-end mental trace: ship gate with no approval → creates pending + throws → human edits approvals/{NN}.json to decision "approved" + decidedBy + decidedAt → ship gate re-run → readApprovalOrFail returns the approved record → assertNoBlockingApprovals passes → ship evidence written. Confirm this lifecycle is covered by the tests (it is — the 6 tests trace this path).

    Commit (only if refactor applied): `refactor(09-04): clean up approval phase derivation`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (full suite green: all existing + all Phase 9 tests across plans 01-04)
    - No regression in any prior-phase test
    - Ship-gate approval lifecycle tests cover: create-pending → block-pending → block-rejected → proceed-approved → proceed-waived
    - Capability manifest is valid JSON (parse check implicit in npm test if any test loads it)
  </acceptance_criteria>
  <done>REFACTOR: full suite green; Phase 9 complete; approval lifecycle verified end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approval store → ship-gate-hook.readApprovalOrFail | Untrusted on-disk approval state; assertApproval (Plan 01) re-validates |
| Model → writePendingApproval | Hook writes ONLY decision "pending" with decidedBy ABSENT (D-07); cannot auto-approve |
| Ship gate → GateEvidence write | Approval check must pass BEFORE ship evidence write (fail-closed ordering) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Spoofing (Elevation of Privilege) | writePendingApproval | high | mitigate | D-07: hook writes only decision "pending" with decidedBy/decidedAt ABSENT. Test asserts decidedBy is undefined. Model cannot auto-approve (Pitfall 5). |
| T-09-04-02 | Tampering | assertNoBlockingApprovals | high | mitigate | D-08: pending/rejected throws; approved/waived proceeds. Mirrors GATE-05 assertNonBlocking. Test covers all 4 decision values. |
| T-09-04-03 | Repudiation | ship evidence isolation | medium | mitigate | Approval state lives in approvals/{NN}.json (separate file); ship GateEvidence record has NO approval fields. Existing assertion stays green. Prevents conflating approval state with gate evidence. |
| T-09-04-04 | Denial of Service | readApprovalOrFail create-then-throw | low | accept | First ship-gate run creates a pending approval and throws (expected — triggers human resolution). Second run after human decision proceeds. Not a DoS — it is the intended blocking flow. |
| T-09-04-05 | Tampering | capability.json manifest edits | low | accept | JSON array edits only; no new hook points or capabilities. Manifest is in git (source of truth). GSD loop host reads it on next session. |
</threat_model>

<verification>
- Ship gate creates pending approval when none exists and throws (D-07)
- Ship gate blocks on pending/rejected, proceeds on approved/waived (D-08)
- Ship evidence record has no approval fields (isolation)
- writePendingApproval omits decidedBy/decidedAt (D-07 anti-auto-approve)
- Capability manifest verify:post consumes test evidence + approvals + requirements/risks sources (D-13)
- Capability manifest ship:pre produces approvals + consumes GOVERNANCE.md (D-16)
- No new capability or hook point (D-13, D-16)
- Full suite (npm test) green across all Phase 9 plans
</verification>

<success_criteria>
- AUDIT-06 enforcement: ship gate blocks on pending/rejected approvals
- APPR-01 ship integration: pending approvals created by the hook; human resolves out-of-band; model never auto-decides
- D-07 anti-auto-approve invariant verified (decidedBy ABSENT on hook-written pending approvals)
- D-08 fail-closed semantics mirror GATE-05 assertNonBlocking
- D-13 verify:post audit hook consumes enriched state (manifest declares it)
- D-16 existing capability extended, no new one created
- Phase 9 fully wired: approval store (01) + test evidence (02) + audit enrichment (03) + ship-gate blocking (04)
</success_criteria>

<output>
Create `.planning/phases/09-complete-audit-record-approval/09-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Symbols (exported/modified):**
- `readApprovalOrFail(projectRoot, phaseNumber): ApprovalRecord` — src/governance/ship-gate-hook.ts (NEW helper)
- `assertNoBlockingApprovals(approval): void` — src/governance/ship-gate-hook.ts (NEW helper)
- `writePendingApproval(projectRoot, phaseNumber): void` — src/governance/ship-gate-hook.ts (NEW helper)
- `shipGateHook(args)` — src/governance/ship-gate-hook.ts (MODIFIED: approval checks inserted before ship evidence write)

**Manifest fields (modified):**
- `.gsd/capabilities/aidlc-governance/capability.json` verify:post audit step `consumes`: +5 entries (tests/{NN}.json, approvals/{NN}.json, REQUIREMENTS.md, VERIFICATION.md, CONTEXT.md)
- `.gsd/capabilities/aidlc-governance/capability.json` ship:pre step `produces`: +1 entry (approvals/{NN}.json); `consumes`: +1 entry (GOVERNANCE.md)

**Test files:**
- `src/governance/ship-gate-hook.test.ts` (MODIFIED: 6 new approval-blocking tests)

**Decisions implemented:** D-07 (pending-only hook writes, decidedBy absent), D-08 (fail-closed on pending/rejected), D-13 (verify:post audit hook consumes enriched state — manifest declaration), D-16 (extend existing capability, no new one). Requirements covered: AUDIT-06 (enforcement), APPR-01 (ship integration).