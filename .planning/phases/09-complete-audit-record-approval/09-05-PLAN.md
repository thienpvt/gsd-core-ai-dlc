---
phase: 09-complete-audit-record-approval
plan: 05
type: tdd
wave: 1
depends_on: ["09-02"]
files_modified:
  - src/governance/capture-test-evidence.ts
  - src/governance/capture-test-evidence.test.ts
  - .claude/skills/aidlc-governance-verify/SKILL.md
autonomous: true
gap_closure: true
requirements: [AUDIT-04]
user_setup: []

must_haves:
  truths:
    - "AUDIT-04 producer side wired: a production verify-step (aidlc-governance-verify SKILL.md) runs `node dist/governance/capture-test-evidence.js {phaseNumber}` BEFORE aidlc-governance-audit writes GOVERNANCE.md (D-03)"
    - "capture-test-evidence.ts spawns `node --test --test-reporter=tap` as a child process, captures real stdout (not model narration), feeds it to parseTapSummary, and persists via existing writeTestEvidence under .planning/governance/tests/{NN}.json (D-02, D-03)"
    - "Malformed runner output (missing # tests N summary line — including model-authored narration that mimics TAP shape) hard-fails through parseTapSummary's D-04 guard BEFORE writeTestEvidence is called; no corrupted record lands on disk"
    - "End-to-end: after capture+persist, the existing WR-01 read-side wiring (readTestEvidence in buildEnrichmentFromPersistedState) populates tests_executed in a real GOVERNANCE.md — closing the single root cause behind both VERIFICATION gaps"
    - "parseTapSummary + writeTestEvidence (currently zero production callers per VERIFICATION §Anti-Patterns BLOCKER) acquire a production caller in capture-test-evidence.ts"
  artifacts:
    - "src/governance/capture-test-evidence.ts (NEW: captureTestEvidence orchestrator + defaultSpawnRunner spawnSync + isDirectRun CLI main)"
    - "src/governance/capture-test-evidence.test.ts (NEW: TDD RED→GREEN tests; injected spawnRunner seam; D-04 narration rejection; end-to-end GOVERNANCE.md tests_executed population)"
    - ".claude/skills/aidlc-governance-verify/SKILL.md (MODIFIED: add step 4 invoking capture-test-evidence.js after verify-gate-hook.js, before the audit skill runs)"
  key_links:
    - "capture-test-evidence.captureTestEvidence → test-evidence.parseTapSummary (production wiring — previously orphaned)"
    - "capture-test-evidence.captureTestEvidence → test-evidence.writeTestEvidence (production wiring — previously orphaned)"
    - "capture-test-evidence.defaultSpawnRunner → child_process.spawnSync (hard-coded argv, no shell — no injection surface)"
    - "aidlc-governance-verify SKILL.md step 4 → node dist/governance/capture-test-evidence.js (producer fires before aidlc-governance-audit consumer)"
    - "Existing WR-01 read-side wiring (audit-artifact.ts:343 readTestEvidence) ← now has a producer file to read"
---

<objective>
Close the single root cause behind both Phase 9 VERIFICATION failures: AUDIT-04 production WRITE path was never wired. `parseTapSummary` and `writeTestEvidence` (test-evidence.ts) have ZERO production callers — only test-evidence.test.ts invokes them. WR-01 wired the READ side (readTestEvidence in buildEnrichmentFromPersistedState); the WRITE side remained dead code. Result: `tests_executed` always ABSENT in production GOVERNANCE.md; AUDIT-04 trust boundary (D-03 narration rejection / D-04 malformed hard-fail) unreachable in production.

This plan adds a production verify-step that captures real `node --test --test-reporter=tap` stdout and persists it as test evidence, so the existing read-side enrichment actually has data to read. Concretely: a thin `capture-test-evidence.ts` entrypoint with a CLI main + injectable spawn seam, and a one-line edit to the `aidlc-governance-verify` SKILL.md invoking it before the audit skill runs.

Purpose: Lift ROADMAP SC-2 / AUDIT-04 (audit records tests executed from real runner output) from FAILED to VERIFIED, and reactivate the D-03/D-04 trust boundary in the production path.
Output: 1 new source module, 1 new test module, 1 SKILL.md edit. Existing 4 plans preserved unchanged. After this plan, GOVERNANCE.md produced by the verify:post flow contains `tests_executed` derived from real runner stdout — no model narration in the loop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-complete-audit-record-approval/09-CONTEXT.md
@.planning/phases/09-complete-audit-record-approval/09-RESEARCH.md
@.planning/phases/09-complete-audit-record-approval/09-VERIFICATION.md
@.planning/phases/09-complete-audit-record-approval/09-REVIEW-FIX.md
@.planning/phases/09-complete-audit-record-approval/09-02-SUMMARY.md
@.planning/phases/09-complete-audit-record-approval/09-03-SUMMARY.md

@src/governance/test-evidence.ts
@src/governance/test-evidence.test.ts
@src/governance/audit-artifact.ts
@src/governance/paths.ts
@.claude/skills/aidlc-governance-verify/SKILL.md
@.claude/skills/aidlc-governance-audit/SKILL.md
</context>

<tasks>

<task type="tdd">
  <name>RED: Write failing capture-test-evidence tests proving producer-side wiring</name>
  <files>src/governance/capture-test-evidence.test.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-VERIFICATION.md (gap reason — WRITE side orphaned; root cause + required fix)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-02 path, D-03 hook captures real output, D-04 malformed hard-fail)
    - .planning/phases/09-complete-audit-record-approval/09-02-SUMMARY.md (parseTapSummary + writeTestEvidence + readTestEvidence signatures; TestEvidenceRecord shape; TAP_OK fixture text)
    - .planning/phases/09-complete-audit-record-approval/09-03-SUMMARY.md (WR-01 read-side wiring — buildEnrichmentFromPersistedState consumes readTestEvidence at audit-artifact.ts:343)
    - src/governance/test-evidence.ts (full — parseTapSummary line 140, writeTestEvidence line 199, TestEvidenceRecord interface line 47, runner const literal)
    - src/governance/test-evidence.test.ts (lines 1-60 — test idiom: `test` from node:test, assert from node:assert/strict, TAP_OK/TAP_OK_FULL fixture strings, withTempRoot pattern using mkdtempSync/rmSync)
    - src/governance/audit-artifact.ts (lines 324-370 buildEnrichmentFromPersistedState — confirms readTestEvidence is the consumer; lines 372-398 runDirect + isDirectRun pattern to mirror for the CLI main)
    - src/governance/audit-artifact.test.ts (lines 500-580 — WR-01 enrichment wiring test idiom: withTempRoot + writeSelection fixture + writeGovernanceAudit result + assert on `result.audit.tests_executed`)
    - .claude/skills/aidlc-governance-verify/SKILL.md (full — current 4-step flow; capture step slots in after step 3 verify-gate-hook, before audit skill runs)
  </read_first>
  <action>
    Create src/governance/capture-test-evidence.test.ts. Import from `./capture-test-evidence.js`: `captureTestEvidence`, `defaultSpawnRunner` (for type only / not invoked in tests). Import from `./test-evidence.js`: `readTestEvidence`, `parseTapSummary` (for direct grep-of-call-site evidence — see final test). Import from `./audit-artifact.js`: `writeGovernanceAudit`. Import from `./paths.js`: `testEvidencePath`, `selectionStatePath`. Import from `./state-store.js`: `writeSelection` (matches audit-artifact.test.ts idiom). Use `node:test` + `node:assert/strict` + the withTempRoot helper cloned from audit-artifact.test.ts (mkdtempSync/rmSync on os.tmpdir).

    The captureTestEvidence signature MUST accept an injectable spawn seam so tests do not actually spawn `node --test` (slow, brittle, environment-dependent):
    `captureTestEvidence({ projectRoot, phaseNumber, spawnRunner }): TestEvidenceRecord`
    where `spawnRunner: () => string` returns captured stdout. Production callers omit spawnRunner (defaults to defaultSpawnRunner); tests inject a fixture-returning fn.

    Tests to author (all MUST fail RED — capture-test-evidence.ts does not exist yet, imports resolve to undefined):
    - "captureTestEvidence parses injected TAP stdout and returns a TestEvidenceRecord with the runner const" — inject spawnRunner returning the TAP_OK fixture (verbatim from test-evidence.test.ts lines 41-47: `TAP version 13\nok 1 - passes\n1..1\n# tests 1\n# pass 1\n# fail 0\n# duration_ms 12.34`). Assert returned record.phase === phaseNumber, record.runner === "node --test --test-reporter=tap", record.summary deepEqual {total:1, pass:1, fail:0, skipped:0, duration_ms:12.34}, record.capturedAt matches the strict ISO 8601 regex from audit-artifact.ts (Date.parse finite + regex).
    - "captureTestEvidence persists via writeTestEvidence to .planning/governance/tests/{NN}.json (D-02)" — inject spawnRunner returning TAP_OK; call captureTestEvidence inside withTempRoot; call readTestEvidence(root, phaseNumber) — assert it returns non-null and deepEqual the captured summary. Assert existsSync(testEvidencePath(root, phaseNumber)) === true.
    - "D-04 malformed runner output hard-fails through parseTapSummary before writeTestEvidence is called" — inject spawnRunner returning a narration prose string (e.g. "All tests passed. Looks good."); call captureTestEvidence; assert.throws with a regex matching the parseTapSummary missing-summary error (concept: missing `# tests N` summary line — do NOT inline the exact error literal in this test's action prose, but the test itself may use a regex like /tests N.*summary/i or /malformed test runner output/i). Critical: assert NO file was written — existsSync(testEvidencePath) === false — proving the guard fires BEFORE persistence (corrupted record never lands on disk).
    - "D-03 narration rejection: TAP-shaped output without the # tests N summary line hard-fails" — inject spawnRunner returning `TAP version 13\nok 1 - passes\n1..1\n` (ok-line + plan-line but NO summary block). assert.throws. Proves the guard is on the summary line, not any TAP prefix.
    - "End-to-end: captureTestEvidence → writeTestEvidence → readTestEvidence → writeGovernanceAudit populates tests_executed in GOVERNANCE.md" — inside withTempRoot: writeSelection(fixtureRecord(), root) (clone fixtureRecord from audit-artifact.test.ts); captureTestEvidence with injected TAP_OK spawnRunner; then call writeGovernanceAudit({ projectRoot: root, outputPath: phaseDir/GOVERNANCE.md }); assert result.audit.tests_executed is truthy AND deepEqual result.audit.tests_executed to the captured summary. This is the single-test proof that the producer-consumer chain is wired end-to-end and lifts VERIFICATION gap #1 (SC-2) directly.
    - "Production-caller grep evidence: parseTapSummary and writeTestEvidence have a non-test caller" — use readFileSync on `src/governance/capture-test-evidence.ts` (the source under test, resolved via path.resolve(__dirname, '..', '..', 'src', 'governance', 'capture-test-evidence.ts')) OR require.resolve the compiled dist sibling; assert the source text includes both `parseTapSummary` and `writeTestEvidence` as call sites (not just imports — assert `.parseTapSummary(` and `writeTestEvidence(` appear). This is the static proof of VERIFICATION §Anti-Patterns BLOCKER being lifted (zero production callers → ≥1 production caller).

    Do NOT implement capture-test-evidence.ts in this task — tests MUST fail RED. Commit: `test(09-05): add failing capture-test-evidence producer-side tests`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test 2>&1 | findstr /R "error TS2307 Cannot find module.*capture-test-evidence" && echo "RED confirmed" || echo "Verify RED state manually"</automated>
  </verify>
  <acceptance_criteria>
    - src/governance/capture-test-evidence.test.ts exists with 6 tests (TAP parse, persist path, D-04 malformed hard-fail pre-persist, D-03 summary-line guard, end-to-end GOVERNANCE.md tests_executed, production-caller grep evidence)
    - Tests import captureTestEvidence from ./capture-test-evidence.js — unimplemented → TS2307 → RED
    - D-04 test asserts NO file is written when parseTapSummary throws (producer-side guard ordering)
    - End-to-end test uses writeGovernanceAudit (the existing WR-01 production path) — proves the chain closes
    - spawnRunner seam is injectable; tests do NOT spawn a real node --test process
  </acceptance_criteria>
  <done>RED confirmed: capture-test-evidence tests fail against unimplemented target; producer-side wiring contract is locked in</done>
</task>

<task type="tdd">
  <name>GREEN: Implement capture-test-evidence.ts + wire aidlc-governance-verify SKILL.md step 4</name>
  <files>src/governance/capture-test-evidence.ts, .claude/skills/aidlc-governance-verify/SKILL.md</files>
  <read_first>
    - src/governance/test-evidence.ts (full — parseTapSummary line 140, writeTestEvidence line 199, TestEvidenceRecord/TestEvidenceSummary types; runner const literal "node --test --test-reporter=tap")
    - src/governance/audit-artifact.ts (lines 372-398 — runDirect + isDirectRun pattern using process.argv[1] === __filename to detect CLI invocation; lines 391-398 try/catch → stderr + exitCode=1 pattern to mirror)
    - src/governance/paths.ts (testEvidencePath signature — confirm it takes projectRoot + phaseNumber and returns the .planning/governance/tests/{NN}.json absolute path)
    - src/governance/test-evidence.test.ts (lines 41-47 TAP_OK fixture — the exact stdout shape defaultSpawnRunner must produce in production)
    - .claude/skills/aidlc-governance-verify/SKILL.md (full — current 4-step flow; the new step 4 slots AFTER step 3 "Invoke the verify hook" and BEFORE the audit skill runs in the capability.json step sequence)
    - package.json (scripts.test = `node --test "dist-test/**/*.test.js"` — this is the exact argv defaultSpawnRunner must spawn, with --test-reporter=tap added)
  </read_first>
  <action>
    Create src/governance/capture-test-evidence.ts. Module shape:

    Exports:
    - `interface CaptureTestEvidenceArgs { projectRoot: string; phaseNumber: string; spawnRunner?: () => string; }`
    - `function captureTestEvidence(args: CaptureTestEvidenceArgs): TestEvidenceRecord` — the orchestrator. Calls `const stdout = (args.spawnRunner ?? defaultSpawnRunner)();` then `const summary = parseTapSummary(stdout);` (production wiring #1 — parseTapSummary gains its first non-test caller). Returns `{ phase: args.phaseNumber, capturedAt: new Date().toISOString(), runner: "node --test --test-reporter=tap", summary }`. Does NOT write — the caller (CLI main or test) chooses whether to persist. This split keeps the function pure-ish and testable.
    - `function defaultSpawnRunner(): string` — production spawn. Uses `child_process.spawnSync(process.execPath, ["--test", "--test-reporter=tap", "dist-test/**/*.test.js"], { cwd: projectRoot, encoding: "utf8", shell: false })`. The `shell: false` + hardcoded argv array closes the injection surface (no shell interpolation). Returns `result.stdout` — even on test failures, TAP summary is on stdout (the `# tests N` / `# pass N` / `# fail N` block is always emitted at the end). If `result.status === null` (signal kill) or stdout is empty, that is a malformed-runner condition — parseTapSummary will throw on the missing summary line (D-04). Do NOT pre-validate stdout shape — let parseTapSummary own the trust boundary (D-03/D-04).
    - CLI main: mirror audit-artifact.ts lines 372-398 exactly. `runDirect(argv)` parses argv = [phaseNumber] (single arg per the user decision); resolves projectRoot = `process.cwd()`; calls `captureTestEvidence({ projectRoot, phaseNumber })` then `writeTestEvidence(projectRoot, phaseNumber, record)` (production wiring #2 — writeTestEvidence gains its first non-test caller); logs the persisted path to stdout for the skill to surface.
    - `isDirectRun()` mirror from audit-artifact.ts: `process.argv[1]` resolved equals `__filename`.
    - Bottom of module: `if (isDirectRun()) { try { runDirect(process.argv.slice(2)); } catch (err) { process.stderr.write("capture-test-evidence: " + (err instanceof Error ? err.message : String(err)) + "\n"); process.exitCode = 1; } }` — fail-loud CLI convention matches the sibling audit-artifact writer.

    Imports: `import { spawnSync } from "node:child_process";`, `import { parseTapSummary, writeTestEvidence, type TestEvidenceRecord } from "./test-evidence.js";`, `import path from "node:path";`. No new deps. CJS module — matches repo convention.

    Modify .claude/skills/aidlc-governance-verify/SKILL.md: insert a new step AFTER the existing step 3 ("Invoke the verify hook") and BEFORE the existing step 4 ("Propagate failures"). Renumber the propagate-failures step to 5. The new step 4 text:

    "4. Capture test evidence (AUDIT-04 producer side). BEFORE the audit skill (aidlc-governance-audit) reads `.planning/governance/tests/{NN}.json`, this step MUST run to write it. Invoke the compiled capture entrypoint:

       ```bash
       node dist/governance/capture-test-evidence.js <phaseNumber>
       ```

       Pass the concrete `{NN}` phase number. The module spawns `node --test --test-reporter=tap` (the actual `npm test` runner), parses the TAP summary via parseTapSummary, and persists a TestEvidenceRecord under `.planning/governance/tests/{NN}.json` via writeTestEvidence. Malformed runner output (including model-authored narration) hard-fails through parseTapSummary's D-04 guard before any record lands on disk. If the runner exits non-zero on this step, surface stderr and fail the verify:post step — evidence loss is blocking."

    Keep the existing "## Failure mode" section unchanged. The SKILL.md edit is prose only — no code, no schema, no new hook point. The capability manifest (.gsd/capabilities/aidlc-governance/capability.json) is NOT edited per 09-REVIEW-FIX deferral (consent-hash constraint); the skill reads files directly.

    Commit: `feat(09-05): wire capture-test-evidence producer into verify:post skill`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test && node --test "dist-test/governance/capture-test-evidence.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:test exits 0
    - node --test dist-test/governance/capture-test-evidence.test.js exits 0 (RED → GREEN; all 6 tests pass)
    - src/governance/capture-test-evidence.ts exports captureTestEvidence + defaultSpawnRunner
    - captureTestEvidence calls parseTapSummary and writeTestEvidence (production wiring — grep `.parseTapSummary(` AND `writeTestEvidence(` in the file returns ≥1 each)
    - defaultSpawnRunner uses spawnSync with shell:false and a hardcoded argv array (no shell injection surface)
    - CLI main runDirect parses argv = [phaseNumber], resolves projectRoot = cwd, calls writeTestEvidence
    - isDirectRun mirrors audit-artifact.ts process.argv[1] === __filename pattern
    - .claude/skills/aidlc-governance-verify/SKILL.md has a new step 4 invoking `node dist/governance/capture-test-evidence.js <phaseNumber>` BEFORE the audit skill runs
    - .gsd/capabilities/aidlc-governance/capability.json UNCHANGED (consent-hash deferral honored — verify via `git diff --stat HEAD -- .gsd/capabilities/` empty)
  </acceptance_criteria>
  <done>GREEN: capture-test-evidence implemented; producer wired into aidlc-governance-verify SKILL.md; parseTapSummary + writeTestEvidence have production callers</done>
</task>

<task type="tdd">
  <name>REFACTOR: Full-suite green + end-to-end CLI smoke + gap-closure verification</name>
  <files>(no source edits unless refactor needed)</files>
  <read_first>
    - src/governance/capture-test-evidence.ts (self-review: is the orchestrator/persist split clean? spawnRunner seam default is correct? No duplicated parser/store logic — reuses test-evidence.ts helpers?)
    - .claude/skills/aidlc-governance-verify/SKILL.md (self-review: step ordering is verify-hook → capture-test-evidence → audit-skill (via capability.json ordering); capture step prose matches the existing fail-loud idiom)
    - .planning/phases/09-complete-audit-record-approval/09-VERIFICATION.md (the two failed truths this plan lifts — confirm both are now demonstrably satisfied by the new producer path)
  </read_first>
  <action>
    Run `npm test` for full-suite green. Confirm zero regressions across plans 01-04 + the new plan 05 tests. Then run the CLI smoke that lifts VERIFICATION gap #1 directly:

    `node dist/governance/capture-test-evidence.js 09` (cwd = repo root, phaseNumber = 09)

    Then verify the produced file: `.planning/governance/tests/09.json` exists, parses as JSON, has `phase: "09"`, `runner: "node --test --test-reporter=tap"`, and a `summary` object with numeric `total`/`pass`/`fail`/`skipped`/`duration_ms` reflecting the real test run. Then run `node dist/governance/audit-artifact.js . .planning/phases/09-complete-audit-record-approval/GOVERNANCE.md` and grep the resulting GOVERNANCE.md for the `tests_executed` key — it MUST now be present (was ABSENT before this plan). This is the end-to-end proof that SC-2 / AUDIT-04 is satisfied in production.

    Clean up the smoke artifacts (rm .planning/governance/tests/09.json and revert/regenerate GOVERNANCE.md if it was modified — the smoke is for verification only, not a committed artifact change) OR leave tests/09.json in place as evidence (planner discretion; the file is gitignored or ephemeral governance state).

    If any refactor is warranted (rare — the module is thin), apply it. Commit (only if refactor applied): `refactor(09-05): clean up capture-test-evidence orchestrator`

    Gap-closure self-check — re-read the two VERIFICATION failed truths and confirm each is now satisfiable:
    - "ROADMAP SC-2 / AUDIT-04: audit records tests executed from real runner output" — SATISFIED: capture-test-evidence.ts spawns the real runner, parseTapSummary extracts the summary, writeTestEvidence persists; the existing WR-01 read-side wiring in buildEnrichmentFromPersistedState now finds a file and populates `tests_executed`.
    - "AUDIT-04 trust boundary (D-03/D-04) enforced in production path" — SATISFIED: parseTapSummary is invoked from capture-test-evidence.ts (production), so its D-04 missing-summary guard and D-03 narration rejection fire on every verify:post run, not only in unit tests.
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm test && node dist/governance/capture-test-evidence.js 09 && node -e "const r=require('./.planning/governance/tests/09.json'); if(!r.summary||typeof r.summary.total!=='number')throw new Error('no summary');console.log('captured:',r.summary.total,'tests')" && node dist/governance/audit-artifact.js . .planning/phases/09-complete-audit-record-approval/GOVERNANCE.md && findstr /C:"tests_executed" .planning/phases/09-complete-audit-record-approval/GOVERNANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (full suite green: all prior 372 tests + 6 new capture-test-evidence tests, 0 fail)
    - node dist/governance/capture-test-evidence.js 09 produces .planning/governance/tests/09.json with a valid TestEvidenceRecord shape (phase, capturedAt, runner const, summary with numeric counts)
    - node dist/governance/audit-artifact.js . .planning/phases/09-complete-audit-record-approval/GOVERNANCE.md writes a GOVERNANCE.md containing the `tests_executed` key (previously ABSENT — the single load-bearing proof SC-2 is lifted)
    - capture-test-evidence.ts has ≥1 production call site each for parseTapSummary and writeTestEvidence (grep on src/governance/capture-test-evidence.ts)
    - No regression in any prior-phase or prior-Phase-9 plan test
    - Both VERIFICATION failed truths (SC-2 producer side; D-03/D-04 production enforcement) now demonstrably satisfiable from the production path
  </acceptance_criteria>
  <done>REFACTOR: full suite green; CLI smoke produces tests/09.json; GOVERNANCE.md contains tests_executed; both VERIFICATION gaps lifted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Child process stdout → parseTapSummary | Untrusted captured output crosses here; parseTapSummary's missing-`# tests N` guard (D-04) is the choke point — anything without the summary line throws before persistence |
| Model → captureTestEvidence | The hook captures real runner output ONLY; the spawnRunner seam in tests is the same shape — model narration has no `# tests N` line and is rejected (D-03) |
| captureTestEvidence → writeTestEvidence → on-disk record | Pre-persist validation via existing validateTestEvidence (Ajv 2020, runner const) + assertTestEvidence (phase match) — unchanged from Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Tampering | defaultSpawnRunner child_process spawnSync | medium | mitigate | Hardcoded argv array `["--test", "--test-reporter=tap", "dist-test/**/*.test.js"]` + `shell: false` — no shell interpolation surface. process.execPath used (not bare "node") so a PATH-hijacked node binary cannot substitute. |
| T-09-05-02 | Tampering | parseTapSummary trust boundary (D-03/D-04) | high | mitigate | The missing-`# tests N` summary guard fires BEFORE writeTestEvidence — corrupted or narration-shaped stdout cannot land on disk. This is the AUDIT-04 trust boundary finally enforced in the production path (previously unreachable per VERIFICATION gap #2). |
| T-09-05-03 | Repudiation (audit integrity) | tests/{NN}.json persistence | medium | mitigate | TestEvidenceRecord carries `capturedAt` (strict ISO 8601), `runner` const, and the captured summary; written via atomicWriteFile (TD-03). Audit trail is reproducible and timestamped. |
| T-09-05-04 | Denial of Service | captureTestEvidence spawns node --test (slow — ~16s on this repo) | low | accept | One additional full test run per verify:post. Acceptable: verify is already a multi-second phase; the captured evidence is load-bearing for AUDIT-04. The spawnRunner seam lets future callers substitute a faster reporter if needed. |
| T-09-05-05 | Information Disclosure | spawnSync stderr capture | low | accept | defaultSpawnRunner returns stdout only; stderr from failing tests is not persisted (avoided leaking test internals into governance state). On malformed stdout, parseTapSummary throws a generic missing-summary message — no test content leaks into the error. |
</threat_model>

<verification>
- capture-test-evidence.ts exports captureTestEvidence + defaultSpawnRunner (NEW module)
- captureTestEvidence calls parseTapSummary and writeTestEvidence (production wiring — both helpers gain their first non-test caller, lifting VERIFICATION §Anti-Patterns BLOCKER)
- defaultSpawnRunner uses spawnSync with shell:false + hardcoded argv (no injection surface)
- CLI main `node dist/governance/capture-test-evidence.js 09` produces .planning/governance/tests/09.json (real runner output captured)
- aidlc-governance-verify SKILL.md step 4 invokes capture-test-evidence.js before the audit skill reads tests/{NN}.json (producer/consumer ordering)
- GOVERNANCE.md produced by the verify:post flow now contains `tests_executed` (was ABSENT before this plan)
- Capability manifest UNCHANGED (consent-hash deferral from 09-REVIEW-FIX honored)
- D-03/D-04 trust boundary enforced in production path (parseTapSummary invoked from non-test code)
- Existing 4 Phase 9 plans preserved unchanged (git diff shows only the 3 files in files_modified)
- Full suite (npm test) green
</verification>

<success_criteria>
- AUDIT-04 producer side wired: parseTapSummary + writeTestEvidence have production callers in capture-test-evidence.ts
- ROADMAP SC-2 / AUDIT-04 lifted from FAILED to VERIFIED — GOVERNANCE.md `tests_executed` derived from real `node --test` TAP output
- D-03 narration rejection enforced in production (parseTapSummary guard fires on every verify:post run)
- D-04 malformed-output hard-fail enforced in production (missing-summary guard throws BEFORE persistence)
- Both VERIFICATION failed truths now satisfiable from the production path (single root cause closed)
- aidlc-governance-verify SKILL.md extended with capture step; capability manifest unchanged per deferral
- No new runtime deps; CJS; tsc-only; node:test — stack constraints honored
</success_criteria>

<output>
Create `.planning/phases/09-complete-audit-record-approval/09-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Symbols (new):**
- `captureTestEvidence(args: CaptureTestEvidenceArgs): TestEvidenceRecord` — src/governance/capture-test-evidence.ts (NEW — the orchestrator: spawn → parseTapSummary → return record; injectable spawnRunner seam for tests)
- `defaultSpawnRunner(): string` — src/governance/capture-test-evidence.ts (NEW — production spawn: `child_process.spawnSync` with hardcoded argv, `shell: false`)
- `interface CaptureTestEvidenceArgs { projectRoot, phaseNumber, spawnRunner? }` — src/governance/capture-test-evidence.ts (NEW)
- `runDirect(argv)` + `isDirectRun()` — src/governance/capture-test-evidence.ts (NEW — CLI main mirroring audit-artifact.ts convention)

**Symbols (gain production callers — previously orphaned):**
- `parseTapSummary` (test-evidence.ts:140) — now invoked from capture-test-evidence.ts
- `writeTestEvidence` (test-evidence.ts:199) — now invoked from capture-test-evidence.ts

**Skill edits:**
- `.claude/skills/aidlc-governance-verify/SKILL.md` — new step 4 invoking `node dist/governance/capture-test-evidence.js <phaseNumber>` after the verify hook, before the audit skill runs (producer/consumer ordering)

**Test files:**
- `src/governance/capture-test-evidence.test.ts` (NEW — 6 tests: TAP parse, persist path, D-04 malformed pre-persist guard, D-03 summary-line guard, end-to-end GOVERNANCE.md tests_executed population, production-caller grep evidence)

**CLI flag / invocation:**
- `node dist/governance/capture-test-evidence.js <phaseNumber>` — single positional arg (phaseNumber); projectRoot resolved from cwd; matches user decision in planning_context

**Manifest:**
- `.gsd/capabilities/aidlc-governance/capability.json` UNCHANGED (per 09-REVIEW-FIX consent-hash deferral; skill reads files directly — existing pattern)

**Decisions implemented:** D-01 (parse real runner output — node --test TAP, already reconciled in 09-02), D-02 (persist under tests/{NN}.json via existing store), D-03 (hook captures real output, narration rejected — production path), D-04 (malformed hard-fail — production path). Requirements covered: AUDIT-04 (the only failed requirement; the other 4 already pass).
