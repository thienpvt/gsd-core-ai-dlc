---
phase: 09-complete-audit-record-approval
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/schema/test-evidence.schema.json
  - src/governance/test-evidence.ts
  - src/governance/test-evidence.test.ts
autonomous: true
requirements: [AUDIT-04]
user_setup: []

must_haves:
  truths:
    - "TAP summary parser (parseTapSummary) extracts total/pass/fail/skipped/duration_ms from node --test --test-reporter=tap stdout (D-01 reconciled — TAP is the actual runner; no run-tests.cjs exists locally)"
    - "parseTapSummary hard-fails on output missing the '# tests N' summary line (D-04 malformed = hard fail, never warn-and-continue)"
    - "parseTapSummary hard-fails on model-authored narration (D-03 — 'All tests passed.' is not TAP)"
    - "test-evidence schema (draft 2020-12, x-binding: binding) validates the parsed-record shape with runner const = 'node --test --test-reporter=tap'"
    - "writeTestEvidence round-trips TestEvidenceRecord via atomicWriteFile; readTestEvidence returns null when absent (gate-evidence-store clone)"
    - "readTestEvidence re-validates parsed JSON via assertTestEvidence (loud-fail on tampered disk state)"
  artifacts:
    - "src/schema/test-evidence.schema.json"
    - "src/governance/test-evidence.ts"
    - "src/governance/test-evidence.test.ts"
  key_links:
    - "test-evidence.writeTestEvidence → atomicWriteFile (TD-03 race fix inherited)"
    - "test-evidence.assertTestEvidence → validateTestEvidence (ENF-02 boundary — malformed-parsed-output hard fail)"
    - "test-evidence.parseTapSummary → TestEvidenceRecord.summary (pure transform, no I/O — testable in isolation)"
    - "test-evidence.testEvidencePath → paths.ts (consumed from Plan 01, no paths.ts modification here)"
---

<objective>
Implement AUDIT-04 test-evidence capture: a pure TAP-summary parser for `node --test --test-reporter=tap` stdout (the actual `npm test` runner — D-01 reconciliation confirmed no `run-tests.cjs` exists locally), a draft 2020-12 schema for the parsed record, and a durable store persisting under `.planning/governance/tests/{NN}.json` (D-02). Malformed runner output hard-fails (D-04); model-authored narration is rejected (D-03).

Purpose: AUDIT-04 requires test results derived from real runner output, not model narration. The TAP summary block (`# tests N`, `# pass N`, `# fail N`) is the parse target; absence of the `# tests N` line is the malformed-input guard — this is the AUDIT-04 trust boundary. The store mirrors gate-evidence-store so the verify:post hook (Plan 03/04) can read persisted evidence across context compaction.
Output: 3 new files. Plan 04's verify:post audit hook will consume readTestEvidence to fold `tests_executed` into the v2 audit artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-complete-audit-record-approval/09-CONTEXT.md
@.planning/phases/09-complete-audit-record-approval/09-RESEARCH.md
@.planning/phases/09-complete-audit-record-approval/09-PATTERNS.md
@.planning/phases/09-complete-audit-record-approval/09-VALIDATION.md

@src/governance/gate-evidence-store.ts
@src/enforcement/validate-gate-result.ts
@src/schema/gate-result.schema.json
@src/governance/paths.ts
</context>

<feature>
  <name>Test-evidence capture (AUDIT-04)</name>
  <files>src/schema/test-evidence.schema.json, src/governance/test-evidence.ts, src/governance/test-evidence.test.ts</files>
  <behavior>
    - parseTapSummary(stdout: string): TestEvidenceSummary — pure function, no I/O. Regex `/^# (tests|pass|fail|skipped|todo|cancelled|duration_ms) (\d+(?:\.\d+)?)/gm` extracts the summary block. Returns {total, pass, fail, skipped, duration_ms}.
    - parseTapSummary throws `malformed test runner output: missing \`# tests N\` summary line` when the `# tests` count is absent or non-finite (D-04). This is the narration-rejection guard — model-authored prose ("All tests passed.") has no `# tests N` line.
    - TestEvidenceRecord: { phase, capturedAt, runner: "node --test --test-reporter=tap", summary }. The runner const is the AUDIT-04 trust boundary — anything else fails closed.
    - writeTestEvidence(projectRoot, phaseNumber, record): asserts → atomicWriteFile → no temp siblings.
    - readTestEvidence(projectRoot, phaseNumber): null when missing; throws `malformed test evidence at ${absPath}: ${detail}` on bad JSON / shape / timestamp.
    - testEvidencePath is consumed from paths.ts (Plan 01 owns it; Plan 02 does not modify paths.ts).
  </behavior>
  <implementation>
    Store half clones gate-evidence-store.ts 1:1: swap GateEvidence→TestEvidenceRecord, assertEvidence→assertTestEvidence, gateEvidencePath→testEvidencePath (imported from paths.ts). Replace validateGateResult call with a new Ajv 2020 validator for the test-evidence schema (inline in test-evidence.ts OR a sibling validate-test-evidence.ts — planner discretion per CONTEXT Claude's Discretion; inline is acceptable since this is the only consumer and the pattern is identical to validate-approval.ts from Plan 01). Parser half is net-new (no codebase analog — 09-PATTERNS.md "No Analog Found" line 700): pure function, regex over the summary block, hard-fail on missing `# tests N`. Schema clones gate-result.schema.json header convention (x-binding: binding because parsed state blocks ship via the audit fold).
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>RED: Write failing test-evidence tests (parser + store)</name>
  <files>src/schema/test-evidence.schema.json, src/governance/test-evidence.test.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (test-evidence.ts section lines 74-112 — parser regex + store clone + malformed-rejection idiom; test-evidence.test.ts section lines 504-538 — TAP_OK fixture + parser test cases)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-01 reconciliation note, D-02 durable state, D-03 narration rejected, D-04 malformed hard-fail)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Code Examples "TAP Output Shape" lines 382-408 — verified local capture; Common Pitfalls #4 — node --test has no json reporter)
    - src/governance/gate-evidence-store.test.ts (full — withTempRoot + fixture builder + malformed-JSON ladder to port for the store half)
    - src/governance/audit-artifact.test.ts (lines 291-298 — determinism test idiom `parse → assert.deepEqual(first, second)` to port for parseTapSummary)
  </read_first>
  <action>
    Author src/schema/test-evidence.schema.json per 09-PATTERNS.md lines 349-378: draft 2020-12 header, $id "https://gsd.dev/schemas/test-evidence.schema.json", title "Test Runner Evidence", description cites AUDIT-04 + D-03/D-04, additionalProperties:false, required ["phase","capturedAt","runner","summary"], runner const "node --test --test-reporter=tap", summary sub-object with total/pass/fail/skipped/duration_ms (integer minimum 0, duration_ms number minimum 0), x-binding:"binding". Strict ISO 8601 pattern on capturedAt (TD-01 shape).

    Write src/governance/test-evidence.test.ts porting gate-evidence-store.test.ts for the store half (round-trip, malformed-JSON loud-fail, missing-required-field loud-fail, metadata-phase-mismatch) and adding the parser tests from 09-PATTERNS.md lines 511-538:
    - parseTapSummary extracts counts from a real TAP fixture (TAP_OK with `# tests 1`, `# pass 1`, `# fail 0`, `# duration_ms 12.34`)
    - parseTapSummary hard-fails on output missing the `# tests N` summary line (D-04) — regex `/malformed test runner output: missing .*# tests N.* summary line/i`
    - parseTapSummary hard-fails on model-authored narration ("All tests passed.") — same regex family
    - parseTapSummary is deterministic: two calls on the same input return deepEqual results (port audit-artifact.test.ts determinism idiom)

    Add a D-03-specific test: a fixture that looks like TAP but has no summary block (e.g., only `ok 1 - passes` without `1..N` and `# tests N`) must hard-fail — proves the guard is on the summary line, not just any TAP-ish prefix.

    Implementations NOT written in this task — tests MUST fail in RED state (imports to test-evidence.ts / TestEvidenceRecord / parseTapSummary / writeTestEvidence / readTestEvidence unresolvable).

    Commit: `test(09-02): add failing test-evidence parser + store tests`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test 2>&1 | findstr /R "error TS2307 Cannot find module" && echo "RED confirmed" || echo "Verify tests actually fail against unimplemented targets"</automated>
  </verify>
  <acceptance_criteria>
    - src/schema/test-evidence.schema.json exists with "x-binding": "binding", "runner" const, summary sub-object, 4 required top-level fields
    - src/governance/test-evidence.test.ts exists with parser tests (TAP_OK fixture, malformed-rejection, narration-rejection, determinism) AND store tests (round-trip, malformed-JSON, missing-field)
    - Tests reference parseTapSummary, writeTestEvidence, readTestEvidence, TestEvidenceRecord — unimplemented → RED
    - D-04 test present (missing `# tests N` summary line throws)
    - D-03 test present (model narration throws)
  </acceptance_criteria>
  <done>RED confirmed: build:test fails on unimplemented targets; tests cover parse + store + D-03/D-04 guards</done>
</task>

<task type="tdd">
  <name>GREEN: Implement test-evidence schema + parser + store</name>
  <files>src/governance/test-evidence.ts</files>
  <read_first>
    - .planning/phases/09-complete-audit-record-approval/09-PATTERNS.md (test-evidence.ts section lines 74-112 — exact parser regex + store clone code excerpts; No Analog Found line 700 confirming parser is net-new)
    - .planning/phases/09-complete-audit-record-approval/09-RESEARCH.md (Code Examples "TAP Output Shape" lines 382-408 — verified TAP summary block on Node v24.14.0; Common Pitfalls #4 — no json reporter; Don't Hand-Roll line 292 — TAP summary parser, no new reporter file)
    - .planning/phases/09-complete-audit-record-approval/09-CONTEXT.md (D-01 reconciliation, D-02 .planning/governance/tests/{NN}.json, D-03 narration rejected, D-04 malformed hard-fail)
    - src/governance/gate-evidence-store.ts (full — writeGateEvidence/readGateEvidence/assertEvidence/fail ladder to clone for the store half)
    - src/enforcement/validate-gate-result.ts (full — Ajv 2020 + addFormats + addKeyword x-binding pre-compile; inline the same pattern for the test-evidence schema OR import a sibling validator)
    - src/governance/paths.ts (testEvidencePath exported by Plan 01 — consume via import, do NOT modify paths.ts)
    - src/governance/atomic-write.ts (atomicWriteFile contract — TD-03 race fix)
  </read_first>
  <action>
    Implement src/governance/test-evidence.ts per 09-PATTERNS.md lines 74-112.

    Parser half (net-new, pure function): export parseTapSummary(stdout: string): TestEvidenceSummary. Use the regex `/^# (tests|pass|fail|skipped|todo|cancelled|duration_ms) (\d+(?:\.\d+)?)/gm` to extract the summary block. Build a counts record. If counts.tests is undefined or not finite, throw `malformed test runner output: missing \`# tests N\` summary line` (D-04). Return {total: counts.tests, pass: counts.pass ?? 0, fail: counts.fail ?? 0, skipped: counts.skipped ?? 0, duration_ms: counts.duration_ms ?? 0}. No I/O — pure function testable in isolation.

    Store half: export TestEvidenceRecord interface ({phase: string, capturedAt: string, runner: "node --test --test-reporter=tap", summary: TestEvidenceSummary}). Export writeTestEvidence(projectRoot, phaseNumber, record) and readTestEvidence(projectRoot, phaseNumber): TestEvidenceRecord | null. Clone gate-evidence-store.ts 1:1: swap types, use testEvidencePath from paths.ts, call a local Ajv 2020 validator (inline validateTestEvidence mirroring validate-approval.ts from Plan 01 — Ajv 2020 strict + addFormats + addKeyword x-binding pre-compile + compile-once; OR import a sibling validate-test-evidence.ts if you prefer symmetry — planner discretion, inline is simpler for one consumer). Keep the 4-rung read ladder verbatim. fail(filePath, detail) message: `malformed test evidence at ${filePath}: ${detail}`.

    assertTestEvidence post-Ajv invariants: phase matches the path phase (mirror gate-evidence-store's metadata-phase check); runner is the const string; summary counts are non-negative integers (Ajv schema enforces, but assert for defense-in-depth).

    Commit: `feat(09-02): implement test-evidence parser + store`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm run build:test && node --test "dist-test/governance/test-evidence.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build:test exits 0
    - node --test dist-test/governance/test-evidence.test.js exits 0 (RED tests now GREEN)
    - src/governance/test-evidence.ts exports parseTapSummary, writeTestEvidence, readTestEvidence, TestEvidenceRecord, TestEvidenceSummary
    - parseTapSummary on the TAP_OK fixture returns {total:1, pass:1, fail:0, skipped:0, duration_ms:12.34}
    - parseTapSummary on "All tests passed." throws with "malformed test runner output" in the message (D-03)
    - parseTapSummary on TAP without summary block throws (D-04)
    - writeTestEvidence → readTestEvidence round-trips byte-identical content
    - readTestEvidence returns null on missing file
    - Malformed-JSON test throws with "malformed test evidence at" prefix
    - paths.ts NOT modified in this plan (testEvidencePath consumed from Plan 01)
  </acceptance_criteria>
  <done>GREEN: parser + store implemented; all test-evidence tests pass; D-03/D-04 guards verified</done>
</task>

<task type="tdd">
  <name>REFACTOR: Full-suite green + captured-fixture self-check</name>
  <files>(no source edits unless refactor needed)</files>
  <read_first>
    - src/governance/test-evidence.ts (self-review: is the inline validator worth extracting to a sibling? Per the one-consumer rule, inline is fine — do NOT extract unless a second consumer appears)
    - src/governance/gate-evidence-store.ts (confirm no accidental drift introduced by the clone)
  </read_first>
  <action>
    Run npm test. If green, no-op commit. If any test outside test-evidence files regressed, fix the source (likely an import-path collision or a wildcard test catching the new exports — narrow the wildcard). Do NOT extract the inline validator to a sibling file unless a second consumer appears in Plan 03/04 (Plan 03 consumes readTestEvidence, not validateTestEvidence, so no second consumer).

    Commit (only if refactor applied): `refactor(09-02): clean up test-evidence store`
  </action>
  <verify>
    <automated>cd "C:/Users/thien/IdeaProjects/gsd-core-ai-dlc" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (full suite green: existing + new test-evidence tests)
    - No regression in any prior-phase test
    - testEvidencePath import resolves from Plan 01's paths.ts export
  </acceptance_criteria>
  <done>REFACTOR: full suite green; Plan 02 complete; parser is pure and store clones gate-evidence-store</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdout → parseTapSummary | Untrusted runner output crosses here; missing `# tests N` summary line = malformed = hard fail (D-04) |
| Disk → readTestEvidence | Tampered/malformed JSON on disk; assertTestEvidence re-validates after parse |
| Model → writeTestEvidence | Caller may pass narration-derived summary; runner const in schema rejects non-TAP sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Tampering | parseTapSummary | high | mitigate | D-04 guard: missing `# tests N` summary line throws. Test asserts model narration ("All tests passed.") is rejected. |
| T-09-02-02 | Tampering (data integrity) | test-evidence schema runner const | high | mitigate | Schema const `runner: "node --test --test-reporter=tap"` — any other runner source fails Ajv validation. Trust boundary is explicit in the persisted record. |
| T-09-02-03 | Repudiation | test-evidence records | low | accept | Strict ISO 8601 capturedAt (TD-01 shape); durable under .planning/governance/tests/{NN}.json. Same repudiation surface as existing gate evidence. |
| T-09-02-04 | Information Disclosure | parseTapSummary error messages | low | accept | Error messages reference the missing summary line, not file contents; no secrets in TAP output by design. |
</threat_model>

<verification>
- parseTapSummary extracts counts from a real captured TAP fixture (verified shape in 09-RESEARCH.md lines 382-408)
- Missing `# tests N` summary line hard-fails (D-04)
- Model-authored narration hard-fails (D-03)
- parseTapSummary is deterministic (deepEqual on repeated calls)
- writeTestEvidence → readTestEvidence round-trips byte-identical
- readTestEvidence returns null on missing file
- Full suite (npm test) green
</verification>

<success_criteria>
- AUDIT-04 test-evidence capture implemented: pure TAP parser + durable store + draft 2020-12 schema
- D-01 reconciled: parser targets `node --test --test-reporter=tap` (the actual `npm test` runner); no `run-tests.cjs` dependency
- D-02 durable state under .planning/governance/tests/{NN}.json
- D-03 narration rejected; D-04 malformed hard-fails
- Plan 03 can consume readTestEvidence to fold `tests_executed` into the v2 audit artifact
</success_criteria>

<output>
Create `.planning/phases/09-complete-audit-record-approval/09-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Symbols (exported):**
- `TestEvidenceSummary` (interface) — {total, pass, fail, skipped, duration_ms}
- `TestEvidenceRecord` (interface) — {phase, capturedAt, runner, summary}
- `parseTapSummary(stdout: string): TestEvidenceSummary` — pure function, no I/O
- `writeTestEvidence(projectRoot, phaseNumber, record): void`
- `readTestEvidence(projectRoot, phaseNumber): TestEvidenceRecord | null`

**Schemas:**
- `src/schema/test-evidence.schema.json` (draft 2020-12, x-binding: binding, runner const)

**Test files:**
- `src/governance/test-evidence.test.ts`

**Decisions implemented:** D-01 (TAP reconciliation — no run-tests.cjs), D-02 (durable state path), D-03 (narration rejected via summary-line guard), D-04 (malformed hard-fail), D-15 (new test-evidence schema), partial D-14 (test-evidence module).