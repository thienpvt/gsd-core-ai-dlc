---
phase: 16-starter-examples-outside-index
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/select/starter-examples.test.ts
  - examples/java-spring/README.md
  - examples/java-spring/domain/Order.java
  - examples/java-spring/domain/PlaceOrderCommand.java
  - examples/java-spring/application/port/PlaceOrderPort.java
  - examples/java-spring/application/port/OrderRepositoryPort.java
  - examples/java-spring/application/PlaceOrderHandler.java
  - examples/java-spring/adapter/in/web/PlaceOrderController.java
  - examples/java-spring/adapter/in/messaging/PlaceOrderKafkaListener.java
  - examples/java-spring/adapter/out/persistence/OrderRepositoryAdapter.java
  - package.json
autonomous: true
requirements:
  - JAVA-EX-01
  - JAVA-EX-02
must_haves:
  truths:
    - "examples/java-spring/ ships at repo root as sibling of aidlc-rules/ with hexagonal Order layout"
    - "Thin compile-shaped Java snippets cover domain model/command, input port, output port, handler, REST adapter, Kafka adapter, persistence adapter under com.example.orders"
    - "Domain and application port contracts are plain Java; Spring/framework annotations appear only in adapter classes"
    - "README.md is plain Markdown with no YAML frontmatter and states non-selectability + mirrored rule ids"
    - "buildIndex(aidlc-rules) emits no sourceFile under examples/ and no example/sample rule ids"
    - "buildIndex(examples/java-spring) throws D-10 scope error (backstop)"
    - "Real-corpus inventory stays exactly 10 winners; Phase 16 adds zero selectable rules"
    - "package.json files includes examples so npm consumers receive the starter tree"
    - "No production engine edits under src/rules, src/index, src/select (except new test), src/inject, src/cli, src/schema"
  artifacts:
    - path: src/select/starter-examples.test.ts
      provides: "TDD suite locking JAVA-EX-01 layout + JAVA-EX-02 non-indexing/D-10/inventory"
      contains: "JAVA-EX-01|JAVA-EX-02|examples/java-spring|D-10"
    - path: examples/java-spring/README.md
      provides: "Purpose, non-runnable ceiling, layout, mirror guidance, governing rule IDs"
    - path: examples/java-spring/domain/Order.java
      provides: "Plain domain aggregate root (Order)"
    - path: examples/java-spring/domain/PlaceOrderCommand.java
      provides: "Plain domain command type"
    - path: examples/java-spring/application/port/PlaceOrderPort.java
      provides: "Input port interface (plain Java)"
    - path: examples/java-spring/application/port/OrderRepositoryPort.java
      provides: "Output port interface (plain Java)"
    - path: examples/java-spring/application/PlaceOrderHandler.java
      provides: "Application handler implementing input port via output port"
    - path: examples/java-spring/adapter/in/web/PlaceOrderController.java
      provides: "Thin REST inbound adapter"
    - path: examples/java-spring/adapter/in/messaging/PlaceOrderKafkaListener.java
      provides: "Kafka inbound adapter with idempotency/retry/DLQ markers"
    - path: examples/java-spring/adapter/out/persistence/OrderRepositoryAdapter.java
      provides: "Persistence outbound adapter implementing output port"
    - path: package.json
      provides: "files includes examples for npm ship"
  key_links:
    - from: src/select/starter-examples.test.ts
      to: aidlc-rules
      via: "buildIndex(REAL_RULES_ROOT) non-indexing + inventory=10"
      pattern: "buildIndex\\(|examples/|D-10"
    - from: src/select/starter-examples.test.ts
      to: examples/java-spring
      via: "layout existence + buildIndex(EXAMPLES_ROOT) D-10 throw"
      pattern: "EXAMPLES_ROOT|PlaceOrderPort|OrderRepositoryPort"
    - from: package.json
      to: examples/
      via: "files array includes examples"
      pattern: "\"examples\""
---

<objective>
Ship thin non-selectable Java/Spring starter examples under `examples/java-spring/` (hexagonal Order slice under `com.example.orders`) and lock isolation with a sibling TDD suite so `build-index` never treats them as rules (JAVA-EX-01, JAVA-EX-02).

Purpose: LLM can mirror ports/handlers/REST/Kafka/persistence layout without growing the selectable corpus; inventory stays 10; engine frozen.
Output: `src/select/starter-examples.test.ts`, `examples/java-spring/**`, `package.json` files+=examples; suite + full `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-starter-examples-outside-index/16-CONTEXT.md
@.planning/phases/16-starter-examples-outside-index/16-RESEARCH.md
@.planning/phases/16-starter-examples-outside-index/16-PATTERNS.md
@.planning/phases/16-starter-examples-outside-index/16-VALIDATION.md
@src/select/java-spring-hex-ddd.test.ts
@src/index/precedence.test.ts
@src/index/build.ts
@src/rules/scope.ts
@src/rules/load.ts
@package.json
@aidlc-rules/domain/java-spring/java-spring-hex-layering.md
@aidlc-rules/domain/java-spring/java-spring-inbound-rest.md
@aidlc-rules/domain/java-spring/java-spring-inbound-kafka.md
</context>

## Artifacts this phase produces

| Path | Kind | Symbols / notes |
|------|------|-----------------|
| `src/select/starter-examples.test.ts` | test | node:test suite; cases JAVA-EX-01 layout, JAVA-EX-02 non-index, D-10 backstop, inventory=10, README no-frontmatter, no-engine-edit smoke via buildIndex only |
| `examples/java-spring/README.md` | docs | plain Markdown; no `---` frontmatter; purpose + non-runnable ceiling + layout + mirror guidance + rule ids |
| `examples/java-spring/domain/Order.java` | Java | `com.example.orders.domain.Order` |
| `examples/java-spring/domain/PlaceOrderCommand.java` | Java | `com.example.orders.domain.PlaceOrderCommand` |
| `examples/java-spring/application/port/PlaceOrderPort.java` | Java | `com.example.orders.application.port.PlaceOrderPort` (input) |
| `examples/java-spring/application/port/OrderRepositoryPort.java` | Java | `com.example.orders.application.port.OrderRepositoryPort` (output) |
| `examples/java-spring/application/PlaceOrderHandler.java` | Java | `com.example.orders.application.PlaceOrderHandler` |
| `examples/java-spring/adapter/in/web/PlaceOrderController.java` | Java | `com.example.orders.adapter.in.web.PlaceOrderController` |
| `examples/java-spring/adapter/in/messaging/PlaceOrderKafkaListener.java` | Java | `com.example.orders.adapter.in.messaging.PlaceOrderKafkaListener` |
| `examples/java-spring/adapter/out/persistence/OrderRepositoryAdapter.java` | Java | `com.example.orders.adapter.out.persistence.OrderRepositoryAdapter` |
| `package.json` | config | `files` gains `"examples"` only |

**Do not produce:** pom.xml, build.gradle, application.yml, new aidlc-rules files, engine patches, new npm deps, edits to `src/index/precedence.test.ts` or existing pack suites.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED starter-examples suite — layout, non-index, D-10, inventory</name>
  <files>src/select/starter-examples.test.ts</files>
  <read_first>
    - src/select/java-spring-hex-ddd.test.ts (sibling suite shape: node:test, buildIndex, path.resolve)
    - src/index/precedence.test.ts (10-winner expectedIds lock — assert same count; do not edit this file)
    - src/rules/scope.ts (D-10 message: outside enterprise/domain/project tiers)
    - src/index/build.ts (buildIndex public API)
    - src/rules/load.ts (only .md indexed; details/ skipped)
    - package.json (current files array)
    - .planning/phases/16-starter-examples-outside-index/16-CONTEXT.md (locked Order/com.example.orders decisions D-01..D-10 class)
  </read_first>
  <behavior>
    - JAVA-EX-01: EXAMPLES_ROOT = path.resolve(cwd, "examples", "java-spring") exists; is NOT nested under REAL_RULES_ROOT = path.resolve(cwd, "aidlc-rules")
    - JAVA-EX-01: all locked relative paths exist under EXAMPLES_ROOT:
      README.md;
      domain/Order.java;
      domain/PlaceOrderCommand.java;
      application/port/PlaceOrderPort.java;
      application/port/OrderRepositoryPort.java;
      application/PlaceOrderHandler.java;
      adapter/in/web/PlaceOrderController.java;
      adapter/in/messaging/PlaceOrderKafkaListener.java;
      adapter/out/persistence/OrderRepositoryAdapter.java
    - JAVA-EX-01: each .java file package line contains com.example.orders; domain + application/port files contain no Spring/JPA annotation tokens (@RestController, @KafkaListener, @Service, @Repository, @Entity, @Autowired)
    - JAVA-EX-01: adapter .java files may use framework annotations; REST adapter references PlaceOrderPort; Kafka adapter mentions idempotency and retry or DLQ in comments/body; persistence adapter implements OrderRepositoryPort
    - JAVA-EX-01: at least one `ponytail:` comment exists under examples/java-spring (ceiling/upgrade marker)
    - JAVA-EX-02: buildIndex(REAL_RULES_ROOT) — every sourceFile uses forward-slash check with no "examples/" substring; no rule id starts with "example" or "sample"
    - JAVA-EX-02 backstop: assert.throws(() => buildIndex(EXAMPLES_ROOT), /outside the enterprise\/domain\/project tiers|D-10/)
    - Inventory regression: buildIndex(REAL_RULES_ROOT).rules.length === 10 (do not edit precedence.test.ts)
    - README: read examples/java-spring/README.md; assert does not match rule-shaped frontmatter (no leading --- YAML block containing id:); asserts non-selectability language (examples outside aidlc-rules / not selectable) and cites at least java-spring-hex-layering, java-spring-inbound-rest, java-spring-inbound-kafka by id string
    - package.json ship: JSON.parse package.json files array includes "examples"
  </behavior>
  <action>
    Create sibling suite only: `src/select/starter-examples.test.ts`. Do not edit production engine files, precedence.test.ts, or existing java-spring-*.test.ts inventories.

    Mirror harness from java-spring-hex-ddd.test.ts: import test from node:test, assert from node:assert/strict, path, fs (existsSync, readFileSync), buildIndex from ../index/build.js.

    Constants: REAL_RULES_ROOT, EXAMPLES_ROOT, EXPECTED_SNIPPETS array of the nine relative paths above, INVENTORY_COUNT = 10.

    Write all behavior assertions as real tests. Expected RED causes until Task 2: missing examples tree, missing package.json files entry. Non-index + inventory + D-10 backstop may already pass as regression locks — keep them.

    Per D-10 CONTEXT non-index guarantee: prove buildIndex(aidlc-rules) never indexes examples; prove D-10 if examples root scanned directly. Per inventory: stays 10 winners (require-mfa + nine java-spring).

    No select/renderInjection needed — this phase is layout isolation, not selection positives.
  </action>
  <verify>
    <automated>npm run build:test && node --test dist-test/select/starter-examples.test.js</automated>
  </verify>
  <done>Suite exists, compiles, and fails only on missing examples layout and/or missing package.json files entry; non-index and inventory guards assert against real aidlc-rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN examples tree + package.json files ship</name>
  <files>examples/java-spring/README.md, examples/java-spring/domain/Order.java, examples/java-spring/domain/PlaceOrderCommand.java, examples/java-spring/application/port/PlaceOrderPort.java, examples/java-spring/application/port/OrderRepositoryPort.java, examples/java-spring/application/PlaceOrderHandler.java, examples/java-spring/adapter/in/web/PlaceOrderController.java, examples/java-spring/adapter/in/messaging/PlaceOrderKafkaListener.java, examples/java-spring/adapter/out/persistence/OrderRepositoryAdapter.java, package.json, src/select/starter-examples.test.ts</files>
  <read_first>
    - src/select/starter-examples.test.ts (RED expectations — make green)
    - .planning/phases/16-starter-examples-outside-index/16-CONTEXT.md (locked: com.example.orders, both ports, REST+Kafka+persistence, ponytail, plain README)
    - aidlc-rules/domain/java-spring/details/java-spring-hex-layering-detail.md (package map mental model; do not copy bank package names)
    - package.json (files array only change)
  </read_first>
  <behavior>
    - All Task 1 assertions pass
    - Full npm test green
    - npm pack --dry-run includes an examples/java-spring path in the file list
  </behavior>
  <action>
    Author the locked starter tree at repo-root `examples/java-spring/` (sibling of aidlc-rules/, never nested inside it). Honor CONTEXT over RESEARCH draft names: Order domain under `com.example.orders`, not SamplePayment/com.example.hexagonal.

    **README.md** (JAVA-EX-01 discoverability): plain Markdown only — no YAML frontmatter, no id/scope fields. Cover purpose (LLM mirror skeleton), non-runnable ceiling (no Maven/Gradle/Boot app), layout map, how to mirror, and governing rule ids: java-spring-hex-layering, java-spring-inbound-rest, java-spring-inbound-kafka, java-spring-svc-internal-outbound, java-spring-ddd-tactical optional. Explicitly state files are NOT selectable governance rules (outside aidlc-rules scan root; D-10 backstop).

    **Domain (plain Java, no framework annotations)** per D-01/D-02:
    - `domain/Order.java` — thin aggregate root type; compact fields ok (orderId, customerId, amount or similar); one-line javadoc citing JS-DDD-01 / java-spring-ddd-tactical or hex rule.
    - `domain/PlaceOrderCommand.java` — immutable-shaped command type used by input port.

    **Application ports + handler (plain Java)** per D-02/D-03:
    - `application/port/PlaceOrderPort.java` — input port interface (e.g. place(PlaceOrderCommand)).
    - `application/port/OrderRepositoryPort.java` — output port interface (e.g. save(Order)).
    - `application/PlaceOrderHandler.java` — implements PlaceOrderPort; depends on OrderRepositoryPort only; core delegation; no Spring stereotypes.

    **Adapters (framework annotations allowed only here)** per D-02/D-03:
    - `adapter/in/web/PlaceOrderController.java` — thin REST: validate/map boundary → PlaceOrderPort; may use @RestController / @PostMapping as pointers; no business logic.
    - `adapter/in/messaging/PlaceOrderKafkaListener.java` — maps payload → port; comments or stubs visibly name idempotency, retry, and DLQ responsibilities; may use @KafkaListener; no Kafka client types in domain packages.
    - `adapter/out/persistence/OrderRepositoryAdapter.java` — implements OrderRepositoryPort; concrete JPA/JDBC omitted with `ponytail:` comment naming ceiling and upgrade path (consumer adds repository/driver).

    Snippet depth: compile-shaped signatures + short bodies; no pom.xml, build.gradle, application.yml, driver JARs, real brokers. Vendor-free (no WSO2/SmartVista/TIBCO tokens). Include `ponytail:` on intentional omissions (at least persistence mechanism; optionally retry/DLQ infra).

    **package.json** per locked ship decision: add only `"examples"` to the `files` array (alongside dist, bin, aidlc-rules). Do not touch dependencies, devDependencies, engines, scripts, bin, main, publishConfig, version, name.

    **Engine freeze:** zero edits to src/rules/**, src/index/**, src/inject/**, src/cli/**, src/schema/**, src/select/** except the new starter-examples.test.ts if a path string tweak is needed for GREEN. Do not edit precedence.test.ts or existing pack suites.

    If Task 1 used provisional paths that differ only by package depth discretion, align test EXPECTED_SNIPPETS to the files written above — do not invent alternate trees.
  </action>
  <verify>
    <automated>npm run build:test && node --test dist-test/select/starter-examples.test.js && npm test && node -e "const p=require('./package.json'); if(!p.files||!p.files.includes('examples')) process.exit(1)" && npm pack --dry-run 2>&1 | grep -E "examples/java-spring" </automated>
  </verify>
  <done>Starter tree ships under examples/java-spring with Order hexagonal slice; suite green; inventory 10; package tarball lists examples; full npm test green; no engine production edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content → rule-index | Only paths under aidlc-rules/ (default build-index root) may become selectable rules |
| examples/ → consumers | Static documentation snippets; no runtime execution in the overlay |
| package tarball → npm consumers | files[] controls which trees ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege / Tampering | examples/** entering rule-index | high | mitigate | Sibling layout outside aidlc-rules; loader only scans --root; suite asserts no examples/ sourceFile; D-10 backstop if examples root scanned; no rule frontmatter on README |
| T-16-02 | Information Disclosure | starter snippets | low | accept | No credentials/secrets in snippets; vendor-free; static only |
| T-16-03 | Tampering | package.json files omission | medium | mitigate | Add examples to files[]; verify via suite + npm pack --dry-run |
| T-16-04 | Denial of Service | accidental full-app sample | low | mitigate | Thin stubs only; no build/runtime deps; README non-runnable ceiling |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this phase — no install tasks |

ASVS L1: no auth/network surface; high severity T-16-01 blocked by layout + automated proof.
</threat_model>

<verification>
1. `npm run build:test && node --test dist-test/select/starter-examples.test.js` — all cases green
2. `npm test` — full suite green; precedence inventory still 10
3. `npm pack --dry-run` lists `examples/java-spring/` paths
4. `git diff --name-only` shows no production engine paths under src/rules, src/index (except none), src/inject, src/cli, src/schema; only new test + examples + package.json
5. README has no leading rule frontmatter
</verification>

<success_criteria>
- JAVA-EX-01: examples/java-spring ships hexagonal Order layout (domain, input+output ports, handler, REST, Kafka, persistence) under com.example.orders outside scan roots
- JAVA-EX-02: buildIndex never selects examples content; D-10 backstop proven; inventory remains 10
- package.json files includes examples
- Engine frozen; zero new npm deps
- Threat T-16-01 mitigated by layout + suite
</success_criteria>

<output>
Create `.planning/phases/16-starter-examples-outside-index/16-01-SUMMARY.md` when done
</output>
