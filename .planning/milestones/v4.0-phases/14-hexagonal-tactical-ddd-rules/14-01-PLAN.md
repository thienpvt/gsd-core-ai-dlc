---
phase: 14-hexagonal-tactical-ddd-rules
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/select/java-spring-hex-ddd.test.ts
autonomous: true
requirements:
  - JAVA-HEX-01
  - JAVA-DDD-01
must_haves:
  truths:
    - "domains=[] never selects java-spring-hex-layering or java-spring-ddd-tactical (out-of-scope)"
    - "Subscribed construction + domain/application/adapter/port paths can select hex-layering"
    - "Subscribed construction + domain/Aggregate/ValueObject/DomainEvent paths can select ddd-tactical"
    - "Keyword-only bank traps (identity, prevent-duplicate, report/support) select neither HEX nor DDD"
    - "Infra EntityManagerConfig / JpaEntityScanner paths do not select ddd-tactical"
    - "docs taskType, test paths, README/non-Java paths, and inception phase select neither"
    - "When selected, inject contains one-sentence summaries only — no BODY_CANARY and no ## Rule JS-HEX-01 / JS-DDD-01 essays"
  artifacts:
    - path: src/select/java-spring-hex-ddd.test.ts
      provides: "RED (then GREEN with plan 02) node:test suite for JAVA-HEX-01 / JAVA-DDD-01 matrices + CR negatives"
      contains: "java-spring-hex-layering|java-spring-ddd-tactical|JAVA-HEX-01|JAVA-DDD-01"
  key_links:
    - from: src/select/java-spring-hex-ddd.test.ts
      to: aidlc-rules (via buildIndex)
      via: "buildIndex(path.resolve(cwd,'aidlc-rules')) then select/renderInjection"
      pattern: "buildIndex\\(|select\\(|renderInjection\\("
---

<objective>
Write the Phase 14 HEX/DDD proof suite first (TDD RED): path-primary hexagonal layering, tactical DDD selection, CR-style false-positive negatives, and inject summary-only quarantine against the real `aidlc-rules` root.

Purpose: Lock JAVA-HEX-01 and JAVA-DDD-01 as failing automated tests before content lands so rule authoring cannot drift from CONTEXT/RESEARCH contracts (multi-token keywords, tightened Entity glob, construction-only, docs/test excludes).
Output: `src/select/java-spring-hex-ddd.test.ts` encoding the full matrix; suite fails until plan 02 content exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-CONTEXT.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-RESEARCH.md
@.planning/phases/14-hexagonal-tactical-ddd-rules/14-VALIDATION.md
@src/select/java-spring-pack.test.ts
@src/select/select.ts
@src/inject/inject.ts
@src/index/build.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED hex-ddd suite — path matrices, CR negatives, inject quarantine</name>
  <files>src/select/java-spring-hex-ddd.test.ts</files>
  <behavior>
    - Hygiene: buildIndex(aidlc-rules) succeeds; JSON.stringify(index) lacks BODY_CANARY java-spring-hex-layering and BODY_CANARY java-spring-ddd-tactical; details/ files are not separate index rules
    - JAVA-HEX-01 / JAVA-DDD-01 subscription: domains=[] + rich hex/ddd signal (domain path + hexagonal + aggregate-root keywords) → neither HEX_DDD_IDS selected; each skipped with reason out-of-scope
    - JAVA-HEX-01 positive paths (domains=["java-spring"], phase construction): .../domain/PaymentService.java selects hex-layering; .../application/...Handler.java selects hex-layering; .../adapter/... or .../ports/... selects hex-layering
    - JAVA-DDD-01 positive paths: .../domain/PaymentAggregate.java selects ddd-tactical; .../domain/MoneyValueObject.java selects ddd-tactical; .../domain/OrderPlacedDomainEvent.java selects ddd-tactical; domain path also selects hex-layering (overlap expected — both may select together)
    - Keyword positives (paths empty): keywords ["hexagonal"] or ["ports-and-adapters"] or ["inbound-port"] or ["outbound-port"] select hex-layering; keywords ["aggregate-root"] or ["value-object"] or ["domain-event"] or ["tactical-ddd"] select ddd-tactical
    - CR negatives (keyword-only, paths []): ["identity"] → neither; ["prevent-duplicate"] or ["eventual-consistency"] → neither; ["report","support"] → neither; ["interest-rate"] → neither
    - CR negatives (path): infrastructure/EntityManagerConfig.java → ddd-tactical NOT selected; config/JpaEntityScanner.java → ddd-tactical NOT selected
    - Unrelated: README.md / non-Java path only → neither; taskType docs + domain path → neither; **/src/test/** or *Test* path → neither; phase inception + domain path → neither (out-of-phase)
    - Summary contract (once records exist): each HEX/DDD record is scope domain, classification advisory, detailPath non-empty, summary one line no newline length &lt;= 160; HEX summary encodes inward/domain purity language (spring|jpa|framework|gateway|inward|port); DDD summary encodes aggregate + value object + past-tense event language
    - Inject quarantine: when HEX and/or DDD selected, renderInjection contains that rule summary and does NOT contain BODY_CANARY tokens or "## Rule JS-HEX-01" / "## Rule JS-DDD-01"
  </behavior>
  <action>
    Create sibling suite `src/select/java-spring-hex-ddd.test.ts` (do NOT extend Phase 13 PACK_IDS inventory in java-spring-pack.test.ts — keeps Phase 13 four-rule assertions stable per RESEARCH discretion).

    Mirror patterns from `src/select/java-spring-pack.test.ts`: `node:test` + `node:assert/strict`, `buildIndex` from `../index/build.js`, `select` from `./select.js`, `renderInjection` from `../inject/inject.js`, types from `../types.js`.

    Constants (locked CONTEXT + RESEARCH):
    - PACK_ROOT = path.resolve(process.cwd(), "aidlc-rules")
    - HEX_DDD_IDS = ["java-spring-hex-layering", "java-spring-ddd-tactical"]
    - BODY_CANARIES for each id: `BODY_CANARY java-spring-hex-layering` and `BODY_CANARY java-spring-ddd-tactical`
    - RULE_ESSAY_HEADINGS: "## Rule JS-HEX-01", "## Rule JS-DDD-01"
    - SUBSCRIBED = { phase: "construction", domains: ["java-spring"] }
    - UNSUBSCRIBED = { phase: "construction", domains: [] }

    Prefer production `aidlc-rules` root via buildIndex — missing pack content is the expected RED cause. Do not invent a duplicate fixture store.

    Path fixtures to encode (use bank-realistic Java paths under a fictional module, e.g. `src/main/java/com/bank/payments/...`):
    - domain/PaymentService.java, domain/PaymentAggregate.java, domain/MoneyValueObject.java, domain/OrderPlacedDomainEvent.java
    - application/PlaceOrderHandler.java
    - adapter/out/persistence/PaymentRepositoryAdapter.java and/or ports/PaymentPort.java
    - infrastructure/EntityManagerConfig.java, config/JpaEntityScanner.java (DDD negatives — tightened Entity glob)
    - README.md, docs-only and test paths for exclude cases

    Document in a file-level handoff comment: HEX_DDD_IDS, expected canary strings, essay headings, and pointer to 14-RESEARCH.md "Recommended Rule Specs" including the locked Entity path `**/domain/**/*Entity*` (not bare `**/*Entity*`).

    Do not modify any production `src/**` modules outside this new test file. Do not author rule Markdown in this plan. Zero new npm deps. Engine stays frozen.

    Expected RED: suite fails because HEX/DDD index entries are missing or selection/summary assertions fail.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-hex-ddd.test.js</automated>
  </verify>
  <done>
    Test file exists, compiles, and fails for the right reasons (missing HEX/DDD rules or failed content assertions). Named tests cover JAVA-HEX-01, JAVA-DDD-01, CR negatives, excludes, inject quarantine.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm RED and document expected GREEN handoff</name>
  <files>src/select/java-spring-hex-ddd.test.ts</files>
  <action>
    Run the targeted hex-ddd suite after `npm run build:test` and confirm failures are content-missing / assertion failures — not import or TypeScript errors.

    If the suite fails to compile due to import paths, fix imports only inside the test file to match project `.js` suffix conventions used in `java-spring-pack.test.ts`.

    Do not author production rules in this plan (plan 02 owns content). Do not rebuild `rule-index.json` here.

    Ensure the file-level comment lists HEX_DDD_IDS, BODY_CANARY tokens, essay headings, and the Entity glob lock (`**/domain/**/*Entity*`) so plan 02 authors match tests exactly.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-hex-ddd.test.js ; test $? -ne 0</automated>
  </verify>
  <done>
    Suite is RED with clear missing-content failures. No production rule files or index rebuild in this plan. Handoff comment documents canaries and ids for plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → aidlc-rules / src filesystem | Tests read rule store; must not execute untrusted code from rules |
| Future inject path ← selected summaries | Essay-length or always-on HEX/DDD summaries would bloat model context (core-value threat) |
| Domain subscription gate | Unsubscribed consumers must not receive java-spring HEX/DDD guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | Domain pack without subscribe | high | mitigate | domains=[] → HEX/DDD out-of-scope asserts in suite (JAVA-HEX-01 / JAVA-DDD-01) |
| T-14-02 | Spoofing | Selection false positives (bare needles / broad Entity) | high | mitigate | CR negatives: identity, prevent-duplicate, report/support; EntityManagerConfig / JpaEntityScanner path negatives; multi-token keyword contract only |
| T-14-03 | Information Disclosure | Index/inject body leak | high | mitigate | BODY_CANARY and essay-heading absence asserts on index JSON and renderInjection |
| T-14-04 | Denial of Service | Always-on architecture / essay summaries | medium | mitigate | Non-empty path/keyword positives required by tests; construction-only; summary length &lt;=160; docs/test/README negatives |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs zero packages; no supply-chain surface |
</threat_model>

<verification>
Targeted hex-ddd suite runs under node:test; RED proven before content plan. Maps to VALIDATION.md rows 14-01-01, 14-01-02, 14-01-03.
</verification>

<success_criteria>
- `src/select/java-spring-hex-ddd.test.ts` exists with named tests for JAVA-HEX-01 and JAVA-DDD-01 matrices plus CR negatives
- `npm run build:test` succeeds
- Hex-ddd suite fails (RED) until plan 02 lands content
- No production `src/` changes outside the new test file; zero new deps; Phase 13 pack suite left intact
</success_criteria>

<output>
Create `.planning/phases/14-hexagonal-tactical-ddd-rules/14-01-SUMMARY.md` when done
</output>
