---
phase: 13-domain-pack-service-classification-integrations
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - aidlc-rules/domain/java-spring/java-spring-svc-internal-outbound.md
  - aidlc-rules/domain/java-spring/java-spring-svc-internet-outbound.md
  - aidlc-rules/domain/java-spring/java-spring-inbound-rest.md
  - aidlc-rules/domain/java-spring/java-spring-inbound-kafka.md
  - aidlc-rules/domain/java-spring/details/java-spring-svc-internal-outbound-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-svc-internet-outbound-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-inbound-rest-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-inbound-kafka-detail.md
  - rule-index.json
  - src/select/java-spring-pack.test.ts
autonomous: true
requirements:
  - JAVA-PACK-01
  - JAVA-PACK-02
  - JAVA-SVC-01
  - JAVA-SVC-02
  - JAVA-SVC-03
  - JAVA-IN-01
  - JAVA-IN-02
must_haves:
  truths:
    - "Subscribing domains java-spring is required before any pack rule appears in select output"
    - "Four advisory domain rules ship under aidlc-rules/domain/java-spring/ with one-sentence summaries and detailPath targets"
    - "Internal and internet-facing outbound rules are mutually exclusive via exclude axes; ambiguous signals select neither"
    - "REST and Kafka inbound rules are construction-only, path-primary, and separate files"
    - "rule-index.json rebuild includes the four pack ids with summaries and detailPath pointers and no body canaries"
    - "npm test is green including java-spring-pack suite"
  artifacts:
    - path: aidlc-rules/domain/java-spring/java-spring-svc-internal-outbound.md
      provides: "Internal outbound advisory rule (JS-SVC-01)"
      contains: "java-spring-svc-internal-outbound"
    - path: aidlc-rules/domain/java-spring/java-spring-svc-internet-outbound.md
      provides: "Internet-facing outbound advisory rule (JS-SVC-02)"
      contains: "java-spring-svc-internet-outbound"
    - path: aidlc-rules/domain/java-spring/java-spring-inbound-rest.md
      provides: "Thin REST controller inbound rule (JS-IN-01)"
      contains: "java-spring-inbound-rest"
    - path: aidlc-rules/domain/java-spring/java-spring-inbound-kafka.md
      provides: "Idempotent Kafka consumer inbound rule (JS-IN-02)"
      contains: "java-spring-inbound-kafka"
    - path: rule-index.json
      provides: "Body-free production index including pack rules"
  key_links:
    - from: aidlc-rules/domain/java-spring/*.md
      to: rule-index.json
      via: "governance build-index --root aidlc-rules --out rule-index.json"
      pattern: "java-spring-"
    - from: src/select/java-spring-pack.test.ts
      to: aidlc-rules/domain/java-spring
      via: "buildIndex + select domains gate"
      pattern: "domains:\\s*\\[\\\"java-spring\\\"\\]"
---

<objective>
Author the subscribe-able `java-spring` domain pack (four advisory rules + detail prose), rebuild the production rule index, and turn the plan 01 pack suite GREEN without engine or dependency changes.

Purpose: Deliver JAVA-PACK/SVC/IN requirements as content on the frozen select/inject engine so bank service classification and inbound conventions inject as one-sentence summaries only.
Output: Pack under `aidlc-rules/domain/java-spring/`, updated `rule-index.json`, green `java-spring-pack` tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-CONTEXT.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-RESEARCH.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-VALIDATION.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-01-SUMMARY.md
@src/select/java-spring-pack.test.ts
@aidlc-rules/enterprise/require-mfa.md
@test/fixtures/detailpath-store/enterprise/with-detail.md
@src/schema/frontmatter.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author four java-spring pack rules + detail files</name>
  <files>aidlc-rules/domain/java-spring/java-spring-svc-internal-outbound.md, aidlc-rules/domain/java-spring/java-spring-svc-internet-outbound.md, aidlc-rules/domain/java-spring/java-spring-inbound-rest.md, aidlc-rules/domain/java-spring/java-spring-inbound-kafka.md, aidlc-rules/domain/java-spring/details/java-spring-svc-internal-outbound-detail.md, aidlc-rules/domain/java-spring/details/java-spring-svc-internet-outbound-detail.md, aidlc-rules/domain/java-spring/details/java-spring-inbound-rest-detail.md, aidlc-rules/domain/java-spring/details/java-spring-inbound-kafka-detail.md</files>
  <action>
    Create pack root `aidlc-rules/domain/java-spring/` (subscription name `java-spring` only — not java_spring / JavaSpring). Ship exactly four content rules; no pack README (avoids accidental fifth indexed markdown); no empty always-on pack-marker rule.

    Shared frontmatter defaults (locked CONTEXT + RESEARCH):
    - scope: domain
    - classification: advisory (omit enforcement)
    - phases: construction only for all four
    - detailPath: details/&lt;id&gt;-detail.md (relative; one detail file per rule)
    - severity: high for both outbound rules; medium for both inbound rules
    - summary: exactly one sentence, inject-ready, target &lt;= ~160 chars, no newlines

    **1) java-spring-svc-internal-outbound** — body heading `## Rule JS-SVC-01: Internal Service Outbound Access`
    - summary (locked intent JAVA-SVC-02): Internal services may use JDBC/ORM or direct DB access outbound; do not force an API gateway on internal-only calls.
    - triggers.keywords: internal-service, internal-only (class markers primary; per RESEARCH discretion avoid bare jdbc-only if it causes ambiguous guess — prefer class markers + internal paths; jdbc/jpa/orm only as additional positives if still mutually exclusive)
    - triggers.paths: **/internal/**, **/services/internal/**, **/module-internal/**
    - triggers.exclude.keywords: internet-facing, external-facing, public-edge, edge-service, api-gateway, and the bank gateway product token in lowercase form used only as exclude needle in frontmatter (Markdown content may discuss it; engine src must not)
    - triggers.exclude.paths: **/internet-facing/**, **/external-facing/**, **/edge/**
    - Body short + ### Verification; include BODY_CANARY token matching plan 01 tests
    - Detail: JDBC/JPA/ORM allowed; no forced gateway; light note domain stays free of driver sprawl; verification checklist; canary

    **2) java-spring-svc-internet-outbound** — body heading `## Rule JS-SVC-02: Internet-Facing Outbound Via Gateway`
    - summary (JAVA-SVC-03): Internet-facing services must send outbound calls through the approved API gateway; do not call external systems with raw WebClient, RestTemplate, or SDKs from domain code. Prefer capability language in summary; put the bank gateway product name (WSO2) in **detail body only**, not in summary if it risks multi-sentence or vendor-in-inject noise (RESEARCH A4).
    - triggers.keywords: internet-facing, external-facing, public-edge, edge-service, api-gateway, webclient, resttemplate, feign, outbound-http (plus optional gateway product keyword only in frontmatter triggers if needed for matching — still Markdown file, not src/)
    - triggers.paths: **/internet-facing/**, **/external-facing/**, **/edge/**, **/adapter/out/http/**, **/infrastructure/gateway/**
    - triggers.exclude.keywords: internal-service, internal-only
    - triggers.exclude.paths: **/internal/**, **/services/internal/**, **/module-internal/**
    - Detail MUST name approved gateway product language including WSO2 as org example; no raw external SDK/WebClient/RestTemplate from domain; verification checklist; canary

    **3) java-spring-inbound-rest** — body heading `## Rule JS-IN-01: Thin REST Controllers`
    - summary (JAVA-IN-01): Keep REST controllers thin: validate at the boundary, map to application ports, and keep business logic out of controllers.
    - triggers.keywords: rest, controller, endpoint
    - triggers.paths: **/*Controller*, **/api/**, **/web/**, **/rest/**, **/adapter/in/web/**
    - triggers.exclude.taskType: docs; exclude.paths: **/*Test*, **/*Tests*, **/src/test/**
    - Domain purity (no HTTP client types in domain) stated in summary/body only — not a fifth rule

    **4) java-spring-inbound-kafka** — body heading `## Rule JS-IN-02: Idempotent Kafka Consumers`
    - summary (JAVA-IN-02): Kafka consumers must be idempotent with explicit retry/DLQ policy, and Kafka client types stay in adapters—not in domain.
    - triggers.keywords: kafka, consumer, listener
    - triggers.paths: **/*Listener*, **/*Consumer*, **/messaging/**, **/kafka/**, **/adapter/in/messaging/**
    - triggers.exclude: same docs/test excludes as REST

    Authoring constraints:
    - Match frontmatter schema (id kebab pattern, required fields) — buildIndex/Ajv must accept
    - Never use empty triggers object on these style rules
    - Never put vendor product names into any production file under `src/`
    - Do not add binding classification
    - Do not touch engine select/types modules
    - Align BODY_CANARY strings with plan 01 test expectations; if tests used different canary literals, update either content or test strings so they match (prefer content matches tests)

    Reference templates: enterprise require-mfa for prose shape; detailpath-store for detailPath layout; RESEARCH Recommended Rule Specs for full YAML.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node -e "const {buildIndex}=require('./dist-test/index/build.js'); const i=buildIndex('aidlc-rules'); const ids=i.rules.map(r=>r.id); for (const id of ['java-spring-svc-internal-outbound','java-spring-svc-internet-outbound','java-spring-inbound-rest','java-spring-inbound-kafka']) { if (!ids.includes(id)) { console.error('missing '+id); process.exit(1);} } console.log('pack ids present', ids.filter(x=>x.startsWith('java-spring')).join(','));"</automated>
  </verify>
  <done>
    Four rule files and four detail files exist under aidlc-rules/domain/java-spring/; buildIndex loads all four pack ids with scope domain, classification advisory, non-empty detailPath, one-sentence summaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild production rule-index.json</name>
  <files>rule-index.json</files>
  <action>
    After rules validate via buildIndex, rebuild the committed production index:

    `node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json`

    (If dist is stale, run `npm run build` first so bin/governance.cjs resolves compiled CLI.)

    Confirm:
    - Existing enterprise require-mfa (or prior winners) still present
    - Exactly four new java-spring-* winners appear (no detail files as separate rules)
    - Index records carry summary + detailPath only — stringify of rule-index.json must not contain BODY_CANARY strings from pack details/bodies

    Do not hand-edit rule-index.json structure; always regenerate from build-index.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json &amp;&amp; node -e "const fs=require('fs'); const j=fs.readFileSync('rule-index.json','utf8'); if (/BODY_CANARY/.test(j)) { console.error('canary leak'); process.exit(1);} const i=JSON.parse(j); const pack=i.rules.filter(r=>String(r.id).startsWith('java-spring')); if (pack.length!==4) { console.error('expected 4 pack rules got', pack.length); process.exit(1);} console.log('index ok', pack.map(r=>r.id).join(','));"</automated>
  </verify>
  <done>
    rule-index.json regenerated with four java-spring pack records, no body canaries, detailPath pointers intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN pack suite — fix content/tests until npm test passes</name>
  <files>src/select/java-spring-pack.test.ts, aidlc-rules/domain/java-spring/java-spring-svc-internal-outbound.md, aidlc-rules/domain/java-spring/java-spring-svc-internet-outbound.md, aidlc-rules/domain/java-spring/java-spring-inbound-rest.md, aidlc-rules/domain/java-spring/java-spring-inbound-kafka.md, aidlc-rules/domain/java-spring/details/java-spring-svc-internet-outbound-detail.md</files>
  <behavior>
    - All plan 01 matrix tests pass against production aidlc-rules
    - Mutual exclusion: internal-only, internet-only, both→neither, neither→neither
    - Inject summary-only; vendor tokens absent from production src
    - Inbound path-primary construction-only
  </behavior>
  <action>
    Run `npm test` (or targeted `node --test dist-test/select/java-spring-pack.test.js` after build:test). Drive failures to green by adjusting **content frontmatter/summaries/details** first — not the selection engine.

    Allowed test-file edits: only if canary strings, path fixtures, or assertion wording need alignment with final summaries; do not weaken XOR/ambiguous-neither/subscription assertions.

    If dual outbound selection occurs, tighten exclude axes and/or remove over-broad positive keywords (locked fail-open: never guess class).

    If bare jdbc selects internal without class marker and tests require neither for ambiguous, remove bare jdbc/jpa from positives (RESEARCH discretion refinement).

    Confirm no production `src/**/*.ts` outside tests was modified for vendor strings or classification logic.

    Optional CLI smoke (non-blocking if suite green): select with and without `--domains java-spring` via governance CLI using construction phase and an internet-facing keyword signal.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full npm test green. All seven Phase 13 requirement behaviors proven by java-spring-pack suite. Engine frozen (no production select/types changes). Zero new npm deps. Pack ships under aidlc-rules and index.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule authors → rule-index.json → select/inject | Malformed or over-broad rules over-inject guidance into agent context |
| Domain subscription boundary | Unsubscribed consumers must not receive java-spring conventions |
| Markdown content vs engine src | Vendor product names allowed only on content side of this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | Outbound class rules | high | mitigate | Symmetric exclude axes + suite XOR/ambiguous cases; advisory only |
| T-13-02 | Tampering | Vendor strings in engine | medium | mitigate | WSO2 and peers only in Markdown details/triggers content; suite walks production src |
| T-13-03 | Information Disclosure | Body leak into index/inject | high | mitigate | details/ quarantine, one-sentence summaries, canary checks, rebuild gate |
| T-13-04 | Elevation of Privilege | Unsubscribed domain injection | high | mitigate | scope domain + folder layout; domains gate already in select; suite proves out-of-scope |
| T-13-05 | Denial of Service | Essay summaries / always-on triggers | medium | mitigate | Non-empty specific triggers; construction-only; summary length asserts |
| T-13-06 | Spoofing | Binding theater without adapter | medium | mitigate | classification advisory on all four rules; no enforcement field |
| T-13-SC | Tampering | Package installs | low | accept | Zero new packages this phase |
</threat_model>

<verification>
`npm test` green; buildIndex(aidlc-rules) returns four pack domain rules; selection matrices for PACK/SVC/IN pass; rule-index.json free of body canaries. Aligns with VALIDATION.md Wave 0 completion and all per-task rows.
</verification>

<success_criteria>
- Project can subscribe domains: ["java-spring"] and only then receive pack rules
- Every pack summary is one sentence; full prose via detailPath only
- Internal vs internet-facing outbound XOR with ambiguous → neither
- REST/Kafka construction path-primary inbound inject works
- rule-index.json rebuilt; npm test green; no production engine edits; no new deps
</success_criteria>

<output>
Create `.planning/phases/13-domain-pack-service-classification-integrations/13-02-SUMMARY.md` when done
</output>
