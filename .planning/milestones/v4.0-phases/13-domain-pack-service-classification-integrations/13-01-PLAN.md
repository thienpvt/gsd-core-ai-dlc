---
phase: 13-domain-pack-service-classification-integrations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/select/java-spring-pack.test.ts
autonomous: true
requirements:
  - JAVA-PACK-01
  - JAVA-PACK-02
  - JAVA-SVC-01
  - JAVA-SVC-02
  - JAVA-SVC-03
  - JAVA-IN-01
  - JAVA-IN-02
must_haves:
  truths:
    - "domains=[] never selects any of the four java-spring pack rule ids (out-of-scope)"
    - "domains=['java-spring'] can select pack rules when phase/triggers match"
    - "Internal XOR internet outbound; both class markers select neither; unnamed class selects neither outbound"
    - "Internal outbound summary encodes JDBC/ORM allowed and no forced gateway"
    - "Internet outbound summary encodes approved gateway outbound; detail may name gateway product; production src has no vendor product tokens"
    - "Controller/API construction paths select REST inbound; Listener/Kafka construction paths select Kafka inbound; inception selects neither inbound"
    - "Every pack summary is one sentence (no newline, target <=160 chars); inject fragment never contains body canaries or Rule JS essay headings"
  artifacts:
    - path: src/select/java-spring-pack.test.ts
      provides: "RED (then GREEN with plan 02) node:test suite for all Phase 13 matrices"
      contains: "JAVA-PACK-01|JAVA-SVC-01|java-spring-inbound"
  key_links:
    - from: src/select/java-spring-pack.test.ts
      to: aidlc-rules (via buildIndex)
      via: "buildIndex(path.resolve(cwd,'aidlc-rules')) then select/renderInjection"
      pattern: "buildIndex\\(|select\\(|renderInjection\\("
---

<objective>
Write the Phase 13 java-spring pack proof suite first (TDD RED): subscription, one-sentence summaries, Internal vs internet-facing outbound XOR, and REST/Kafka inbound path-primary matrices against the real `aidlc-rules` root.

Purpose: Lock all seven requirement behaviors as failing automated tests before content lands so pack authoring cannot drift from CONTEXT/RESEARCH contracts.
Output: `src/select/java-spring-pack.test.ts` encoding the full matrix; suite fails until plan 02 content exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-CONTEXT.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-RESEARCH.md
@.planning/phases/13-domain-pack-service-classification-integrations/13-VALIDATION.md
@src/select/select.test.ts
@src/inject/inject.ts
@src/index/build.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED pack suite — subscription, summary, outbound XOR, inbound paths</name>
  <files>src/select/java-spring-pack.test.ts</files>
  <behavior>
    - JAVA-PACK-01: buildIndex(aidlc-rules); select with domains=[] and a rich signal (internet-facing + controller + kafka paths) → none of PACK_IDS in selected; each pack id skipped with reason out-of-scope
    - JAVA-PACK-01 positive: domains=["java-spring"] + matching class/path signals can place the matching pack id in selected
    - JAVA-PACK-02: each pack rule record summary has no newline, length &lt;= 160, ends with sentence punctuation or is a single prose sentence; detailPath present and non-empty; classification advisory; scope domain
    - JAVA-PACK-02 inject: when a pack rule is selected, renderInjection output contains that rule's summary and does not contain that rule's body canary token or the "## Rule JS-" essay heading form
    - JAVA-SVC-01: under domains java-spring + construction — keywords ["internal-service"] selects internal-outbound only; ["internet-facing"] selects internet-outbound only; both markers → neither svc-*; neither class marker (e.g. only taskType feature) → neither svc-*
    - JAVA-SVC-02: when internal-outbound selected, summary asserts JDBC/ORM-or-direct-DB allowed language and no-forced-gateway language (substring checks on summary text)
    - JAVA-SVC-03: when internet-outbound selected, summary asserts approved API gateway / no raw WebClient|RestTemplate|SDK-from-domain language; production sources under src/ excluding **/*.test.ts contain no case-insensitive vendor tokens wso2, tibco, smartvista (read/walk assert); internet detail file MAY contain the bank gateway product name
    - JAVA-IN-01: construction + path .../PaymentController.java (or **/api/**) selects inbound-rest; phase inception with same path does not select inbound-rest; taskType docs excluded when exclude configured
    - JAVA-IN-02: construction + path .../OrderListener.java (or **/kafka/**) selects inbound-kafka; REST-only controller path does not select inbound-kafka; inception does not select inbound-kafka
    - Hygiene: buildIndex(aidlc-rules) succeeds; JSON.stringify(index) lacks each body canary; details/ files are not separate index rules (no id from detail filenames)
  </behavior>
  <action>
    Create co-located `src/select/java-spring-pack.test.ts` using the same patterns as `src/select/select.test.ts`: `node:test` + `node:assert/strict`, `buildIndex` from `../index/build.js`, `select` from `./select.js`, `renderInjection` from `../inject/inject.js`, types from `../types.js`.

    Constants (per RESEARCH Code Examples and locked inventory):
    - PACK_ROOT = path.resolve(process.cwd(), "aidlc-rules")
    - PACK_IDS = java-spring-svc-internal-outbound, java-spring-svc-internet-outbound, java-spring-inbound-rest, java-spring-inbound-kafka
    - packIndex() = buildIndex(PACK_ROOT)

    Prefer testing the **production** `aidlc-rules` root (RESEARCH A2) — do not invent a duplicate fixture store unless buildIndex on aidlc-rules is impossible mid-RED (missing pack is the expected RED cause).

    Encode mutual exclusion and fail-open ambiguous cases explicitly (locked: keyword+path hybrid; exclude wins; ambiguous → neither outbound). Do not hardcode classification vocabulary in engine source — only assert against select outcomes and rule frontmatter once content exists.

    Body-canary strategy: each test that checks inject/index absence should look for a unique canary substring that plan 02 will place in rule bodies/details (e.g. `BODY_CANARY java-spring-inbound-rest`). Document the expected canary strings in test comments so plan 02 authors match them.

    Vendor gate: implement as a small fs walk over `src/**/*.ts` excluding `*.test.ts` / `*.property.test.ts`, asserting zero matches for the three vendor tokens case-insensitively. Do not put those vendor tokens into production identifiers in this test file beyond string literals used as search needles (acceptable in tests).

    Do not modify any production `src/**` modules outside this new test file. Zero new npm deps. Engine stays frozen.

    Expected RED: `npm test` fails because pack files/index entries are missing or assertions on summaries/triggers fail.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-pack.test.js</automated>
  </verify>
  <done>
    Test file exists, compiles, and fails for the right reasons (missing pack rules / failed assertions). Full matrix cases for JAVA-PACK-01/02, JAVA-SVC-01/02/03, JAVA-IN-01/02 are present as named tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm RED and document expected GREEN handoff</name>
  <files>src/select/java-spring-pack.test.ts</files>
  <action>
    Run the targeted pack suite (after `npm run build:test`) and capture that failures are content-missing / assertion failures — not import/TypeScript errors.

    If the suite fails to compile due to import paths, fix imports only inside the test file to match project ESM-from-CJS `.js` suffix conventions used in `select.test.ts`.

    Do not author production rules in this plan (plan 02 owns content). Do not rebuild `rule-index.json` here.

    Add a short file-level comment block listing PACK_IDS and pointing executors of plan 02 at RESEARCH "Recommended Rule Specs" for frontmatter/triggers/summaries and detail canary strings the tests expect.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/select/java-spring-pack.test.js ; test $? -ne 0</automated>
  </verify>
  <done>
    Suite is RED with clear missing-content failures. No production rule files or index rebuild in this plan. Handoff comment documents canary tokens and PACK_IDS for plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → aidlc-rules / src filesystem | Tests read rule store and source tree; must not execute untrusted code from rules |
| Future inject path ← selected summaries | Malformed or essay-length summaries would bloat model context (core-value threat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | Outbound class selection | high | mitigate | Plan 01 tests encode XOR + ambiguous-neither so dual/guess classification cannot ship (JAVA-SVC-01) |
| T-13-02 | Tampering | Vendor lock-in via src identifiers | medium | mitigate | Vendor-token walk assert on production src; product names only allowed in Markdown content (JAVA-SVC-03) |
| T-13-03 | Information Disclosure | Index/inject body leak | high | mitigate | Canary absence asserts on index JSON and renderInjection (JAVA-PACK-02) |
| T-13-04 | Elevation of Privilege | Domain pack without subscribe | high | mitigate | domains=[] → all pack ids out-of-scope (JAVA-PACK-01) |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs zero packages; no supply-chain surface |
</threat_model>

<verification>
Targeted pack suite runs under node:test; RED proven before content plan. Maps to VALIDATION.md rows 13-01-01 and 13-01-02 plus forward coverage for SVC/IN cases that turn green in plan 02.
</verification>

<success_criteria>
- `src/select/java-spring-pack.test.ts` exists with named tests for all seven requirement IDs
- `npm run build:test` succeeds
- Pack suite fails (RED) until plan 02 lands content
- No production `src/` changes outside the new test file; zero new deps
</success_criteria>

<output>
Create `.planning/phases/13-domain-pack-service-classification-integrations/13-01-SUMMARY.md` when done
</output>
