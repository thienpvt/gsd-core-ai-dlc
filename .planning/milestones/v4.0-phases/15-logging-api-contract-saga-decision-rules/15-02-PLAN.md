---
phase: 15-logging-api-contract-saga-decision-rules
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - aidlc-rules/domain/java-spring/java-spring-logging-audit.md
  - aidlc-rules/domain/java-spring/java-spring-api-contract.md
  - aidlc-rules/domain/java-spring/java-spring-saga-outbox.md
  - aidlc-rules/domain/java-spring/details/java-spring-logging-audit-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md
  - aidlc-rules/domain/java-spring/details/java-spring-saga-outbox-detail.md
  - rule-index.json
  - src/index/precedence.test.ts
  - src/select/java-spring-log-api-evt.test.ts
autonomous: true
requirements:
  - JAVA-LOG-01
  - JAVA-API-01
  - JAVA-EVT-01
must_haves:
  truths:
    - "Subscribing domains java-spring is required before LOG/API/EVT rules appear in select output"
    - "Three advisory domain rules ship under aidlc-rules/domain/java-spring/ with one-sentence summaries and detailPath targets"
    - "Construction logging/config Log/aop (or multi-token log keywords) inject logging-audit: correlation/trace id, no PII/secrets, audit for state-changing ops"
    - "Construction api/openapi/Resource/web (or multi-token API keywords) inject api-contract: OpenAPI SoT or generated-and-checked, one versioning policy, error envelope code/message/correlationId"
    - "Construction outbox/saga/messaging (or multi-token saga keywords) inject saga-outbox with when-NOT: no saga on single-service ACID"
    - "Bare log/logger/rest and unrelated README/docs/test/inception do not select LOG/API/EVT"
    - "rule-index.json rebuild includes three new ids with summaries and detailPath and no body canaries"
    - "precedence real-corpus inventory grows 7 → 10 winners (require-mfa + nine java-spring rules)"
    - "npm test is green including java-spring-log-api-evt suite"
  artifacts:
    - path: aidlc-rules/domain/java-spring/java-spring-logging-audit.md
      provides: "Logging/audit advisory rule (JS-LOG-01)"
      contains: "java-spring-logging-audit"
    - path: aidlc-rules/domain/java-spring/java-spring-api-contract.md
      provides: "API contract advisory rule (JS-API-01)"
      contains: "java-spring-api-contract"
    - path: aidlc-rules/domain/java-spring/java-spring-saga-outbox.md
      provides: "Saga/outbox decision advisory rule (JS-EVT-01)"
      contains: "java-spring-saga-outbox"
    - path: aidlc-rules/domain/java-spring/details/java-spring-logging-audit-detail.md
      provides: "Lazy detail prose for logging/audit"
    - path: aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md
      provides: "Lazy detail prose for API contracts including versioning default"
    - path: aidlc-rules/domain/java-spring/details/java-spring-saga-outbox-detail.md
      provides: "Lazy detail prose with saga/outbox/plain decision table and when-NOT"
    - path: rule-index.json
      provides: "Body-free production index including LOG/API/EVT pack rules"
    - path: src/index/precedence.test.ts
      provides: "Real-corpus inventory lock at 10 winners"
  key_links:
    - from: aidlc-rules/domain/java-spring/java-spring-logging-audit.md
      to: rule-index.json
      via: "governance build-index --root aidlc-rules --out rule-index.json"
      pattern: "java-spring-logging-audit"
    - from: src/select/java-spring-log-api-evt.test.ts
      to: aidlc-rules/domain/java-spring
      via: "buildIndex + select domains gate"
      pattern: "java-spring-logging-audit|java-spring-api-contract|java-spring-saga-outbox"
    - from: src/index/precedence.test.ts
      to: aidlc-rules
      via: "buildIndex(REAL_CORPUS) expectedIds length 10"
      pattern: "java-spring-saga-outbox"
---

<objective>
Author the three Phase 15 advisory rules (logging/audit, API contract, saga/outbox decision) and their detail files under the existing `java-spring` pack, rebuild the production rule index, grow the real-corpus inventory lock 7→10, and turn the plan 01 log-api-evt suite GREEN without engine or dependency changes.

Purpose: Deliver JAVA-LOG-01, JAVA-API-01, and JAVA-EVT-01 as path-primary, multi-token-triggered summaries so construction work gets cross-cutting logging, contract, and distributed-workflow guidance — including explicit saga when-NOT — without always-on essays.
Output: Three rules + three details under `aidlc-rules/domain/java-spring/`, updated `rule-index.json`, updated precedence inventory, green `java-spring-log-api-evt` tests and full `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-logging-api-contract-saga-decision-rules/15-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/phases/15-logging-api-contract-saga-decision-rules/15-01-SUMMARY.md
@src/select/java-spring-log-api-evt.test.ts
@aidlc-rules/domain/java-spring/java-spring-inbound-rest.md
@aidlc-rules/domain/java-spring/details/java-spring-inbound-rest-detail.md
@aidlc-rules/domain/java-spring/java-spring-hex-layering.md
@src/schema/frontmatter.schema.json
@src/index/precedence.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author logging-audit + api-contract + saga-outbox rules and detail files</name>
  <files>aidlc-rules/domain/java-spring/java-spring-logging-audit.md, aidlc-rules/domain/java-spring/java-spring-api-contract.md, aidlc-rules/domain/java-spring/java-spring-saga-outbox.md, aidlc-rules/domain/java-spring/details/java-spring-logging-audit-detail.md, aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md, aidlc-rules/domain/java-spring/details/java-spring-saga-outbox-detail.md</files>
  <action>
    Extend existing pack root `aidlc-rules/domain/java-spring/` (subscription name remains `java-spring`). Ship exactly three new content rules for this phase — no CQRS rule, no pack README, no empty always-on style rule, no production `src/` engine edits, no binding OpenAPI/file-presence enforcement (advisory only; binding deferred).

    Shared frontmatter defaults (locked CONTEXT):
    - scope: domain
    - classification: advisory (omit enforcement — no binding theater)
    - phases: [construction] only
    - severity: medium
    - detailPath: details/&lt;id&gt;-detail.md (relative; loader skips details/)
    - exclude.taskType: [docs]
    - exclude.paths: **/*Test*, **/*Tests*, **/src/test/**
    - summary: exactly one sentence, inject-ready, &lt;= ~160 chars, no newlines

    **1) java-spring-logging-audit** — body heading `## Rule JS-LOG-01: Logging and Audit` (JAVA-LOG-01)
    - id: java-spring-logging-audit
    - summary intent (must encode): Propagate correlation/trace ids (e.g. MDC); never log PII/secrets/PAN; emit audit events for state-changing operations with actor and outcome.
    - triggers.keywords ONLY: correlation-id, trace-id, mdc, audit-log, structured-logging
      - Never add bare log, logger, audit, or correlation needles (substring false positives / always-on noise; CONTEXT forbids bare log/logger)
    - triggers.paths: **/logging/**, **/config/*Log*, **/aop/**
      - Claude discretion (CONTEXT): optionally add **/filter/** if suite handoff locks CorrelationIdFilter positive; if added, keep it narrow (filter package for MDC/correlation filters), not bare **/log/**
    - Body: short prose on correlation/trace propagation, structured fields, forbid PII/secrets in logs, audit trail for mutations; ### Verification checklist; include `<!-- BODY_CANARY java-spring-logging-audit -->` matching plan 01
    - Detail: MDC/filter wiring sketch, what counts as PII/secret for bank services, audit event minimum fields (actor, action, resource, outcome, correlationId); verification checklist; same canary

    **2) java-spring-api-contract** — body heading `## Rule JS-API-01: API Contract` (JAVA-API-01)
    - id: java-spring-api-contract
    - summary intent (must encode): Treat OpenAPI as source-of-truth or generated-and-checked; apply one org versioning policy; return uniform error envelope with code, message, and correlationId.
    - triggers.keywords ONLY: openapi, api-version, error-envelope, swagger-spec
      - Never add bare rest, api, swagger, or endpoint needles (Phase 13 inbound-rest already covers controllers; CONTEXT forbids bare rest)
    - triggers.paths: **/api/**, **/openapi/**, **/*Resource.java, **/web/**
    - Body: OpenAPI SoT or generate-and-check drift; one versioning policy; error shape code/message/correlationId (details optional in envelope); ### Verification; `<!-- BODY_CANARY java-spring-api-contract -->`
    - Detail: Claude discretion — pick **URI path versioning** (`/api/v1/...`) as the org default and document header versioning as the explicit alternative only when already adopted; bank-specific error code catalogs stay in project scope; verification checklist; canary
    - Overlap with inbound-rest on **/api/** and **/web/** is expected — do NOT mutual-exclude REST vs API contract

    **3) java-spring-saga-outbox** — body heading `## Rule JS-EVT-01: Saga and Outbox Decisions` (JAVA-EVT-01)
    - id: java-spring-saga-outbox
    - summary intent (must encode): Choose saga for multi-service business transactions, outbox for same-TX DB+message publish, and plain calls when single-service ACID suffices — no saga cargo-cult.
    - triggers.keywords ONLY: saga, outbox, transactional-outbox, choreography, orchestration, distributed-transaction
      - Never add bare event, message, kafka, or transaction needles that reintroduce CR-style noise
    - triggers.paths: **/outbox/**, **/saga/**, **/messaging/**
    - Body: decision guidance + explicit when-NOT (single-service ACID → plain call; no saga for local CRUD); outbox when atomic DB write + message publish required; saga when business TX spans services (choreography or orchestration); ### Verification; `<!-- BODY_CANARY java-spring-saga-outbox -->`
    - Detail: decision table (plain vs outbox vs saga), when-NOT examples (internal single-DB tool, one-module ACID service), pointer that domain-event naming remains under tactical DDD rule; no CQRS mandate; verification checklist; canary
    - Overlap with inbound-kafka on **/messaging/** is expected — do NOT mutual-exclude

    Authoring constraints:
    - Match frontmatter schema (kebab id, required fields) — buildIndex/Ajv must accept
    - Never use empty triggers object
    - Vendor-neutral content (no WSO2/gateway product names required in these three rules)
    - Align BODY_CANARY strings with plan 01 test expectations
    - Reference Phase 13/14 rule files for prose/frontmatter shape
    - Deferred: starter examples (Phase 16), binding coverage (Phase 17), consumer docs (Phase 18), CQRS, binding OpenAPI gate

    Do not touch Phase 13/14 rule files except optional one-line forward pointers (not required for success).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node -e "const {buildIndex}=require('./dist-test/index/build.js'); const i=buildIndex('aidlc-rules'); const ids=i.rules.map(r=>r.id); for (const id of ['java-spring-logging-audit','java-spring-api-contract','java-spring-saga-outbox']) { if (!ids.includes(id)) { console.error('missing '+id); process.exit(1);} const r=i.rules.find(x=>x.id===id); if (r.classification!=='advisory') { console.error(id+' not advisory'); process.exit(1);} if (!r.detailPath) { console.error(id+' missing detailPath'); process.exit(1);} if (!r.summary || r.summary.includes('\n') || r.summary.length&gt;160) { console.error(id+' bad summary'); process.exit(1);} } console.log('log-api-evt ids ok');"</automated>
  </verify>
  <done>
    Three rule files and three detail files exist; buildIndex loads all three ids with scope domain, classification advisory, non-empty detailPath, one-sentence summaries &lt;=160 chars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild production rule-index.json and grow inventory lock 7→10</name>
  <files>rule-index.json, src/index/precedence.test.ts</files>
  <action>
    After rules validate via buildIndex, rebuild the committed production index:

    `node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json`

    (If dist is stale, run `npm run build` first so bin/governance.cjs resolves compiled CLI.)

    Confirm:
    - Prior winners still present (enterprise require-mfa + six Phase 13/14 java-spring rules)
    - Exactly three new Phase 15 ids appear: java-spring-logging-audit, java-spring-api-contract, java-spring-saga-outbox
    - details/ files are not separate index rules
    - Stringify of rule-index.json must not contain BODY_CANARY java-spring-logging-audit, BODY_CANARY java-spring-api-contract, or BODY_CANARY java-spring-saga-outbox

    Update `src/index/precedence.test.ts` real-corpus inventory lock (CONTEXT proof): grow expected winners from 7 to 10 by appending the three new ids to expectedIds:
    - require-mfa
    - java-spring-svc-internal-outbound
    - java-spring-svc-internet-outbound
    - java-spring-inbound-rest
    - java-spring-inbound-kafka
    - java-spring-hex-layering
    - java-spring-ddd-tactical
    - java-spring-logging-audit
    - java-spring-api-contract
    - java-spring-saga-outbox

    Update the inventory comment to state Phase 13+14+15 pack (mfa + nine java-spring domain rules). Keep sort() + deepEqual shape unchanged.

    Do not hand-edit rule-index.json structure; always regenerate from build-index. Do not change selection engine production modules.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node bin/governance.cjs build-index --root aidlc-rules --out rule-index.json &amp;&amp; node -e "const fs=require('fs'); const j=fs.readFileSync('rule-index.json','utf8'); if (/BODY_CANARY java-spring-logging-audit|BODY_CANARY java-spring-api-contract|BODY_CANARY java-spring-saga-outbox/.test(j)) { console.error('canary leak'); process.exit(1);} const i=JSON.parse(j); for (const id of ['java-spring-logging-audit','java-spring-api-contract','java-spring-saga-outbox']) { if (!i.rules.some(r=>r.id===id)) { console.error('missing '+id); process.exit(1);} } const pack=i.rules.filter(r=>String(r.id).startsWith('java-spring')); if (pack.length&lt;9) { console.error('expected at least 9 java-spring rules got', pack.length); process.exit(1);} console.log('index ok', pack.map(r=>r.id).join(','));" &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/index/precedence.test.js</automated>
  </verify>
  <done>
    rule-index.json regenerated with three LOG/API/EVT records, no body canaries, detailPath pointers intact, Phase 13/14 pack ids retained; precedence.test.ts expects exactly 10 real-corpus winners and passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN log-api-evt suite — fix content/tests until npm test passes</name>
  <files>src/select/java-spring-log-api-evt.test.ts, aidlc-rules/domain/java-spring/java-spring-logging-audit.md, aidlc-rules/domain/java-spring/java-spring-api-contract.md, aidlc-rules/domain/java-spring/java-spring-saga-outbox.md, aidlc-rules/domain/java-spring/details/java-spring-logging-audit-detail.md, aidlc-rules/domain/java-spring/details/java-spring-api-contract-detail.md, aidlc-rules/domain/java-spring/details/java-spring-saga-outbox-detail.md</files>
  <behavior>
    - All plan 01 matrix tests pass against production aidlc-rules
    - domains=[] out-of-scope; path-primary and multi-token positives for LOG/API/EVT; bare log/logger/rest negatives
    - docs/test/README/inception excludes; inject summary-only quarantine
    - Full npm test green (Phase 13 pack + Phase 14 hex-ddd + precedence inventory still green)
  </behavior>
  <action>
    Run `npm test` (or targeted `node --test dist-test/select/java-spring-log-api-evt.test.js` after build:test). Drive failures to green by adjusting **content frontmatter/summaries/details** first — not the selection engine.

    Allowed test-file edits: only if canary strings, path fixtures, or assertion wording need alignment with final summaries; do not weaken subscription, bare-needle negatives, docs/test excludes, or inject quarantine assertions.

    If bare log/logger/rest still select rules, remove any short needles that snuck into frontmatter.

    If keyword-only bank terms or unrelated paths select LOG/API/EVT, tighten triggers to CONTEXT multi-token lists and path globs only.

    Confirm no production `src/**/*.ts` outside tests was modified for selection logic. Confirm zero new npm deps. Confirm no CQRS rule file was created. Confirm classification remains advisory on all three.

    Optional CLI smoke (non-blocking if suite green): select with `--domains java-spring`, construction phase, and a logging or openapi path signal.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full npm test green. JAVA-LOG-01, JAVA-API-01, and JAVA-EVT-01 proven by java-spring-log-api-evt suite. Inventory 10 winners. Engine frozen. Zero new npm deps. No CQRS. Pack + index ship all three new rules.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule authors → rule-index.json → select/inject | Malformed or over-broad rules over-inject logging/API/saga guidance into agent context |
| Domain subscription boundary | Unsubscribed consumers must not receive java-spring LOG/API/EVT conventions |
| Content store vs engine src | Style guidance lives only in Markdown; engine remains frozen and vendor-neutral |
| Advisory logging/API guidance vs real enforcement | Markdown cannot guarantee no-PII or OpenAPI drift control — must not claim binding without adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | Unsubscribed domain injection | high | mitigate | scope domain + domains gate; suite proves out-of-scope for LOG/API/EVT |
| T-15-02 | Spoofing | False-positive selection (bare log/rest) | high | mitigate | Multi-token keywords only; bare log/logger/rest negatives in suite; no bare rest in API rule |
| T-15-03 | Information Disclosure | Body leak into index/inject; PII in logs if guidance ignored | high | mitigate | details/ quarantine, one-sentence summaries, canary checks, rebuild gate; rule body forbids PII/secrets in logs (advisory) |
| T-15-04 | Denial of Service | Essay summaries / always-on conventions | medium | mitigate | Non-empty specific triggers; construction-only; docs/test excludes; summary length asserts |
| T-15-05 | Spoofing | Binding theater without adapter; saga cargo-cult | medium | mitigate | classification advisory on all three; no enforcement field; saga when-NOT for single-service ACID in body+detail |
| T-15-SC | Tampering | Package installs | low | accept | Zero new packages this phase |
</threat_model>

<verification>
`npm test` green; buildIndex(aidlc-rules) returns all three LOG/API/EVT domain rules; selection matrices pass; rule-index.json free of body canaries; precedence inventory locked at 10 winners.
</verification>

<success_criteria>
- Construction logging/aop/config Log paths inject correlation/trace, no-PII/secrets, audit-for-mutations guidance
- Construction api/openapi/Resource/web paths inject OpenAPI SoT or generated-and-checked, one versioning policy, error envelope code/message/correlationId
- Construction outbox/saga/messaging paths inject saga/outbox decision including when-NOT (no saga on single-service ACID)
- Unrelated README/non-Java/docs/test/inception and bare log/rest tasks do not select LOG/API/EVT
- rule-index.json rebuilt; inventory 7→10; npm test green; no production engine edits; no new deps; no CQRS
</success_criteria>

<output>
Create `.planning/phases/15-logging-api-contract-saga-decision-rules/15-02-SUMMARY.md` when done
</output>
