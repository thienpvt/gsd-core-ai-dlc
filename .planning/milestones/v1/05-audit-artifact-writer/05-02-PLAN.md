---
phase: 05-audit-artifact-writer
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .claude/skills/aidlc-governance-audit/SKILL.md
  - .gsd/capabilities/aidlc-governance/capability.json
  - src/governance/audit-hook-contract.test.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
requirements_addressed:
  - AUDIT-01
  - AUDIT-02
user_setup: []
must_haves:
  truths:
    - "GSD capability render-hooks at verify:post includes an artifact-only step for aidlc-governance-audit."
    - "Manifest remains step-only for audit output and keeps gates empty."
    - "aidlc-governance-audit skill invokes the Phase 05 writer over persisted selection-state.json and writes GOVERNANCE.md."
    - "aidlc-governance-audit resolves current_phase from `.planning/STATE.md`, pads it to two digits, finds exactly one `.planning/phases/{NN}-*/` directory, and passes explicit `<phaseDir>/GOVERNANCE.md` to the writer."
    - "Hook contract tests prove the verify:post step renders and carries no scan, approval, ship, or enforcement adapter behavior."
  artifacts:
    - path: ".claude/skills/aidlc-governance-audit/SKILL.md"
      provides: "Thin verify:post skill that marshals projectRoot/outputPath into the audit writer"
    - path: ".gsd/capabilities/aidlc-governance/capability.json"
      provides: "verify:post step registration for aidlc-governance-audit"
    - path: "src/governance/audit-hook-contract.test.ts"
      provides: "Manifest/skill contract tests for artifact-only verify:post behavior"
  key_links:
    - from: ".gsd/capabilities/aidlc-governance/capability.json"
      to: ".claude/skills/aidlc-governance-audit/SKILL.md"
      via: "steps[].ref.skill"
      pattern: "aidlc-governance-audit"
    - from: ".claude/skills/aidlc-governance-audit/SKILL.md"
      to: "src/governance/audit-artifact.ts"
      via: "runs dist/governance/audit-artifact.js after build with `<projectRoot> <phaseDir>/GOVERNANCE.md`"
      pattern: "audit-artifact"
    - from: "src/governance/audit-hook-contract.test.ts"
      to: ".gsd/capabilities/aidlc-governance/capability.json"
      via: "test reads manifest JSON and asserts verify:post step-only contract"
      pattern: "verify:post"
---

<objective>
Wire the Phase 05 audit writer into the GSD capability surface as an artifact-only verify:post step.

Purpose: satisfy the ROADMAP verify:post acceptance path for AUDIT-01 and AUDIT-02 without adding enforcement adapters, tool-runner ingestion, approval workflow, ship hooks, or gates.
Output: `.claude/skills/aidlc-governance-audit/SKILL.md`, manifest update, and contract tests.
</objective>

<execution_context>
@C:/Users/thien/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thien/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-audit-artifact-writer/05-CONTEXT.md
@.planning/phases/05-audit-artifact-writer/05-RESEARCH.md
@.planning/phases/04-gsd-capability-integration-persistence/04-03-SUMMARY.md
@.planning/phases/05-audit-artifact-writer/05-01-SUMMARY.md
@src/governance/audit-artifact.ts
@.gsd/capabilities/aidlc-governance/capability.json
@.claude/skills/aidlc-governance-discuss/SKILL.md
@.claude/skills/aidlc-governance-execute/SKILL.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Produce a machine-derived per-task audit of rules applied and skipped | 05-01, 05-02 | COVERED | 05-01 builds writer; 05-02 wires verify:post artifact step.
REQ | AUDIT-01 | Per-task audit artifact records rules applied from actual selector output, not model narration | 05-01, 05-02 | COVERED | 05-02 makes writer reachable from GSD verify:post.
REQ | AUDIT-02 | Audit artifact records skipped rules with reason from fixed public enum | 05-01 | COVERED | 05-02 preserves writer output and does not add alternate reason logic.
RESEARCH | Artifact-only verify:post | Add skill plus manifest step with `produces: ["GOVERNANCE.md"]`, consumes persisted state, no gates | 05-02 | COVERED | Task 1 updates manifest/skill; Task 2 tests contract and render-hooks.
RESEARCH | No package installs | Use existing Node test tooling and GSD runtime only | 05-02 | COVERED | Package files must not change.
RESEARCH | Avoid path confusion | Skill resolves current phase from `.planning/STATE.md`, pads it to two digits, finds exactly one `.planning/phases/{NN}-*/` directory, and passes explicit `<phaseDir>/GOVERNANCE.md` output path | 05-02 | COVERED | Skill instructions match resolved research question and 05-01 direct runner validation.
CONTEXT | Machine-Derived Artifact | Artifact must depend on persisted machine data only | 05-01, 05-02 | COVERED | Skill invokes the writer; it does not narrate or synthesize audit facts.
CONTEXT | Audit Reason Enum | Public enum and normalization live in the writer, not the skill | 05-01, 05-02 | COVERED | Skill has no duplicate reason logic.
CONTEXT | Reproducibility | Same persisted state renders identical applied/skipped records | 05-01 | COVERED | Hook delegates to deterministic writer.
CONTEXT | Hook Boundary | verify:post hook is artifact generation only | 05-02 | COVERED | Manifest keeps `gates: []` and adds only a `steps` entry.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact-only verify:post capability step</name>
  <files>.claude/skills/aidlc-governance-audit/SKILL.md, .gsd/capabilities/aidlc-governance/capability.json</files>
  <action>Create `.claude/skills/aidlc-governance-audit/SKILL.md` following the existing discuss/execute skill style: frontmatter name `aidlc-governance-audit`, description for verify:post artifact generation, and steps that confirm `.planning/governance/selection-state.json`, read `current_phase` from `.planning/STATE.md`, pad the phase number to two digits, find exactly one `.planning/phases/{NN}-*/` directory, run `node dist/governance/audit-artifact.js <projectRoot> <phaseDir>/GOVERNANCE.md`, and propagate failures. Update `.gsd/capabilities/aidlc-governance/capability.json`: add `aidlc-governance-audit` to `skills`; append one `steps` entry with `point: "verify:post"`, `ref.skill: "aidlc-governance-audit"`, `produces: ["GOVERNANCE.md"]`, `consumes: [".planning/governance/selection-state.json"]`, `when: "governance.enabled"`, and `onError: "halt"`. Keep `gates` as an empty array and do not add ship hooks, approval fields, scan runners, policy adapters, or package dependencies. Commit expectation: `feat(05-02): add audit verify-post capability step`.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('.gsd/capabilities/aidlc-governance/capability.json','utf8')); if(!m.skills.includes('aidlc-governance-audit')) process.exit(1); const step=m.steps.find(s=>s.point==='verify:post'&&s.ref&&s.ref.skill==='aidlc-governance-audit'); if(!step) process.exit(1); if(JSON.stringify(step.produces)!==JSON.stringify(['GOVERNANCE.md'])) process.exit(1); if(!Array.isArray(m.gates)||m.gates.length!==0) process.exit(1);"</automated>
  </verify>
  <done>Manifest registers exactly one artifact-producing verify:post step for `aidlc-governance-audit`; `gates` remains empty; skill delegates to the writer and does not duplicate audit mapping logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add manifest and render-hooks contract tests</name>
  <files>src/governance/audit-hook-contract.test.ts</files>
  <action>Create `src/governance/audit-hook-contract.test.ts` using `node:test` and `node:assert/strict`. Test the manifest JSON contains one `verify:post` step for `aidlc-governance-audit`, consumes `.planning/governance/selection-state.json`, produces `GOVERNANCE.md`, and has no gates. Test the audit skill file names `selection-state.json`, `.planning/STATE.md`, `current_phase`, `.planning/phases/{NN}-*/`, `GOVERNANCE.md`, and `dist/governance/audit-artifact.js`, while avoiding duplicated reason-mapping code. Include an integration-style test that shells `node C:/Users/thien/.codex/gsd-core/bin/gsd-tools.cjs loop render-hooks verify:post --raw --config-dir C:/Users/thien/.codex` when that runtime file exists, parses JSON, and asserts an active hook or step references `aidlc-governance-audit`; skip with `t.skip()` if the local runtime file is absent. Do not edit `src/governance/audit-artifact.test.ts` unless needed for compile imports. Commit expectation: `test(05-02): cover audit verify-post contract`.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/governance/audit-hook-contract.test.js</automated>
    <automated>node C:/Users/thien/.codex/gsd-core/bin/gsd-tools.cjs loop render-hooks verify:post --raw --config-dir C:/Users/thien/.codex</automated>
  </verify>
  <done>Contract test passes; render-hooks output includes `aidlc-governance-audit`; no package dependency files are modified.</done>
</task>

<task type="auto">
  <name>Task 3: Run full phase verification</name>
  <files>src/governance/audit-hook-contract.test.ts, .gsd/capabilities/aidlc-governance/capability.json, .claude/skills/aidlc-governance-audit/SKILL.md</files>
  <action>Run the full suite and focused hook checks after Task 1 and Task 2. Confirm the manifest remains step-only by parsing JSON rather than relying on manual inspection. Confirm package dependency files are untouched. Commit any verification-only test fix in the Task 2 test commit; no separate docs-only commit is needed unless skill wording changes after tests pass.</action>
  <verify>
    <automated>npm test</automated>
    <automated>node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('.gsd/capabilities/aidlc-governance/capability.json','utf8')); if(!Array.isArray(m.gates)||m.gates.length!==0) process.exit(1); const matches=m.steps.filter(s=>s.point==='verify:post'&&s.ref&&s.ref.skill==='aidlc-governance-audit'); if(matches.length!==1) process.exit(1);"</automated>
  </verify>
  <done>Full test suite passes, focused contract checks pass, render-hooks shows audit step, and manifest has no gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD loop -> project skill | `verify:post` dispatch invokes a skill by manifest reference; incorrect manifest shape can route no artifact or wrong behavior. |
| skill -> Node writer | Skill resolves current planning phase output path and invokes built writer over persisted local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-R1 | Repudiation | `.gsd/capabilities/aidlc-governance/capability.json` | high | mitigate | Add render-hooks and manifest contract tests proving verify:post references `aidlc-governance-audit` and produces `GOVERNANCE.md`. |
| T-05-02-T1 | Tampering | `.claude/skills/aidlc-governance-audit/SKILL.md` | high | mitigate | Skill must require persisted selection state, resolve `current_phase` from `.planning/STATE.md`, pad it to two digits, resolve exactly one `.planning/phases/{NN}-*/` directory, invoke the built writer with explicit `<phaseDir>/GOVERNANCE.md`, and propagate failures rather than writing narrative content. |
| T-05-02-E1 | Elevation of Privilege | manifest gates/hooks | medium | mitigate | Keep `gates` empty and add only one `steps` entry at `verify:post`; contract test asserts no gates and no ship hook for this capability. |
| T-05-02-T2 | Tampering | output path resolution | medium | mitigate | Skill resolves output to `<phaseDir>/GOVERNANCE.md` from `.planning/STATE.md` `current_phase`; writer from 05-01 validates direct script output path basename and containment. |
</threat_model>

<verification>
Plan-level verification:
- `npm run build:test && node --test dist-test/governance/audit-hook-contract.test.js`
- `node C:/Users/thien/.codex/gsd-core/bin/gsd-tools.cjs loop render-hooks verify:post --raw --config-dir C:/Users/thien/.codex`
- `npm test`
- Confirm no dependency changes: `git diff --name-only -- package.json package-lock.json npm-shrinkwrap.json` prints no files.
</verification>

<success_criteria>
- `verify:post` render-hooks output references `aidlc-governance-audit`.
- Capability manifest lists `aidlc-governance-audit` in `skills` and one artifact-only `verify:post` step in `steps`.
- Capability manifest keeps `gates: []`.
- Audit skill resolves `current_phase` from `.planning/STATE.md`, pads to two digits, finds exactly one `.planning/phases/{NN}-*/` directory, and passes `<phaseDir>/GOVERNANCE.md` as explicit output path.
- Audit skill writes `GOVERNANCE.md` through the Phase 05 writer and does not duplicate mapping or reason normalization.
- No package dependencies are added.
</success_criteria>

<output>
Create `.planning/phases/05-audit-artifact-writer/05-02-SUMMARY.md` when done.
</output>
