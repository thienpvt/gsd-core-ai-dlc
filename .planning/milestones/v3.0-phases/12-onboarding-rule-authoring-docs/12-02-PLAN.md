---
phase: 12-onboarding-rule-authoring-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/rule-authoring.md
  - README.md
autonomous: true
requirements: [DOC-03]
user_setup: []
must_haves:
  truths:
    - docs/rule-authoring.md documents all 7 frontmatter fields (id, scope, triggers, phases, severity, summary, detailPath) plus classification (advisory|binding) with the exact enum values from src/schema/frontmatter.schema.json. require-mfa.md is the 6-base-field + classification example (NO detailPath); a SEPARATE complete example (billing-review) demonstrates all 7 fields including detailPath.
    - docs/rule-authoring.md documents the 3 scope directories (enterprise, domain, project) with project-wins precedence
    - docs/rule-authoring.md documents all 3 trigger axes (keywords array, taskType enum, paths picomatch globs) with examples
    - docs/rule-authoring.md contains a runnable verify-the-rule-fires loop using a TEMP specifically-triggered rule (billing-review, phases:[construction], triggers:{keywords:["billing"]}, detailPath set): build-index, positive select (matching keyword → rule present), negative select (wrong keyword → rule absent), negative select (wrong phase → rule absent), eval for corpus regression. require-mfa is NOT used for the negative case (always-in-phase + common → fires for every signal, negative test impossible).
    - README.md contains a Documentation section linking to all 3 docs (docs/onboarding.md, docs/governance-workflow.md, docs/rule-authoring.md)
    - docs/rule-authoring.md ends with a Next cross-reference (closes the install-operate-author loop)
  artifacts:
    - docs/rule-authoring.md
    - README.md
  key_links:
    - README.md links to docs/onboarding.md, docs/governance-workflow.md, docs/rule-authoring.md
    - docs/rule-authoring.md Next-link to docs/onboarding.md (closes the loop)
---

<objective>
Write the rule-authoring guide (DOC-03) and the root README.md with a Documentation section. The authoring guide lets a rule author write, integrate, and verify a new governance rule by following docs alone. The README.md provides the single root entry point that links all 3 docs and makes them discoverable.

Purpose: DOC-03 proves a rule author can write a Markdown+frontmatter rule, place it in the correct scope, declare binding-vs-advisory, and verify it fires. README.md makes the 3 docs discoverable from the repo root.
Output: docs/rule-authoring.md, README.md
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-rule-authoring-docs/12-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@package.json
@src/cli/index.ts
@src/cli/commands/build-index.ts
@src/cli/commands/select.ts
@src/cli/commands/eval.ts
@src/select/eval-cli.ts
@src/schema/frontmatter.schema.json
@src/schema/task-signal.schema.json
@src/types.ts
@aidlc-rules/enterprise/require-mfa.md
@test/fixtures/eval/eval-rules/enterprise/secrets-management.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/rule-authoring.md (DOC-03)</name>
  <files>docs/rule-authoring.md</files>
  <read_first>
    - src/schema/frontmatter.schema.json — the authoritative authoring contract: required fields (lines 8-16), id pattern (line 20), scope enum (lines 23-26), triggers object with taskType/keywords/paths/exclude (lines 28-47), phases enum (lines 49-57), severity enum (lines 59-63), summary (lines 65-68), classification enum (lines 70-73), detailPath (lines 75-79), enforcement (lines 81-85), binding if/then rule (lines 109-117), taskType enum values (lines 88-106)
    - src/types.ts — Frontmatter interface (lines 57-69), Triggers interface (lines 49-54), Scope/Severity/Phase/TaskType/Classification types (lines 12-35)
    - aidlc-rules/enterprise/require-mfa.md — canonical example for the 6 BASE fields (id, scope, triggers, phases, severity, summary) + classification: advisory. NOTE: require-mfa has NO detailPath field (it is a summary-only rule), so it demonstrates 6 base fields + classification, NOT all 7. Use it as the frontmatter-format reference for those 6 fields.
    - src/cli/index.ts — CLI dispatcher, exact usage text + subcommand list (lines 31-36)
    - src/cli/commands/build-index.ts — build-index command for the verify loop
    - src/cli/commands/select.ts — select command for the verify loop (positive + negative signal); VALID_PHASES line 34 (inception|construction|operations|common); selector treats common as applying to every phase (inPhase, line 159) and empty triggers as always-in-phase (isEmptyTriggers, lines 144-149)
    - src/cli/commands/eval.ts and src/select/eval-cli.ts — eval command for corpus regression; exit codes 0 pass, 2 critical-recall regression, 3 parse/load error (eval-cli.ts lines 261-307)
    - src/schema/task-signal.schema.json — TaskSignal format for the verify-loop signals
    - .planning/codebase/STRUCTURE.md — aidlc-rules/ directory layout: enterprise/, domain/<name>/, project/ (lines 100-104)
    - test/fixtures/eval/eval-rules/enterprise/secrets-management.md — second example rule showing triggers with keywords (for variety beyond require-mfa's empty triggers)
    - src/cli/commands/rule-detail.ts — rule-detail output behavior: summary-only branch (lines 79-86) for rules WITHOUT detailPath prints summary + "no separate detail file" message; detailPath branch (lines 98-105) prints the Markdown body
  </read_first>
  <action>
    Create `docs/rule-authoring.md` with these sections. Use exact field names, enum values, and constraints from `src/schema/frontmatter.schema.json` — do NOT invent fields or values.

    1. **Title + intro**: "GSD Governance Overlay — Rule Authoring Guide" and 1-2 sentence purpose (write, integrate, and verify a new governance rule).

    2. **## Frontmatter Fields**: Document all 7 fields (6 base required + 1 optional) + classification + the optional enforcement field. Use a table with columns: Field, Type, Required, Description. Source: `src/schema/frontmatter.schema.json` required array (lines 8-16) and properties (lines 17-85).
       - `id` (string, required, pattern `^[a-z0-9]+(?:-[a-z0-9]+)*$` — kebab-case slug, globally unique)
       - `scope` (string enum, required, one of enterprise|domain|project)
       - `triggers` (object, required — empty `{}` is valid and means always-in-phase)
       - `phases` (string array, required, minItems 1, uniqueItems, enum: inception|construction|operations|common)
       - `severity` (string enum, required, one of critical|high|medium|low)
       - `summary` (string, required, minLength 1 — one-line injected summary)
       - `classification` (string enum, required, one of advisory|binding — note: this is the frontmatter field that maps to the JSON Schema `x-binding` annotation in the enforcement boundary; rule authors set it here, not in the schema)
       - `detailPath` (string, OPTIONAL, minLength 1 — relative to the declaring rule file; absent on summary-only rules like require-mfa)
       - `enforcement` (string, optional but REQUIRED when classification is binding — free-form contract id like `semgrep:no-eval`, `ci:exit-code`, `human-approval`; enforced by the `allOf` if/then rule in the schema, lines 109-117)

    3. **## Scope Placement**: Document the 3 scope directories and precedence:
       - `aidlc-rules/enterprise/` — applies to all projects, all domains
       - `aidlc-rules/domain/<name>/` — applies to a subscribed domain only (subscription gated by the `--domains` select flag)
       - `aidlc-rules/project/` — applies to this project only; on an id collision across scopes, project wins (precedence: project > domain > enterprise). Source: `src/types.ts` Scope type (line 12), `.planning/codebase/STRUCTURE.md` aidlc-rules directory purpose (lines 100-104).
       - State: the scope frontmatter value MUST match the declaring directory (a post-parse check enforces this).

    4. **## Trigger Axes**: Document all 3 positive axes + the exclude axis with examples. Source: `src/schema/frontmatter.schema.json` triggers definition (lines 28-47) and `src/types.ts` Triggers interface (lines 49-54).
       - **keywords** (string array, uniqueItems): matched as case-insensitive substring of any signal keyword. Example: `keywords: ["auth", "mfa"]` matches a signal with `keywords: ["authentication"]`.
       - **taskType** (string array, uniqueItems, enum: feature|bugfix|refactor|docs|test|infra|security|data): matched by enum equality against the signal's taskType. Example: `taskType: ["security", "infra"]`.
       - **paths** (string array, uniqueItems): matched as picomatch globs against the signal's paths, with `dot: true` (includes dot-prefixed paths like `.github/`, `.env`). Example: `paths: ["src/auth/**", ".env*"]`.
       - **exclude** (object with taskType/keywords/paths): negative selectors — exclusion wins over any positive match. Example: `exclude: { paths: ["**/*.test.ts"] }`.
       - State: axes OR-combine — any positive axis match selects the rule; an empty `triggers: {}` means always-in-phase (the escape hatch a critical rule uses).

    5. **## Worked Examples**: TWO complete authored rules covering all 7 fields.
       - **Example A — 6 base fields + classification (summary-only, no detailPath):** Use `aidlc-rules/enterprise/require-mfa.md` as the reference. Show its frontmatter (id, scope, triggers:{}, phases:[common], severity:critical, summary, classification:advisory — 6 base fields + classification, NO detailPath). Show the Markdown body with a `## Rule <PREFIX-NN>: <Title>` heading and a `### Verification` section. Explain: `triggers: {}` means always-in-phase; `classification: advisory` vs `binding`; a summary-only rule has no detailPath and `rule-detail` prints its summary + a no-detail message.
       - **Example B — all 7 fields including detailPath (specifically-triggered rule):** Author a NEW sample rule in a TEMP pack for the verify-the-rule-fires loop (see step 6). Frontmatter: `id: billing-review`, `scope: enterprise`, `triggers: { keywords: ["billing"] }`, `phases: [construction]`, `severity: high`, `summary: <one-line>`, `classification: advisory`, `detailPath: ./billing-review-detail.md` (all 7 fields). This is the canonical example demonstrating detailPath. Show the frontmatter + the referenced detail file + the body.
       - Show a binding example contrast: a rule with `classification: binding` MUST also declare `enforcement: <contract-id>` (the schema's if/then rule, `src/schema/frontmatter.schema.json` lines 109-117). State: `require-mfa.md` does NOT show all 7 fields — it is the 6-base-field + classification reference; Example B shows all 7 including detailPath.

    6. **## Verify the Rule Fires**: A concrete, copy-pasteable loop proving a SPECIFICALLY-TRIGGERED rule fires for its intended task/phase and NOT otherwise. CRITICAL: do NOT use `require-mfa` for the negative case — require-mfa has `triggers: {}` (always-in-phase) + `phases: [common]`, and the selector treats empty triggers as always-in-phase + common as applying to every phase, so it fires for EVERY signal and the negative test CANNOT work with it. Use a TEMP rule pack + temp index containing the specifically-triggered sample rule from Example B (`billing-review`, `phases: [construction]`, `triggers: { keywords: ["billing"] }`, `detailPath: ./billing-review-detail.md`). Use fenced code blocks for each step:
       - Step 1: Create a temp rule pack dir (e.g. `/tmp/verify-rules/enterprise/billing-review.md` + `/tmp/verify-rules/enterprise/billing-review-detail.md`) with the Example B frontmatter + body.
       - Step 2: `node bin/governance.cjs build-index --root /tmp/verify-rules --out /tmp/verify-rules/rule-index.json` — build the temp index.
       - Step 3 (positive): Create `match-signal.json` with `{ "taskType": "feature", "keywords": ["billing"], "paths": [] }`. Run `node bin/governance.cjs select --phase construction --index /tmp/verify-rules/rule-index.json --input match-signal.json`. Assert: `billing-review` appears in `selected[]` (matched via keywords axis).
       - Step 4 (negative — wrong keyword): Create `nomatch-signal.json` with `{ "taskType": "feature", "keywords": ["unrelated"], "paths": [] }`. Run `node bin/governance.cjs select --phase construction --index /tmp/verify-rules/rule-index.json --input nomatch-signal.json`. Assert: `billing-review` does NOT appear in `selected[]` (it appears in `skipped[]` with reason `out-of-scope-by-trigger`).
       - Step 5 (negative — wrong phase): Run `node bin/governance.cjs select --phase inception --index /tmp/verify-rules/rule-index.json --input match-signal.json`. Assert: `billing-review` does NOT appear in `selected[]` (it appears in `skipped[]` with reason `out-of-phase`).
       - Step 6 (corpus regression): `node bin/governance.cjs eval 12` — runs the labeled eval corpus (uses the shipped `test/fixtures/eval/` corpus, NOT the temp pack). Document exit codes: 0 pass, 2 critical-recall regression, 3 parse/load error. Source: `src/select/eval-cli.ts` (lines 261-307).
       - State: `require-mfa` can still be USED in the guide as a frontmatter-format example (Example A), but the verify-the-rule-fires dry-run MUST use the temp triggered rule because a negative selection against require-mfa is impossible (always-in-phase + common).

    7. **Next**: End with a cross-reference line: "Next: [Onboarding Guide](onboarding.md)" — closes the install-operate-author loop back to DOC-01.

    Use exact field names, enum values, and constraints from the schema. Do NOT invent fields, enum values, or exit codes.
  </action>
  <verify>
    <automated><![CDATA[
      set -e
      cd "$(git rev-parse --show-toplevel)"
      npm run build 2>&1 | tail -1
      node bin/governance.cjs build-index
      echo '{"taskType":"feature","keywords":["auth"],"paths":[]}' > match-signal.json
      POS=$(node bin/governance.cjs select --phase inception --input match-signal.json)
      # require-mfa has phases:[common]+triggers:{} → always-in-phase, so it lands in selected[] for inception.
      echo "$POS" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const o=JSON.parse(d);if(!o.selected.map(r=>r.id).includes('require-mfa')){console.error('FAIL: require-mfa not in selected[] for inception');process.exit(1)}console.log('PASS positive: require-mfa in selected[]')})"
      TMP_RULES=$(mktemp -d)
      mkdir -p "$TMP_RULES/enterprise"
      cat > "$TMP_RULES/enterprise/billing-review.md" <<'RULE'
---
id: billing-review
scope: enterprise
triggers:
  keywords:
    - billing
phases:
  - construction
severity: high
summary: Billing changes require cost-impact review.
classification: advisory
detailPath: ./billing-review-detail.md
---
## Rule ENT-BILL: Billing Change Review
All billing-flow changes must be reviewed for cost impact.
### Verification
Confirm the cost-impact analysis is attached before merge.
RULE
cat > "$TMP_RULES/enterprise/billing-review-detail.md" <<'DETAIL'
## Rule ENT-BILL: Billing Change Review (detail)
Billing changes touch pricing, invoicing, or payment flows.
### Verification
Cost-impact analysis attached; finance sign-off recorded.
DETAIL
      node bin/governance.cjs build-index --root "$TMP_RULES" --out "$TMP_RULES/rule-index.json"
      echo '{"taskType":"feature","keywords":["billing"],"paths":[]}' > bill-match.json
      echo '{"taskType":"feature","keywords":["unrelated"],"paths":[]}' > bill-nomatch.json
      POSB=$(node bin/governance.cjs select --phase construction --index "$TMP_RULES/rule-index.json" --input bill-match.json)
      echo "$POSB" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const o=JSON.parse(d);if(!o.selected.map(r=>r.id).includes('billing-review')){console.error('FAIL: billing-review not in selected[] for matching keyword+phase');process.exit(1)}console.log('PASS positive-billing: billing-review in selected[]')})"
      NEGB=$(node bin/governance.cjs select --phase construction --index "$TMP_RULES/rule-index.json" --input bill-nomatch.json)
      # Selector emits non-matching rules in skipped[] with the SAME id — grep on whole JSON would false-fail.
      # Assert selected[] excludes billing-review AND skipped[] contains it with reason out-of-scope-by-trigger.
      echo "$NEGB" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const o=JSON.parse(d);const sel=o.selected.map(r=>r.id);const skip=o.skipped.map(r=>r.id);if(sel.includes('billing-review')){console.error('FAIL: billing-review in selected[] for unrelated keyword');process.exit(1)}if(!skip.includes('billing-review')){console.error('FAIL: billing-review not in skipped[]');process.exit(1)}console.log('PASS negative-keyword: billing-review in skipped[], not selected[]')})"
      NEGBPHASE=$(node bin/governance.cjs select --phase inception --index "$TMP_RULES/rule-index.json" --input bill-match.json)
      echo "$NEGBPHASE" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const o=JSON.parse(d);const sel=o.selected.map(r=>r.id);const skip=o.skipped.map(r=>r.id);if(sel.includes('billing-review')){console.error('FAIL: billing-review in selected[] for inception phase');process.exit(1)}if(!skip.includes('billing-review')){console.error('FAIL: billing-review not in skipped[] for inception');process.exit(1)}console.log('PASS negative-phase: billing-review in skipped[], not selected[]')})"
      test "$(grep -c '## Frontmatter Fields' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'classification: advisory' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'classification: binding' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'detailPath' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'enterprise' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'domain' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'project' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'keywords' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'taskType' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'paths' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'governance build-index' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'governance select --phase' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'governance eval' docs/rule-authoring.md)" -ge 1
      test "$(grep -c 'onboarding.md' docs/rule-authoring.md)" -ge 1
      rm -f match-signal.json bill-match.json bill-nomatch.json
      rm -rf "$TMP_RULES"
      echo "DOC-03 verify passed"
    ]]></automated>
  </verify>
  <acceptance_criteria>
    - docs/rule-authoring.md contains heading '## Frontmatter Fields'
    - docs/rule-authoring.md contains 'classification: advisory' and 'classification: binding'
    - docs/rule-authoring.md documents 'detailPath' (the optional 7th field) with a complete example showing all 7 fields (Example B: billing-review)
    - docs/rule-authoring.md documents require-mfa as the 6-base-field + classification example (Example A), NOT as a 7-field example
    - docs/rule-authoring.md documents all 3 scopes: 'enterprise', 'domain', 'project'
    - docs/rule-authoring.md documents all 3 trigger axes: 'keywords', 'taskType', 'paths'
    - docs/rule-authoring.md contains 'governance build-index' and 'governance select --phase' and 'governance eval' (verify loop documented)
    - docs/rule-authoring.md contains 'onboarding.md' (Next cross-reference closes the loop)
    - running the documented positive verify step (temp triggered rule billing-review, matching keyword signal) returns billing-review in selected[]
    - running the documented negative verify step (wrong keyword) does NOT return billing-review in selected[]
    - running the documented negative verify step (wrong phase, --phase inception) does NOT return billing-review in selected[]
  </acceptance_criteria>
  <done>docs/rule-authoring.md exists with Frontmatter Fields, Scope Placement, Trigger Axes, two Worked Examples (6-base-field require-mfa + 7-field billing-review with detailPath), and Verify-the-Rule-Fires (temp triggered rule, positive + two negative cases) sections; the documented verify loop runs as written and produces the expected results; Next link to onboarding.md present.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with Documentation section</name>
  <files>README.md</files>
  <read_first>
    - package.json — name (@opengsd/gsd-aidlc-overlay), version (0.1.0), description (line 4), license (MIT), engines (Node>=22, npm>=10), bin (governance), files (dist, bin, aidlc-rules), scripts (build, test, eval)
    - src/cli/index.ts — CLI dispatcher, exact usage text + 5 subcommand names (lines 31-36) for the CLI table
    - .planning/PROJECT.md — project constraints (anti-context-bloat, tool-agnostic enforcement, overlay-not-fork, auditability)
    - .claude/CLAUDE.md — project description and core value statement (lines 4-10)
    - .planning/codebase/STRUCTURE.md — repo layout overview (lines 1-54)
    - .planning/codebase/INTEGRATIONS.md — GSD capability integration overview (lines 7-13)
    - .gsd/capabilities/aidlc-governance/capability.json — capability title and description (lines 5-6)
  </read_first>
  <action>
    Create `README.md` at the repo root (it does not exist yet). Include these sections:

    1. **Title + description**: `# GSD Governance Overlay (AI-DLC × GSD Core)` and 1-2 sentence description (source: `package.json` description line 4 and `.claude/CLAUDE.md` core value, lines 4-10).

    2. **## Overview**: 3-4 sentence paragraph explaining what the overlay does: layers AI-DLC-style enterprise SDLC governance onto GSD Core's long-running development runtime, without polluting the context window. The rule selection engine injects only the relevant AI-DLC rule summaries for the current task and phase. Source: `.claude/CLAUDE.md` project description and core value.

    3. **## Installation**: Brief install steps: `npm install @opengsd/gsd-aidlc-overlay`, `npm run build` (for in-repo development), Node>=22 / npm>=10 required. Reference `docs/onboarding.md` for the full guide.

    4. **## Documentation**: A section with a numbered or bulleted list linking to all 3 docs in the install→operate→author flow:
       - [Onboarding Guide](docs/onboarding.md) — install, consent, first-run smoke check
       - [Governance Workflow Guide](docs/governance-workflow.md) — all 5 CLI commands + gate chain
       - [Rule Authoring Guide](docs/rule-authoring.md) — write, integrate, and verify governance rules
       Each link uses a relative path from the repo root.

    5. **## CLI**: Brief table of the `governance` binary and its 5 subcommands (build-index, select, inject, rule-detail, eval). Reference `docs/governance-workflow.md` for full command documentation. Source: `package.json` bin field and `src/cli/index.ts` usage block (lines 31-36).

    6. **## Development**: Brief dev setup: `npm install`, `npm run build`, `npm test`. Source: `package.json` scripts (lines 20-25).

    Keep the README concise — it's a landing page, not a full doc. The 3 linked docs carry the detail.
  </action>
  <verify>
    <automated><![CDATA[
      set -e
      cd "$(git rev-parse --show-toplevel)"
      test -f README.md
      test "$(grep -c '## Documentation' README.md)" -ge 1
      test "$(grep -c 'docs/onboarding.md' README.md)" -ge 1
      test "$(grep -c 'docs/governance-workflow.md' README.md)" -ge 1
      test "$(grep -c 'docs/rule-authoring.md' README.md)" -ge 1
      echo "README verify passed"
    ]]></automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - README.md contains heading '## Documentation'
    - README.md contains links to all 3 docs: 'docs/onboarding.md', 'docs/governance-workflow.md', 'docs/rule-authoring.md'
  </acceptance_criteria>
  <done>README.md exists at repo root with a Documentation section linking all 3 docs; the 3 docs are discoverable from the repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs-to-schema | Authoring guide describes the frontmatter schema; drift misleads rule authors into writing invalid rules |
| docs-to-CLI | Verify loop documents CLI commands; drift causes verify failures |
| README-to-docs | README links must resolve to existing doc files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | docs/rule-authoring.md | medium | mitigate | Dry-run the verify loop (build-index + positive select + two negative selects with temp triggered rule billing-review) in <verify>; grep assertions confirm all field names and enum values match the schema; require-mfa NOT used for negative case (always-in-phase makes it un-negatable) |
| T-12-04 | Tampering | README.md | low | mitigate | Grep assertions confirm all 3 doc links present; links use relative paths from repo root |
</threat_model>

<verification>
1. `npm run build` succeeds
2. `node bin/governance.cjs build-index` succeeds
3. Positive select (temp billing-review rule, matching keyword signal, --phase construction) returns billing-review in selected[]
4. Negative select (wrong keyword) does NOT return billing-review in selected[]
5. Negative select (wrong phase, --phase inception) does NOT return billing-review in selected[]
6. docs/rule-authoring.md contains all 7 frontmatter fields + classification + all 3 scopes + all 3 trigger axes + verify loop
7. docs/rule-authoring.md uses require-mfa as the 6-base-field example (Example A) and billing-review as the 7-field example with detailPath (Example B)
8. README.md exists with Documentation section linking all 3 docs
9. docs/rule-authoring.md cross-references onboarding.md (closes the loop)
</verification>

<success_criteria>
- docs/rule-authoring.md is followable: a rule author can write, integrate, and verify a new rule by following the doc alone
- The documented verify loop (temp triggered rule: positive select + two negative selects) runs as written and produces expected results — require-mfa is NOT used for the negative case (always-in-phase makes it un-negatable)
- docs/rule-authoring.md shows require-mfa as the 6-base-field example (no detailPath) AND a separate 7-field example with detailPath (billing-review)
- README.md makes the 3 docs discoverable from the repo root
</success_criteria>

<output>
Create `.planning/phases/12-onboarding-rule-authoring-docs/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- docs/onboarding.md (created — DOC-01, Plan 01)
- docs/governance-workflow.md (created — DOC-02, Plan 01)
- docs/rule-authoring.md (created — DOC-03, this plan)
- README.md (created — cross-cutting, this plan)