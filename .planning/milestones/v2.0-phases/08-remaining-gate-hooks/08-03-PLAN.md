---
phase: 08-remaining-gate-hooks
plan: 03
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/governance/verify-gate-hook.ts
  - src/governance/verify-gate-hook.test.ts
autonomous: true
requirements:
  - GATE-04
requirements_addressed:
  - GATE-04
must_haves:
  truths:
    - "Verify evidence routes through runAdapter(adapter, request) per D-04."
    - "Production verify wiring uses existing ADAPTERS no-op registry, with ECHO_ADAPTERS reserved for tests/fixtures per D-05."
    - "Verify gate writes a GateRequest with gateId verify, a validated GateResult, and tested per-rule pass/fail/waived status derivation from request.rules plus result/findings per D-06."
  artifacts:
    - path: "src/governance/verify-gate-hook.ts"
      provides: "verify:post adapter evidence collection"
      exports: ["VerifyGateHookArgs", "VerifyGateHookResult", "RuleGateStatus", "deriveRuleGateStatuses", "verifyGateHook"]
    - path: "src/governance/verify-gate-hook.test.ts"
      provides: "Wave 0 GATE-04 adapter-flow coverage"
  key_links:
    - from: "src/governance/verify-gate-hook.ts"
      to: "src/enforcement/run-adapter.ts"
      via: "verifyGateHook calls runAdapter for adapter evidence"
      pattern: "runAdapter\\("
    - from: "src/governance/verify-gate-hook.ts"
      to: "src/governance/gate-evidence-store.ts"
      via: "verifyGateHook writes validated verify evidence"
      pattern: "writeGateEvidence\\("
---

<objective>
Implement the verify gate hook for GATE-04.

Purpose: Verification evidence must cross the Phase 7 adapter-output validation boundary before it becomes durable governance state.
Output: `verifyGateHook(args)` plus tests proving D-04, D-05, D-06.
</objective>

<execution_context>
@C:/Users/thienpv/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thienpv/.codex/gsd-core/templates/summary.md
@C:/Users/thienpv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-remaining-gate-hooks/08-CONTEXT.md
@.planning/phases/08-remaining-gate-hooks/08-RESEARCH.md
@.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
@.planning/phases/08-remaining-gate-hooks/08-PATTERNS.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-03-SUMMARY.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-04-SUMMARY.md
@src/governance/state-store.ts
@src/governance/gate-evidence-store.ts
@src/enforcement/adapters.ts
@src/enforcement/run-adapter.ts
@src/enforcement/types.ts
@src/enforcement/run-adapter.test.ts
</context>

## Artifacts This Phase Produces

Files and symbols are listed in `08-01-PLAN.md`; this plan owns `src/governance/verify-gate-hook.ts`, `src/governance/verify-gate-hook.test.ts`, `VerifyGateHookArgs`, `VerifyGateHookResult`, and `verifyGateHook(args)`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for verify adapter flow</name>
  <files>src/governance/verify-gate-hook.test.ts</files>
  <read_first>
    @src/enforcement/run-adapter.test.ts
    @src/governance/state-store.test.ts
    @src/governance/gate-evidence-store.test.ts
    @.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
  </read_first>
  <behavior>
    - `verifyGateHook` reads the existing governance selection state and builds a `GateRequest` with `gateId: "verify"`, selected applied rules, task signal, phase, and strict ISO `requestedAt`.
    - default production path resolves `generic-exit-ci` from `ADAPTERS` and records a pass `GateResult`.
    - an injected malformed adapter result rejects through the `runAdapter` boundary and no verify evidence file is written.
    - injected `ECHO_ADAPTERS`/custom adapters are allowed in tests only to prove findings are persisted.
    - `deriveRuleGateStatuses(request, result)` returns one record for every `request.rules[]` id: `status: "fail"` when any finding id equals that rule id or references it as a distinct token, otherwise `status` equals overall `result.status` when the result is `pass` or `waived`.
    - tests cover exact finding id match, referenced finding id match, non-matching finding id, overall pass, overall waived, and ambiguous substring non-match.
    - missing selection state fails loud and does not synthesize empty verify evidence.
  </behavior>
  <action>Create `src/governance/verify-gate-hook.test.ts` before production code. Use `writeSelection` to seed a valid `GovernanceRecord`, `readGateEvidence` to inspect `.planning/governance/gates/08-verify.json`, and custom `GateAdapter` fixtures from `run-adapter.test.ts` to prove D-04. Add direct unit tests for `deriveRuleGateStatuses(request, result)` so each `request.rules[]` id maps to exactly one `RuleGateStatus`, failed findings win only on exact id or distinct-token id reference, unmatched rules inherit overall `pass` or `waived`, and no APPR-01, rollback, approval, or full audit fields are required. Cover validation item `08-W0-03`, D-06, and requirement GATE-04.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/verify-gate-hook.test.js" "dist-test/enforcement/run-adapter.test.js"</automated>
  </verify>
  <acceptance_criteria>RED tests fail before `verify-gate-hook.ts` exists and pin D-04/D-05/D-06 behavior, including per-rule pass/fail/waived derivation from persisted request/result data without Phase 9 audit expansion.</acceptance_criteria>
  <done>RED gate committed as `test(08-03): add failing tests for verify gate hook`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implementation for verifyGateHook</name>
  <files>src/governance/verify-gate-hook.ts, src/governance/verify-gate-hook.test.ts</files>
  <read_first>
    @src/governance/state-store.ts
    @src/governance/gate-evidence-store.ts
    @src/enforcement/adapters.ts
    @src/enforcement/run-adapter.ts
    @src/enforcement/types.ts
  </read_first>
  <action>Create `src/governance/verify-gate-hook.ts`. Define `VerifyGateHookArgs` with `projectRoot`, `phaseNumber`, optional `adapterName`, and optional `adapters` for tests. Define `RuleGateStatus` as `{ ruleId: string; status: "pass" | "fail" | "waived"; findingIds: string[] }` and export `deriveRuleGateStatuses(request, result)`. For each request rule, collect findings where `finding.id === rule.id` or the rule id appears inside `finding.id` as a distinct token bounded by string start/end or a non-alphanumeric delimiter; set that rule to `fail` with matching finding ids when any are present, otherwise inherit overall `result.status` when it is `pass` or `waived`. Read `GovernanceRecord` with `readSelection`; missing state throws `verifyGateHook: missing governance selection state at <path>`. Build `GateRequest` from the record using `gateId: "verify"`, `phase: record.phase`, `taskSignal: record.taskSignal`, `rules: record.selectionResult.selected`, and `requestedAt: new Date().toISOString()`. Resolve the adapter from `args.adapters ?? ADAPTERS` using `args.adapterName ?? "generic-exit-ci"`, throw if absent, call `runAdapter(adapter, request)`, derive `ruleStatuses`, and persist `{ request, result, metadata: { phase: phaseNumber, source: "aidlc-governance-verify", writtenAt } }` through `writeGateEvidence`; do not add APPR-01 approval capture, rollback evidence, test catalog, or full audit enrichment. Return `{ evidence, ruleStatuses }`. Add a direct runner accepting `<projectRoot> <phaseNumber>` that prints JSON containing evidence and ruleStatuses to stdout and uses `process.exitCode = 1` on errors.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/verify-gate-hook.test.js" "dist-test/enforcement/run-adapter.test.js"</automated>
  </verify>
  <acceptance_criteria>Verify hook cannot persist malformed adapter output, uses ADAPTERS no-op registry by default, writes valid verify evidence, and exposes tested per-rule pass/fail/waived records derived from request rules and findings.</acceptance_criteria>
  <done>GREEN gate committed as `feat(08-03): implement verify gate hook`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter -> verify hook | Adapter output is untrusted structured data. |
| Selection state -> verify request | Persisted selected rules become enforcement request input. |
| Verify hook -> evidence store | Adapter result becomes durable governance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | adapter output | high | mitigate | Route all verify adapter output through `runAdapter`; tests inject spoofed/malformed output and assert rejection. |
| T-08-10 | Spoofing | adapter attribution | high | mitigate | `runAdapter` enforces `evaluatedBy === adapter.name`; tests include mismatched attribution. |
| T-08-11 | Repudiation | verify evidence | high | mitigate | Persist request/result/metadata with source `aidlc-governance-verify` and selected applied rules. |
| T-08-12 | Information Disclosure | verify request | low | accept | Evidence stores rule summaries already selected by governance; no rule bodies or scanner output are introduced. |
| T-08-SC | Tampering | package installs | low | accept | No package install task exists. |
</threat_model>

<verification>
Plan-level checks:
- `npm run build:test && node --test "dist-test/governance/verify-gate-hook.test.js" "dist-test/enforcement/run-adapter.test.js"`
- `npm test` after Wave 2 merge.
</verification>

<success_criteria>
- GATE-04 is satisfied by a verify:post-ready hook module.
- D-04/D-05/D-06 are covered in tests and implementation.
- Wave 0 item `08-W0-03` is green.
- Production path uses `ADAPTERS` and the `runAdapter` boundary.
- Per-rule verify records are derived for every `request.rules[]` id without adding Phase 9 audit enrichment.
</success_criteria>

<output>
Create `.planning/phases/08-remaining-gate-hooks/08-03-SUMMARY.md` when done.
</output>
