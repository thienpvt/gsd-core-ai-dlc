---
phase: 08-remaining-gate-hooks
plan: 02
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/governance/plan-hook.ts
  - src/governance/plan-hook.test.ts
autonomous: true
requirements:
  - GATE-03
requirements_addressed:
  - GATE-03
must_haves:
  truths:
    - "Plan gate uses select() + renderInjection() and no second selector per D-01."
    - "Plan gate derives TaskSignal from phase goal, requirement IDs, risk/threat model text, acceptance criteria, and impacted files/modules, then renders summary-only governance context per D-02."
    - "Plan gate writes separate plan evidence and does not overwrite execute selection state per D-03."
  artifacts:
    - path: "src/governance/plan-hook.ts"
      provides: "plan:pre governance selection, fragment rendering, and plan evidence write"
      exports: ["PlanHookArgs", "PlanHookResult", "planHook"]
    - path: "src/governance/plan-hook.test.ts"
      provides: "Wave 0 GATE-03 plan hook RED coverage"
  key_links:
    - from: "src/governance/plan-hook.ts"
      to: "src/select/select.ts"
      via: "planHook validates signal, classifies risk, and calls select with SelectionConfig"
      pattern: "select\\("
    - from: "src/governance/plan-hook.ts"
      to: "src/governance/gate-evidence-store.ts"
      via: "planHook writes 08-plan.json without touching selection-state.json"
      pattern: "writeGateEvidence\\("
---

<objective>
Implement the plan gate hook for GATE-03.

Purpose: The planner must receive the same summary-only governance context pattern used by discuss/execute, while plan-time evidence is persisted separately for ship checks.
Output: `planHook(args)` plus tests proving D-01, D-02, D-03.
</objective>

<execution_context>
@C:/Users/thienpv/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thienpv/.codex/gsd-core/templates/summary.md
@C:/Users/thienpv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-remaining-gate-hooks/08-CONTEXT.md
@.planning/phases/08-remaining-gate-hooks/08-RESEARCH.md
@.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
@.planning/phases/08-remaining-gate-hooks/08-PATTERNS.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-01-SUMMARY.md
@.planning/phases/07-enforcement-contracts-adapter-stubs/07-04-SUMMARY.md
@src/governance/discuss-hook.ts
@src/governance/gate-evidence-store.ts
@src/governance/paths.ts
@src/select/select.ts
@src/select/validate-signal.ts
@src/inject/inject.ts
@src/governance/risk.ts
@src/enforcement/types.ts
</context>

## Artifacts This Phase Produces

Files and symbols are listed in `08-01-PLAN.md`; this plan owns `src/governance/plan-hook.ts`, `src/governance/plan-hook.test.ts`, `PlanHookArgs`, `PlanHookResult`, and `planHook(args)`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for plan gate selection and evidence</name>
  <files>src/governance/plan-hook.test.ts</files>
  <read_first>
    @src/governance/discuss-hook.test.ts
    @src/governance/execute-hook.test.ts
    @src/governance/gate-evidence-store.test.ts
    @.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
  </read_first>
  <behavior>
    - `derivePlannerTaskSignal` accepts `PlanTaskSignalInputs` containing `phaseGoal`, `requirementIds`, `riskThreatModel`, `acceptanceCriteria`, `impactedFiles`, and optional `impactedModules`, then returns a validated `TaskSignal`.
    - phase goal words contribute normalized keywords, requirement IDs contribute stable lowercase keywords such as `gate-03`, risk/threat text contributes threat/risk keywords such as `tampering`, acceptance criteria contribute behavior keywords, and impacted files/modules contribute normalized POSIX `paths`.
    - tests prove removing each source removes its corresponding derived keyword or path, so D-02 cannot regress to a caller-provided opaque signal.
    - `planHook` selects with the same core as `discussHook`, renders a `<governance>` fragment, and includes summaries plus rule-detail pointers only.
    - `planHook` writes `.planning/governance/gates/08-plan.json` with `request.gateId: "plan"`, `result.gateId: "plan"`, and `result.evaluatedBy: "aidlc-governance-plan"`.
    - `planHook` does not create or overwrite `.planning/governance/selection-state.json`.
    - when selection budget is exceeded, `planHook` still returns the fragment and records a failing plan `GateResult` with an actionable finding.
  </behavior>
  <action>Create `src/governance/plan-hook.test.ts` before production code. Mirror fixture index/state style from `discuss-hook.test.ts`; use `readGateEvidence` and `selectionStatePath` to prove D-03. Add focused tests for `derivePlannerTaskSignal`: one fixture source each for phase goal, requirement IDs, risk/threat model, acceptance criteria, impacted files, and impacted modules must produce an observable `TaskSignal.keywords` or `TaskSignal.paths` contribution, and removal of that source must remove the contribution. Include a summary-only assertion that the fragment starts with `<governance>`, contains selected rule summaries, and does not contain rule Markdown body text from the fixture. This covers validation item `08-W0-02`, D-02, and requirement GATE-03.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/plan-hook.test.js"</automated>
  </verify>
  <acceptance_criteria>RED tests pin D-01/D-02/D-03, including concrete TaskSignal derivation from all five D-02 input classes, and fail before `plan-hook.ts` exists.</acceptance_criteria>
  <done>RED gate committed as `test(08-02): add failing tests for plan gate hook`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implementation for planHook</name>
  <files>src/governance/plan-hook.ts, src/governance/plan-hook.test.ts</files>
  <read_first>
    @src/governance/discuss-hook.ts
    @src/governance/gate-evidence-store.ts
    @src/enforcement/types.ts
    @src/select/validate-signal.ts
    @src/select/select.ts
    @src/inject/inject.ts
  </read_first>
  <action>Create `src/governance/plan-hook.ts` as a thin hook wrapper. Define `PlanTaskSignalInputs` with `phaseGoal: string`, `requirementIds: string[]`, `riskThreatModel: string[]`, `acceptanceCriteria: string[]`, `impactedFiles: string[]`, and optional `impactedModules: string[]`; define `PlanHookArgs` with `projectRoot`, `phaseNumber`, `plannerInputs`, optional `statePath`, `indexPath`, `baseDomains`, and `budget`. Export `derivePlannerTaskSignal(inputs)` that normalizes tokens to lowercase ASCII, dedupes them, maps impacted files/modules to POSIX `paths`, sets `taskType: "security"` when risk/threat text is present and otherwise `"feature"`, and calls `validateSignal` before returning. Reuse the `discussHook` pattern after derivation: resolve phase from STATE.md, resolve/validate the rule index, `classifyRisk`, `riskAdjustedDomains`, `select`, and `renderInjection`. Build a `GateRequest` with `gateId: "plan"`, resolved phase, selected rules, the derived validated signal, and `requestedAt`. Build a `GateResult` evaluated by `aidlc-governance-plan`: `status: "pass"` when selection is within budget, `status: "fail"` with one finding when `selectionResult.budgetExceeded` is true. Write via `writeGateEvidence(args.projectRoot, args.phaseNumber, evidence)`. Return `{ fragment, evidence, taskSignal }`. Add a direct runner that accepts `<projectRoot> <phaseNumber> <plannerInputsJsonFile>` and writes the fragment to stdout; errors go to stderr with `process.exitCode = 1`.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/plan-hook.test.js"</automated>
  </verify>
  <acceptance_criteria>Plan hook derives and validates TaskSignal from the D-02 planner input fields, renders summary-only governance, writes valid plan evidence with the derived signal, and leaves execute selection state untouched.</acceptance_criteria>
  <done>GREEN gate committed as `feat(08-02): implement plan gate hook`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner context -> selection core | Untrusted planning prose becomes a structured `TaskSignal`. |
| Plan hook -> planner context | Selected rule summaries become injected planner context. |
| Plan hook -> evidence store | Plan gate result becomes durable ship input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Information Disclosure | `planHook` rendering | high | mitigate | Use `renderInjection(selectionResult)` only; tests assert summaries/pointers only and no body text. |
| T-08-06 | Tampering | plan evidence state | high | mitigate | Write to `.planning/governance/gates/{NN}-plan.json` via `writeGateEvidence`; never call selection-state writer. |
| T-08-07 | Denial of Service | token budget overflow | medium | mitigate | Preserve fragment output and record a failing plan `GateResult` with budget offenders. |
| T-08-08 | Spoofing | `TaskSignal` input | medium | mitigate | Run `validateSignal` before selection and fail loud on malformed signal. |
| T-08-SC | Tampering | package installs | low | accept | No package install task exists. |
</threat_model>

<verification>
Plan-level checks:
- `npm run build:test && node --test "dist-test/governance/plan-hook.test.js"`
- `npm test` after Wave 2 merge.
</verification>

<success_criteria>
- GATE-03 is satisfied by a plan:pre-ready hook module.
- D-01/D-02/D-03 are covered in tests and implementation.
- Wave 0 item `08-W0-02` is green.
- Plan hook writes plan evidence only.
</success_criteria>

<output>
Create `.planning/phases/08-remaining-gate-hooks/08-02-SUMMARY.md` when done.
</output>
