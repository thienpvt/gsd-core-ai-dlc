---
phase: 08-remaining-gate-hooks
plan: 05
type: tdd
wave: 3
depends_on:
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - .claude/skills/aidlc-governance-plan/SKILL.md
  - .claude/skills/aidlc-governance-verify/SKILL.md
  - .claude/skills/aidlc-governance-ship/SKILL.md
  - .gsd/capabilities/aidlc-governance/capability.json
  - src/governance/audit-hook-contract.test.ts
  - src/governance/consent.test.ts
  - src/governance/consent-verify-post.test.ts
autonomous: true
requirements:
  - GATE-03
  - GATE-04
  - GATE-05
requirements_addressed:
  - GATE-03
  - GATE-04
  - GATE-05
must_haves:
  truths:
    - "Capability manifest exposes `aidlc-governance-plan` at `plan:pre` for GATE-03 and feeds it phase goal, requirement IDs, risk/threat model, acceptance criteria, and impacted file/module inputs per D-01 and D-02."
    - "Capability manifest exposes verify evidence at `verify:post` and preserves audit verify:post step for GATE-04 per D-04 and D-08."
    - "Capability manifest exposes `aidlc-governance-ship` at `ship:pre` for GATE-05 per D-07 and D-08."
  artifacts:
    - path: ".claude/skills/aidlc-governance-plan/SKILL.md"
      provides: "plan:pre marshal-and-invoke skill"
    - path: ".claude/skills/aidlc-governance-verify/SKILL.md"
      provides: "verify:post marshal-and-invoke skill"
    - path: ".claude/skills/aidlc-governance-ship/SKILL.md"
      provides: "ship:pre marshal-and-invoke skill"
    - path: ".gsd/capabilities/aidlc-governance/capability.json"
      provides: "registered plan, verify, and ship governance steps"
  key_links:
    - from: ".gsd/capabilities/aidlc-governance/capability.json"
      to: ".claude/skills/aidlc-governance-plan/SKILL.md"
      via: "plan:pre ref.skill"
      pattern: "aidlc-governance-plan"
    - from: ".gsd/capabilities/aidlc-governance/capability.json"
      to: ".claude/skills/aidlc-governance-verify/SKILL.md"
      via: "verify:post ref.skill"
      pattern: "aidlc-governance-verify"
    - from: ".gsd/capabilities/aidlc-governance/capability.json"
      to: ".claude/skills/aidlc-governance-ship/SKILL.md"
      via: "ship:pre ref.skill"
      pattern: "aidlc-governance-ship"
---

<objective>
Register the Phase 8 gate hooks with the GSD capability host.

Purpose: Hook modules do not govern the loop until capability skills and manifest steps expose them at the canonical plan, verify, and ship points without replacing existing discuss/execute/audit behavior.
Output: three skill docs, additive manifest steps, and integration tests for consent/render-hooks contracts.
</objective>

<execution_context>
@C:/Users/thienpv/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/thienpv/.codex/gsd-core/templates/summary.md
@C:/Users/thienpv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-remaining-gate-hooks/08-CONTEXT.md
@.planning/phases/08-remaining-gate-hooks/08-RESEARCH.md
@.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
@.planning/phases/08-remaining-gate-hooks/08-PATTERNS.md
@.gsd/capabilities/aidlc-governance/capability.json
@.claude/skills/aidlc-governance-discuss/SKILL.md
@.claude/skills/aidlc-governance-execute/SKILL.md
@.claude/skills/aidlc-governance-audit/SKILL.md
@src/governance/audit-hook-contract.test.ts
@src/governance/consent.test.ts
@src/governance/consent-verify-post.test.ts
@src/governance/plan-hook.ts
@src/governance/verify-gate-hook.ts
@src/governance/ship-gate-hook.ts
</context>

## Artifacts This Phase Produces

Files and symbols are listed in `08-01-PLAN.md`; this plan owns the three `.claude/skills/aidlc-governance-*` skill docs, capability manifest wiring, and render-hooks/consent test updates.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for capability hook registration</name>
  <files>src/governance/audit-hook-contract.test.ts, src/governance/consent.test.ts, src/governance/consent-verify-post.test.ts</files>
  <read_first>
    @src/governance/audit-hook-contract.test.ts
    @src/governance/consent.test.ts
    @src/governance/consent-verify-post.test.ts
    @.gsd/capabilities/aidlc-governance/capability.json
    @.planning/phases/08-remaining-gate-hooks/08-VALIDATION.md
  </read_first>
  <behavior>
    - manifest skills include `aidlc-governance-plan`, `aidlc-governance-verify`, and `aidlc-governance-ship` while preserving existing discuss/execute/audit skills.
    - manifest steps include additive `plan:pre`, `verify:post`, and `ship:pre` entries with expected `produces`, `consumes`, `when: "governance.enabled"`, and `onError` policy.
    - verify manifest assertions prove both `aidlc-governance-verify` and existing `aidlc-governance-audit` are present at `verify:post`, with verify evidence before audit in the manifest.
    - plan manifest and skill-doc contract assertions prove the plan skill consumes or names sources for phase goal, requirement IDs, risk/threat model, acceptance criteria, impacted files, and impacted modules before invoking `plan-hook`.
    - consent integration tests add fixture skills for the three new refs and assert inactive pre-consent, active post-consent, inactive after revoke, and inactive after tamper where existing tests already cover that lifecycle.
  </behavior>
  <action>Extend tests before changing manifest or creating skill docs. In `audit-hook-contract.test.ts`, add filters for `aidlc-governance-plan`, `aidlc-governance-verify`, and `aidlc-governance-ship`; keep the existing audit test. Add a plan-skill contract assertion that `.claude/skills/aidlc-governance-plan/SKILL.md` instructs the host to collect `phaseGoal`, `requirementIds`, `riskThreatModel`, `acceptanceCriteria`, `impactedFiles`, and `impactedModules`, then pass a `PlanTaskSignalInputs` JSON file to `plan-hook`; this proves D-02 source coverage at the capability boundary. In `consent.test.ts`, add runtime fixture skills `gsd-aidlc-governance-plan`, `gsd-aidlc-governance-verify`, and `gsd-aidlc-governance-ship`, then assert post-consent `plan:pre`, `execute:pre`, and `ship:pre` hooks. In `consent-verify-post.test.ts`, add a helper that finds verify hooks by skill and assert both verify evidence and audit hooks carry `onError: "halt"` and appear in verify-before-audit order. This covers validation item `08-W0-05`, D-02, and D-08 composition.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/audit-hook-contract.test.js" "dist-test/governance/consent.test.js" "dist-test/governance/consent-verify-post.test.js"</automated>
  </verify>
  <acceptance_criteria>RED tests fail before manifest/skill changes, preserve all existing hook assertions, prove D-02 source coverage at `plan:pre`, and prove verify evidence renders before audit at `verify:post`.</acceptance_criteria>
  <done>RED gate committed as `test(08-05): add failing tests for remaining gate hook registration`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN manifest and skill wiring</name>
  <files>.claude/skills/aidlc-governance-plan/SKILL.md, .claude/skills/aidlc-governance-verify/SKILL.md, .claude/skills/aidlc-governance-ship/SKILL.md, .gsd/capabilities/aidlc-governance/capability.json</files>
  <read_first>
    @.claude/skills/aidlc-governance-discuss/SKILL.md
    @.claude/skills/aidlc-governance-execute/SKILL.md
    @.claude/skills/aidlc-governance-audit/SKILL.md
    @.gsd/capabilities/aidlc-governance/capability.json
    @src/governance/plan-hook.ts
    @src/governance/verify-gate-hook.ts
    @src/governance/ship-gate-hook.ts
  </read_first>
  <action>Create the three skill docs as thin marshal-and-invoke prompts. `aidlc-governance-plan` collects planner input sources into `PlanTaskSignalInputs`: phase goal from `.planning/ROADMAP.md`, requirement IDs from the phase `Requirements` line plus `.planning/REQUIREMENTS.md`, risk/threat text from phase research/security/threat sections when present, acceptance criteria from roadmap/requirements/validation text, and impacted files/modules from phase context canonical refs, pattern map file assignments, and plan frontmatter when present. It writes that JSON to a temp file, invokes `node dist/governance/plan-hook.js <projectRoot> <phaseNumber> <plannerInputsJsonFile>`, and attaches stdout `<governance>` to planner context per D-01/D-02. `aidlc-governance-verify` invokes `node dist/governance/verify-gate-hook.js <projectRoot> <phaseNumber>` and fails loud on non-zero exit per D-04/D-06. `aidlc-governance-ship` invokes `node dist/governance/ship-gate-hook.js <projectRoot> <phaseNumber>` and fails loud on missing/failing evidence per D-07. Update `capability.json` by appending skills and additive steps only: `plan:pre` consumes `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, phase `*-CONTEXT.md`, `*-RESEARCH.md`, `*-VALIDATION.md`, and `*-PATTERNS.md`, and produces `planner-context` plus `.planning/governance/gates/{NN}-plan.json` with `onError: "skip"` to match context-injection anti-bloat behavior; `verify:post` produces `.planning/governance/gates/{NN}-verify.json`, consumes `.planning/governance/selection-state.json`, and uses `onError: "halt"`; `ship:pre` consumes plan/verify evidence, produces `.planning/governance/gates/{NN}-ship.json`, and uses `onError: "halt"`. Preserve existing discuss, execute, and audit steps exactly; verify evidence must appear before audit at `verify:post` so Phase 9 can consume evidence from a stable manifest order. Do not add APPR-01 approval capture or real scanner integration per D-05/D-09.</action>
  <verify>
    <automated>npm run build:test</automated>
    <automated>node --test "dist-test/governance/audit-hook-contract.test.js" "dist-test/governance/consent.test.js" "dist-test/governance/consent-verify-post.test.js"</automated>
  </verify>
  <acceptance_criteria>Capability render-hooks expose the new plan, verify, and ship governance hooks only after consent; plan hook inputs cover every D-02 source class; verify evidence precedes audit at `verify:post`; existing discuss/execute/audit hooks remain active and unchanged.</acceptance_criteria>
  <done>GREEN gate committed as `feat(08-05): register remaining governance gate hooks`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capability registry -> hook invocation | Manifest controls which skills run at GSD loop points. |
| Consent store -> active hooks | User consent and content hash control hook activation. |
| Skill prompt -> dist runner | Markdown skill must not duplicate governance logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-17 | Tampering | `capability.json` steps | high | mitigate | Tests assert existing steps remain and new steps are additive with exact points/ref/producers/consumers/onError. |
| T-08-18 | Elevation of Privilege | consent activation | high | mitigate | Consent tests assert pre-consent inactive, post-consent active, revoke inactive, and tamper inactive for new hook points. |
| T-08-19 | Repudiation | verify hook ordering | medium | mitigate | Manifest contract test asserts verify evidence step and existing audit step both exist and order is explicit. |
| T-08-20 | Tampering | skill docs | medium | mitigate | Skill docs invoke `dist/governance/*hook.js` runners and do not duplicate selector, adapter, ship, or audit logic. |
| T-08-SC | Tampering | package installs | low | accept | No package install task exists. |
</threat_model>

<verification>
Plan-level checks:
- `npm run build:test && node --test "dist-test/governance/audit-hook-contract.test.js" "dist-test/governance/consent.test.js" "dist-test/governance/consent-verify-post.test.js"`
- `npm test` after Wave 3 merge.
</verification>

<success_criteria>
- GATE-03/GATE-04/GATE-05 hooks are registered at `plan:pre`, `verify:post`, and `ship:pre`.
- D-01/D-02/D-04/D-07/D-08 are visible in manifest, skill-doc, and consent tests.
- Wave 0 item `08-W0-05` is green.
- No existing governance hook is removed or replaced.
</success_criteria>

<output>
Create `.planning/phases/08-remaining-gate-hooks/08-05-SUMMARY.md` when done.
</output>
