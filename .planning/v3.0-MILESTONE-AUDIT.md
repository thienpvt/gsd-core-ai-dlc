---
milestone: v3.0
name: Adoption & Hygiene
audited: 2026-07-09
status: gaps_found
scores:
  requirements: 4/4
  phases: 2/2
  integration: 3/4
  flows: 3/4
gaps:
  requirements:
    - id: "DOC-01"
      status: "partial"
      phase: "12"
      claimed_by_plans: ["12-01-PLAN.md"]
      completed_by_plans: ["12-01-SUMMARY.md"]
      verification_status: "passed"
      evidence: "docs/onboarding.md documents the real `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw` consent grant (matches consent.test.ts grantConsent). Phase 12 verification passed (static + runtime smoke: build-index + select --phase inception + require-mfa selected). However the integration checker ran the documented consent command and the gsd-core cross-capability validator rejects the install because capability.json `plan:pre` step consumes `RESEARCH.md` and `PATTERNS.md`, which no host artifact or capability hook produces. Direct CLI smoke passes; governance hooks do not activate for a new user. Root cause = RUN-01 upstream gsd-core constraint (already deferred in STATE.md, out of v3.0 scope)."
    - id: "DOC-02"
      status: "partial"
      phase: "12"
      claimed_by_plans: ["12-01-PLAN.md"]
      completed_by_plans: ["12-01-SUMMARY.md"]
      verification_status: "passed"
      evidence: "All 5 documented CLI commands (build-index, select, inject, rule-detail, eval) run as written against bin/governance.cjs — integration checker verified each. End-to-end worked example chains successfully. The audit/ship gate HOOK chain cannot operate for a new user only because the capability never activates (same RUN-01 blocker as DOC-01). The CLI commands themselves are fully documented and runnable."
  integration:
    - "onboarding consent → capability activation: BLOCKER — `gsd-tools capability install` fails on unproduced RESEARCH.md/PATTERNS.md consumes (RUN-01 upstream). Affects DOC-01 full install→consent→activate flow and DOC-02 audit/ship hook chain. NOT a Phase 12 doc defect: the documented command is the real consent API (consent.test.ts); the failure is a pre-existing capability-manifest `consumes` mismatch deferred from v2.0."
  flows:
    - "Install → consent → smoke (onboarding.md): PARTIAL — install + direct CLI smoke work; consent-activate blocked by RUN-01 so governance hooks do not fire for a new user following the doc end-to-end."
tech_debt:
  - phase: 12-onboarding-rule-authoring-docs
    items:
      - "WARNING (DOC-03): optional `rule-detail billing-review --index <temp-pack>` step in docs/rule-authoring.md fails when the temp rule pack lives outside the repo root (mktemp -d) — rule-detail.ts uses process.cwd() as containment root, so detailPath './details/billing-review-detail.md' is rejected as escaping the pack (IN-05). The core verify-the-rule-fires loop (build-index + positive/negative select) passes. Optional detail-fetch example only; non-blocking."
      - "RUN-01 (carried from v2.0): capability.json `plan:pre` consumes RESEARCH.md + PATTERNS.md which host hooks produce but the cross-capability validator cannot resolve. Blocks `gsd-tools capability install` for a new user. Out of v3.0 scope (upstream gsd-core constraint)."
  - phase: 11-summary-frontmatter-hygiene
    items: []
---

# Milestone v3.0 — Adoption & Hygiene — Audit

**Audited:** 2026-07-09
**Status:** gaps_found — all 4 requirements satisfied per 3-source cross-reference (VERIFICATION + SUMMARY frontmatter + REQUIREMENTS.md traceability), but the integration checker found 1 runtime blocker on the DOC-01 install→consent→activate flow rooted in the previously-deferred RUN-01 upstream constraint.

## Scores

| Dimension | Score |
|-----------|-------|
| Requirements (3-source) | 4/4 satisfied |
| Phases verified | 2/2 passed |
| Integration links | 3/4 wired (1 blocker) |
| E2E flows | 3/4 complete (1 partial) |

## 3-Source Requirements Cross-Reference

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Integration | Final |
|--------|-------|-----------------|----------------------|------------------|-------------|-------|
| TD-10 | 11 | passed (3/3) | listed (11-01) | [x] | WIRED | satisfied |
| DOC-01 | 12 | passed (14/14) | listed (12-01) | [x] | PARTIAL (RUN-01) | satisfied (3-source) / partial (runtime) |
| DOC-02 | 12 | passed (14/14) | listed (12-01) | [x] | PARTIAL (RUN-01) | satisfied (3-source) / partial (runtime) |
| DOC-03 | 12 | passed (14/14) | listed (12-02) | [x] | WIRED (1 optional warning) | satisfied |

**Orphan detection:** none — every v3.0 REQ-ID (TD-10, DOC-01, DOC-02, DOC-03) appears in a phase VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md traceability row.

## Phase Verification Summary

| Phase | Name | Status | Score |
|-------|------|--------|-------|
| 11 | SUMMARY Frontmatter Hygiene | passed | 3/3 must-haves |
| 12 | Onboarding & Rule-Authoring Docs | passed | 14/14 must-haves |

## Integration Check

**Wired (3/4):**
- TD-10 → archived v2 VERIFICATION.md source-plan mapping → archived v2 SUMMARY `requirements-completed` → v2 union scanner → milestone audit (CRLF-tolerant, 21/21 v2 REQ-IDs covered).
- DOC-02 → docs/governance-workflow.md → bin/governance.cjs (all 5 CLI commands run as documented) — CLI surface wired.
- DOC-03 → docs/rule-authoring.md → frontmatter.schema.json → temp rule pack → build-index → select positive/negative (billing-review fires for construction+billing, skips out-of-scope-by-trigger / out-of-phase) — wired.
- Discoverability → README links all 3 docs; each doc links install→operate→author.

**Blocked (1/4):**
- DOC-01 → onboarding consent command → capability activation: `gsd-tools capability install ./.gsd/capabilities/aidlc-governance --scope project --yes --raw` fails. gsd-core cross-capability validator rejects because `aidlc-governance/capability.json` `plan:pre` step `consumes: [CONTEXT.md, RESEARCH.md, PATTERNS.md]` and RESEARCH.md/PATTERNS.md are produced by host (gsd-core) hooks, not by this capability's own steps — the validator cannot resolve them. This is the RUN-01 upstream constraint deferred from v2.0 (STATE.md line 130). The documented command is the real consent API (consent.test.ts grantConsent); the failure is a pre-existing capability-manifest `consumes` mismatch, not a Phase 12 doc defect.

## E2E Flows

| # | Flow | Result | Affected REQ-IDs |
|---|------|--------|------------------|
| 1 | Install → consent → smoke (onboarding.md) | PARTIAL | DOC-01 (runtime activation blocked by RUN-01) |
| 2 | Operate 5 CLI commands (governance-workflow.md) | PASS | DOC-02 |
| 3 | Author → integrate → verify rule (rule-authoring.md) | PASS (1 optional warning) | DOC-03 |
| 4 | Discoverability README → 3 docs → cross-refs | PASS | (criterion 4) |

## Tech Debt

- **RUN-01 (carried from v2.0, out of v3.0 scope):** capability.json `plan:pre` consumes RESEARCH.md + PATTERNS.md. Host produces them via gsd-core's research + pattern-mapper hooks, but the cross-capability validator cannot resolve host-produced consumes. Blocks `gsd-tools capability install` for a new end user → governance hooks do not activate via the documented consent flow. Resolution requires an upstream gsd-core fix to the capability manifest `consumes` model (allow host-produced consumes), tracked as RUN-01.
- **DOC-03 optional warning:** `rule-detail billing-review --index <temp-pack>` in docs/rule-authoring.md fails when the temp pack is outside the repo root (rule-detail.ts uses process.cwd() as containment root → detailPath rejected as escaping pack, IN-05). The core verify loop (build-index + select positive/negative) passes. Non-blocking; the optional detail-fetch example can be made repo-relative or skipped.

## Nyquist

Skipped — no phase RESEARCH.md / VALIDATION.md (docs + hygiene milestone; Nyquist not applicable to documentation work).

## Recommendation

The 3-source requirements cross-reference is fully satisfied (4/4), and both phases passed verification. The single integration blocker (DOC-01 runtime consent-activate) is the previously-deferred RUN-01 upstream constraint, not a v3.0 defect — the documented command is accurate. Options:

1. **Accept gaps (complete milestone):** the deferred RUN-01 is already tracked; the docs are correct and will work once the upstream capability-manifest fix lands. Complete v3.0.
2. **Stop — fix RUN-01 first:** out of v3.0 scope (upstream gsd-core constraint); would require a new milestone or upstream contribution.