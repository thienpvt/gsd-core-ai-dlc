---
phase: 260709-uk6-add-private-npm-registry-install-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .npmrc.example
  - .gitignore
  - README.md
  - docs/onboarding.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
must_haves:
  truths:
    - package.json keeps name @opengsd/gsd-aidlc-overlay and declares publishConfig.registry placeholder for org private registry
    - .npmrc.example maps @opengsd scope + auth token placeholders; real .npmrc with secrets is gitignored
    - README.md and docs/onboarding.md lead install with private-registry path (configure .npmrc then npm install @opengsd/gsd-aidlc-overlay) after gsd-core
    - Docs keep git clone / file: / git+ssh as fallback and add short maintainer publish section (npm run build && npm publish)
    - Docs explicitly state org private registry only — not public npmjs.com; @opengsd scope is local to that registry
  artifacts:
    - package.json
    - .npmrc.example
    - .gitignore
    - README.md
    - docs/onboarding.md
  key_links:
    - package.json publishConfig.registry matches the registry URL placeholder used in .npmrc.example
    - README Installation and docs/onboarding.md Installation describe the same private-registry primary path
    - .gitignore covers .npmrc so committed tokens cannot land in git
---

<objective>
Add shortest private-npm-registry install/publish path: package publishConfig, `.npmrc.example`, gitignore hygiene, and docs that lead with org private registry (not public npmjs.com).

Purpose: Org members install `@opengsd/gsd-aidlc-overlay` from self-hosted registry after gsd-core; maintainers publish via `npm publish` against that registry. No registry-server scaffolding.

Output: Updated `package.json`, new `.npmrc.example`, `.gitignore` entry, rewritten install/publish sections in `README.md` + `docs/onboarding.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260709-ucq-assess-private-org-install/260709-ucq-ASSESSMENT.md
@.planning/quick/260709-ucq-assess-private-org-install/260709-ucq-SUMMARY.md
@package.json
@README.md
@docs/onboarding.md
@.gitignore

Prior quick (260709-ucq): install docs fixed to private git / local / file: only — no public npm claim. This task *adds* org private-registry as the **primary** install path while keeping git/file/git+ssh as fallbacks. Still never claim public npmjs.com ownership of `@opengsd`.

Constraints (hard):
- Keep package name `@opengsd/gsd-aidlc-overlay` — do not rename
- No new runtime deps
- No Verdaccio / GitHub Packages / CI publish pipeline scaffolding
- Placeholders only for registry URL and auth token — never real secrets
- Target docs+config only; ~30% context
</context>

<tasks>

<task type="auto">
  <name>Task 1: publishConfig + .npmrc.example + gitignore</name>
  <files>package.json, .npmrc.example, .gitignore</files>
  <read_first>
    - package.json (name must stay @opengsd/gsd-aidlc-overlay; no publishConfig today)
    - .gitignore (confirm whether .npmrc already ignored)
  </read_first>
  <action>
    1. **package.json** — keep `"name": "@opengsd/gsd-aidlc-overlay"`. Add top-level `publishConfig` with registry placeholder only, e.g. `"publishConfig": { "registry": "https://npm.example-org.local/repository/npm-private/" }`. Optional: add `repository` field if useful (git URL placeholder OK). Do not change version, files, scripts, deps, or engines. No new dependencies.

    2. **`.npmrc.example`** (new, committed) — project-level template for org members. Include:
       - Scope map: `@opengsd:registry=https://npm.example-org.local/repository/npm-private/` (same host/path style as publishConfig)
       - Auth token placeholder on that registry host, using npm's `_authToken` form with a clearly fake value such as `REPLACE_WITH_ORG_TOKEN` (or `${NPM_TOKEN}` env-style comment + static placeholder — pick one pattern and document it in a one-line comment at top of file)
       - Short header comment: copy to `.npmrc` (project, user, or org-managed); never commit real tokens; this is org private registry not public npmjs.com
       - Do not include real credentials

    3. **`.gitignore`** — ensure `.npmrc` is ignored (add if missing). Keep ignoring only the secret-bearing file; do **not** ignore `.npmrc.example`.

    Placeholder host `npm.example-org.local` (or equivalent clearly-fake org host) is intentional — executor must not invent a real org URL.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='@opengsd/gsd-aidlc-overlay') process.exit(1); if(!p.publishConfig||!p.publishConfig.registry) process.exit(2); console.log('ok', p.publishConfig.registry)" && test -f .npmrc.example && grep -q '@opengsd:registry=' .npmrc.example && grep -qE '_authToken|NPM_TOKEN|REPLACE_WITH' .npmrc.example && grep -q '^\.npmrc$' .gitignore && ! grep -q '^\.npmrc\.example$' .gitignore</automated>
  </verify>
  <done>
    package.json has publishConfig.registry placeholder and unchanged name; `.npmrc.example` maps `@opengsd` + auth placeholder; `.npmrc` gitignored; no secrets committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lead docs with private-registry install + maintainer publish</name>
  <files>README.md, docs/onboarding.md</files>
  <read_first>
    - README.md Installation section (current private git / file: primary from 260709-ucq)
    - docs/onboarding.md Installation + prerequisites (same)
    - package.json publishConfig + .npmrc.example from Task 1 (use same placeholder registry URL)
  </read_first>
  <action>
    Rewrite install sections so **org private registry is the primary path**. Keep gsd-core as hard prerequisite. Keep git clone / `file:` / `git+ssh` as explicit fallbacks. Add short maintainer publish blurb. Apply same facts to both README.md and docs/onboarding.md (onboarding can be slightly more step-by-step).

    **Must include (both files):**

    1. **Prerequisite** — Node `>=22`, npm `>=10`, GSD Core (`@opengsd/gsd-core` / `gsd-tools`) already installed.

    2. **Primary install (private registry):**
       - Copy `.npmrc.example` → project/user `.npmrc` (or use org-managed user/global npmrc)
       - Replace registry URL + token placeholders with org values
       - `npm install @opengsd/gsd-aidlc-overlay`
       - Note: still requires gsd-core first

    3. **Explicit scope disclaimer** (bold or blockquote, both files): this is an **org private registry**, **not** public npmjs.com. Package name may use `@opengsd` only because the private registry owns that scope locally. Do **not** claim public npmjs.com ownership of `@opengsd` / do not present this as a public-registry package.

    4. **Fallback install** (keep, demote under "Fallback" heading):
       - git clone + `npm install` + `npm run build` in repo
       - consumer `npm install file:../gsd-core-ai-dlc` or path
       - `npm install git+ssh://git@&lt;org-host&gt;/&lt;team&gt;/gsd-core-ai-dlc.git`

    5. **Maintainer publish** (short section, both files or README + onboarding "For maintainers"):
       - Ensure publishConfig / auth point at org private registry
       - `npm run build && npm publish`
       - Reminder: publishes to org private registry only (via publishConfig), never instruct publishing to public npmjs.com

    6. Keep existing links to onboarding / other docs. Do not invent Verdaccio/GitHub Packages setup steps. Do not rename package. Do not add new runtime deps or scripts unless already present.

    Tone: terse, operational. Match existing doc voice.
  </action>
  <verify>
    <automated>grep -n 'npm install @opengsd/gsd-aidlc-overlay' README.md docs/onboarding.md | head -20; grep -nE 'private registry|not.*public npm|npmjs\.com' README.md docs/onboarding.md | head -30; grep -nE 'npm publish|\.npmrc' README.md docs/onboarding.md | head -20; grep -nE 'file:|git\+ssh|git clone' README.md docs/onboarding.md | head -20; node -e "const p=require('./package.json'); if(p.name!=='@opengsd/gsd-aidlc-overlay') process.exit(1)"</automated>
  </verify>
  <done>
    Both docs lead with private-registry `.npmrc` + `npm install @opengsd/gsd-aidlc-overlay`; disclaimer blocks public-npm ownership claims; fallbacks and maintainer `npm run build && npm publish` present; package name unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → private registry | publishConfig + auth token authorize package upload |
| org member → private registry | `.npmrc` token authorizes scoped install of `@opengsd/*` |
| git repo → consumers | committed templates must not leak real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-uk6-01 | Information Disclosure | `.npmrc` with auth token | high | mitigate | gitignore `.npmrc`; commit only `.npmrc.example` with fake placeholders |
| T-uk6-02 | Spoofing | public npmjs.com vs private registry | medium | mitigate | docs explicitly state org private registry only; never instruct public publish; scope disclaimer |
| T-uk6-03 | Tampering | publishConfig.registry mis-pointed | medium | mitigate | single placeholder URL shared between package.json and `.npmrc.example`; docs say replace with org URL |
| T-uk6-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages installed in this plan |
</threat_model>

<verification>
- `package.json` name still `@opengsd/gsd-aidlc-overlay` with `publishConfig.registry`
- `.npmrc.example` exists; `.npmrc` in `.gitignore`
- README + onboarding: primary private-registry install, disclaimer, fallbacks, maintainer publish
- No real tokens; no registry-server code; no package rename; no new deps
</verification>

<success_criteria>
Org member with configured private-registry `.npmrc` can follow docs to `npm install @opengsd/gsd-aidlc-overlay` after gsd-core; maintainer can follow `npm run build && npm publish` against private registry; public npmjs.com ownership never claimed.
</success_criteria>

<output>
Create `.planning/quick/260709-uk6-add-private-npm-registry-install-publish/260709-uk6-SUMMARY.md` when done
</output>
